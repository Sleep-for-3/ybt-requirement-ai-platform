"use client";import {useEffect,useState} from "react";import {useParams} from "next/navigation";import {WorkspaceHeader} from "@/components/WorkspaceHeader";import {apiGet} from "@/lib/api";export default function Page(){const id=Number(useParams<{runId:string}>().runId);const[run,setRun]=useState<Record<string,unknown>|null>(null);const[results,setResults]=useState<Record<string,unknown>[]>([]);useEffect(()=>{if(id)void Promise.all([apiGet<Record<string,unknown>>(`/evaluation-runs/${id}`),apiGet<Record<string,unknown>[]>(`/evaluation-runs/${id}/results`)]).then(([a,b])=>{setRun(a);setResults(b)})},[id]);return <main><WorkspaceHeader title="评测结果" meta={String(run?.status||"")}/><pre className="panel mx-auto mt-6 max-w-5xl overflow-auto p-4">{JSON.stringify({run,results},null,2)}</pre></main>}
