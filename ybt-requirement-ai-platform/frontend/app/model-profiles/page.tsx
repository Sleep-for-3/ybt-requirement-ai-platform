"use client";import {useEffect,useState} from "react";import {WorkspaceHeader} from "@/components/WorkspaceHeader";import {apiGet} from "@/lib/api";export default function Page(){const[items,setItems]=useState<Record<string,unknown>[]>([]);useEffect(()=>{void apiGet<Record<string,unknown>[]>("/model-profiles").then(setItems)},[]);return <main><WorkspaceHeader title="模型配置" meta="密钥仅通过环境变量引用"/><pre className="panel mx-auto mt-6 max-w-5xl overflow-auto p-4">{JSON.stringify(items,null,2)}</pre></main>}
