"use client";
import { useEffect,useState } from "react";import { WorkspaceHeader } from "@/components/WorkspaceHeader";import { useProjectWorkspace } from "@/components/ProjectContext";import { apiGet } from "@/lib/api";
type Audit={id:number;action:string;resource_type:string;resource_id?:string|null;actor_user_id?:number|null;result:string;created_at:string};
export default function Page(){const{projectId}=useProjectWorkspace();const[items,setItems]=useState<Audit[]>([]);useEffect(()=>{if(projectId)apiGet<Audit[]>(`/audit?project_id=${projectId}`).then(setItems).catch(()=>setItems([]));},[projectId]);return <main><WorkspaceHeader title="操作审计" meta="不可修改的脱敏操作摘要"/><section className="panel mx-auto mt-6 max-w-6xl overflow-hidden">{items.map(item=><div className="grid grid-cols-[160px_140px_1fr_100px] border-b border-line p-3 text-sm" key={item.id}><span>{item.action}</span><span>{item.resource_type}</span><span>资源 #{item.resource_id||"-"} · 操作者 #{item.actor_user_id||"系统"}</span><span>{item.result}</span></div>)}</section></main>}
