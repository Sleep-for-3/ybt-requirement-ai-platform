"use client";
import {useEffect,useState} from "react";import {useProjectWorkspace} from "@/components/ProjectContext";import {WorkspaceHeader} from "@/components/WorkspaceHeader";import {LineageNode,apiGet,apiPost} from "@/lib/api";
type Candidate={id:number;candidate_type:string;candidate_id:number;score:number;match_reason:string;selected_flag:boolean};type Unresolved=LineageNode&{candidates:Candidate[]};
export default function Page(){const{projectId}=useProjectWorkspace();const[items,setItems]=useState<Unresolved[]>([]);async function load(){if(projectId)setItems(await apiGet(`/projects/${projectId}/lineage/unresolved`))}useEffect(()=>{void load()},[projectId]);async function select(node:number,candidate:number){await apiPost(`/lineage/nodes/${node}/resolution-candidates/${candidate}/select`,{});await load()}return <main><WorkspaceHeader title="未解析血缘节点" meta="多候选必须人工选择；选择结果可随时解绑"/><section className="panel mx-auto mt-6 max-w-6xl">{items.map(node=><div className="border-b p-4" key={node.id}><div className="flex justify-between"><div><strong className="font-mono text-sm">{node.logical_name}</strong><div className="text-xs text-slate-500">{node.node_type} · 节点 #{node.id}</div></div><span className="text-xs text-amber-700">unresolved</span></div>{node.candidates.length?<div className="mt-3 grid gap-2">{node.candidates.map(candidate=><div className="flex flex-wrap items-center justify-between gap-2 rounded border p-3 text-sm" key={candidate.id}><div><strong>{candidate.candidate_type} #{candidate.candidate_id}</strong><div className="text-xs text-slate-500">{candidate.match_reason} · {Math.round(candidate.score*100)}%</div></div><button className="button-primary" onClick={()=>select(node.id,candidate.id)}>选择绑定</button></div>)}</div>:<p className="mt-3 text-sm text-slate-500">当前无结构化候选，需要先同步元数据目录或导入业务模型。</p>}</div>)}</section></main>}
