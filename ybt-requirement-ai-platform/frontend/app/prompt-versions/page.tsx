"use client";import {useEffect,useState} from "react";import {WorkspaceHeader} from "@/components/WorkspaceHeader";import {apiGet} from "@/lib/api";export default function Page(){const[items,setItems]=useState<Record<string,unknown>[]>([]);useEffect(()=>{void apiGet<Record<string,unknown>[]>("/prompt-versions").then(setItems)},[]);return <main><WorkspaceHeader title="Prompt 版本" meta="普通顾问只读"/><pre className="panel mx-auto mt-6 max-w-5xl overflow-auto p-4">{JSON.stringify(items,null,2)}</pre></main>}
