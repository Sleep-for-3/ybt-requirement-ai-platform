# 一表通字段级口径智能辅助平台 MVP 使用与验收说明

## 当前启动状态

本机没有安装 Docker CLI，因此本次验收使用本机 Python/Node 启动同一套应用：

- 后端：`http://127.0.0.1:8000`
- 前端：`http://127.0.0.1:3001`
- 后端健康检查：`http://127.0.0.1:8000/api/health`
- API 文档：`http://127.0.0.1:8000/docs`

Docker-first 配置已经提供在 `docker-compose.yml`。安装 Docker 后可运行：

```powershell
cd C:\Users\李儒伟\Documents\智能分析智能体平台\ybt-requirement-ai-platform
docker compose up --build
```

## 本机启动方式

后端：

```powershell
cd C:\Users\李儒伟\Documents\智能分析智能体平台\ybt-requirement-ai-platform\backend
python -m pip install -r requirements.txt
python -m alembic upgrade head
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

前端：

```powershell
cd C:\Users\李儒伟\Documents\智能分析智能体平台\ybt-requirement-ai-platform\frontend
npm install
npx next dev -H 127.0.0.1 -p 3001
```

## 验收命令

后端单元测试：

```powershell
cd C:\Users\李儒伟\Documents\智能分析智能体平台\ybt-requirement-ai-platform\backend
python -m pytest -q
```

前端构建：

```powershell
cd C:\Users\李儒伟\Documents\智能分析智能体平台\ybt-requirement-ai-platform\frontend
npm run build
```

端到端 smoke test：

```powershell
cd C:\Users\李儒伟\Documents\智能分析智能体平台\ybt-requirement-ai-platform
python scripts\smoke_test.py
```

本次 smoke test 已验证：

- 创建项目；
- 创建目标表；
- 创建目标字段；
- 上传 EAST 口径文档；
- 上传 SQL 文件；
- sqlglot 解析来源表、字段、where、join；
- RAG 检索文档 chunk；
- 生成字段级口径草稿；
- 保存证据来源；
- 人工审核通过。

## 技术组件

前端：

- Next.js 14
- React 18
- TypeScript
- Tailwind CSS
- lucide-react 图标

后端：

- Python 3.12
- FastAPI
- SQLAlchemy 2
- Pydantic / pydantic-settings
- Alembic
- Uvicorn
- PostgreSQL Docker 配置
- SQLite 本机验收配置
- sqlglot
- httpx
- pytest / pytest-asyncio

AI 与检索：

- `LLMService` 抽象接口
- OpenAI-compatible LLM Gateway
- Mock LLM Gateway
- `VectorStore` 抽象接口
- MockVectorStore
- MilvusVectorStore 预留
- PostgreSQL LIKE 关键词检索预留路径

集成预留：

- CozeConnector
- SafeSqlExecutor
- DbProfileTask
- Milvus / Knowhere 说明

## 已实现功能

- 项目管理：创建、列表、选择项目。
- 一表通目标表管理：创建和展示目标表。
- 目标字段管理：创建和展示字段，字段详情进入分析区域。
- 文档知识库：上传 `txt` / `md` / `sql`，切分 chunk，保存元数据，生成 mock embedding，写入 VectorStore。
- SQL 解析：上传 `.sql`，识别来源表、select 字段、join 条件、where 条件，失败时保存错误信息。
- RAG 检索：`POST /api/retrieval/search`，融合 MockVectorStore 与关键词检索。
- LLM Gateway：统一抽象，业务代码不直接调用 OpenAI SDK。
- 字段口径生成：读取字段、检索证据、检索 SQL 解析结果、调用 LLM、保存任务、草稿和证据链。
- 证据链：支持文档 chunk 和 SQL 解析结果。
- 人工审核：支持通过、修改、驳回状态。
- Coze Studio：保留 mock connector。
- 数据库探查：保留 SafeSqlExecutor 和 profile task，MVP 不连接银行数据库。

## 本次验收结果

后端测试结果：

```text
9 passed in 1.36s
```

端到端 smoke test 输出摘要：

```json
{
  "document_count": 1,
  "sql_parse_success": true,
  "source_tables": ["ecif_customer", "loan_contract"],
  "selected_fields": ["c.customer_id", "c.cert_type", "l.loan_no"],
  "retrieval_count": 1,
  "evidence_count": 2,
  "review_status": "approved",
  "confidence_level": "medium"
}
```

前端页面验收：

- 页面标题显示“一表通字段口径智能辅助平台”；
- 页面展示“验收测试项目”；
- 页面展示字段 `CERT_TYPE / 客户证件类型`；
- 页面展示 SQL 文件 `ecif_customer.sql`；
- 页面展示生成口径、置信度、审核状态、风险提示、人工确认问题和证据来源。

## 现有限制

- 当前机器没有 Docker CLI，所以未能实际执行 `docker compose up --build`。
- MVP 默认使用 mock LLM；接入云端或本地大模型只需修改环境变量。
- MockVectorStore 是内存实现，重启后向量索引会重建或丢失；正式版本应接 Milvus。
- docx / pdf 解析目前只预留接口。
- 只读数据库探查只做规则校验，不连接真实银行数据库。
