# 服务器配置说明（生产机 103.236.97.213）

本文说明生产机上“配置在哪里、每个变量管什么、改了以后怎么生效”。部署与回滚步骤见《服务器上线操作手册》。

## 1. 主机与访问

| 项目 | 值 |
| --- | --- |
| SSH | `ssh -p 42438 root@103.236.97.213` |
| 系统 | Ubuntu 22.04 LTS，Docker 29.1.3，Docker Compose v2.40 |
| 站点 | `http://57008897.xyz`（Nginx 反向代理，80 端口） |
| 规格 | 16 vCPU / 16 GB 内存 / 根分区 29 GB |
| 业务代码 | `/opt/ybt/source`（Docker Compose 项目名固定为 `source`） |
| 持久化数据 | `/data/ybt/*`（Milvus、MinIO、对象存储、Embedding 缓存）与 Docker named volume |

> Compose 项目名决定 named volume 的名字（`source_postgres_data`、`source_backend_storage` 等）。
> 部署目录名不能随意改，也不要执行 `docker compose down -v`。

## 2. 配置文件位置

| 文件 | 作用 |
| --- | --- |
| `/opt/ybt/source/.env` | **唯一配置来源**（权限 600，不进 Git）：编排变量 + 运行变量 + 密钥 |
| `/opt/ybt/source/docker-compose.yml` | 通用生产拓扑：迁移门禁、健康检查、依赖端口不对外 |
| `/opt/ybt/source/docker-compose.server.yml` | 本机差异：镜像标签、`/data/ybt` 路径、本地 Embedding |
| `/etc/nginx/sites-enabled/ybt.conf` | 反向代理；代理超时 600s |
| `/opt/ybt/source/backend/.env` | 历史遗留文件，新拓扑不再读取（内容已合并进根 `.env`） |

`.env` 的键合并规则：`backend/.env` 提供默认值，根 `.env` 优先，`--set` 最高。合并用
`scripts/deploy/merge-env-files.py`，它只打印键名、不打印值，避免密钥进入日志。

## 3. 环境变量分组

### 3.1 编排控制

| 变量 | 当前值 | 说明 |
| --- | --- | --- |
| `COMPOSE_FILE` | `docker-compose.yml:docker-compose.server.yml` | 决定 `docker compose` 加载哪些文件；改动后要重新 `up -d` |
| `COMPOSE_PROFILES` | `milvus,object-storage` | 启用 Milvus 依赖与业务对象存储容器 |
| `YBT_RELEASE_TAG` | `0020b42` | 不可变镜像标签；回滚时改回上一版（`ede8472`）即可 |
| `BACKEND_BIND_ADDRESS` / `BACKEND_HOST_PORT` | `127.0.0.1` / `18000` | 后端只绑定回环；Nginx 转发到该端口 |
| `FRONTEND_BIND_ADDRESS` / `FRONTEND_HOST_PORT` | `127.0.0.1` / `13000` | 前端只绑定回环 |
| `NEXT_PUBLIC_API_BASE_URL` | `http://57008897.xyz/api` | 前端构建期注入的公网 API 地址，改动必须重新构建前端镜像 |
| `PIP_INDEX_URL` | `https://pypi.tuna.tsinghua.edu.cn/simple` | 后端镜像构建源；该机直连 pypi.org 不稳定 |
| `NPM_REGISTRY` | `https://registry.npmmirror.com` | 前端镜像构建源 |

### 3.2 运行与安全

| 变量 | 当前值 | 说明 |
| --- | --- | --- |
| `ENVIRONMENT` | `production` | 生产模式；会拒绝 SQLite/inline 队列等开发默认值 |
| `AUTH_MODE` | `required` | 强制真实登录鉴权 |
| `DEBUG` | `false` | |
| `ACCESS_TOKEN_MINUTES` | `15` | 访问令牌有效期；前端在这些分钟内会自动续期 |
| `REFRESH_TOKEN_DAYS` | `30` | 刷新令牌有效期 |
| `TRUST_PROXY_HEADERS` | `true` | 信任 Nginx 传来的 `X-Forwarded-*` |
| `HEALTH_DETAILS_PUBLIC` | `false` | `/health/details` 仅管理员可见 |
| `CORS_ORIGINS` | `http://57008897.xyz,https://57008897.xyz,http://103.236.97.213` | 允许的前端来源 |
| `APP_SECRET_KEY` / `JWT_SECRET_KEY` | 服务器本地生成 | 密钥；**不入库、不写文档** |
| `POSTGRES_USER` / `POSTGRES_PASSWORD` / `POSTGRES_DB` | `ybt` / 本地生成 / `ybt` | 数据库凭据 |
| `PRODUCTION_DATABASE_URL` | `postgresql+psycopg://ybt:…@postgres:5432/ybt` | 容器内连接串（主机名是 compose 服务名） |
| `DATABASE_URL` | 同上 | compose 显式覆盖，避免误用 `backend/.env` 里的开发值 |

### 3.3 队列与存储

| 变量 | 当前值 | 说明 |
| --- | --- | --- |
| `TASK_QUEUE_PROVIDER` | `celery` | 生产必须用真实队列 |
| `TASK_QUEUE_INLINE_MAX_JOBS` | `0` | 禁止请求内同步跑任务 |
| `REDIS_URL` / `CELERY_BROKER_URL` | `redis://redis:6379/0` | |
| `CELERY_RESULT_BACKEND` | `redis://redis:6379/1` | |
| `STORAGE_PROVIDER` | `local` | 目前落在 `backend_storage` 卷；切 S3 属于下一版计划 |
| `STORAGE_DIR` | `/app/storage` | |

### 3.4 向量检索与 Embedding

| 变量 | 当前值 | 说明 |
| --- | --- | --- |
| `VECTOR_STORE_PROVIDER` | `milvus` | |
| `MILVUS_URI` | `http://milvus:19530` | 不对外发布端口 |
| `MILVUS_COLLECTION_PREFIX` | `ybt_semantic` | 集合名前缀 |
| `MILVUS_INDEX_TYPE` / `MILVUS_METRIC_TYPE` | `AUTOINDEX` / `COSINE` | |
| `EMBEDDING_PROVIDER` | `local_vllm` | 使用本机离线 Embedding 服务 |
| `EMBEDDING_BASE_URL` | `http://embedding:11434/v1` | `ybt-embedding` 容器 |
| `EMBEDDING_MODEL` / `EMBEDDING_DIMENSION` | `BAAI/bge-small-zh-v1.5` / `512` | 模型缓存挂载在 `/data/ybt/fastembed-cache`，容器以 `HF_HUB_OFFLINE=1` 运行 |

### 3.5 大模型

| 变量 | 当前值 | 说明 |
| --- | --- | --- |
| `LLM_PROVIDER` | `openai_compatible` | 走 OpenAI 兼容协议（**只是兜底默认值，见下方“档案优先”**） |
| `LLM_BASE_URL` | `https://www.hejuapi.com/v1` | **该地址在服务器上不可达**（域名解析到 Cloudflare，服务器无对应出网通道），见第 5 节 |
| `LLM_MODEL` | `gpt-5.6-sol` | |
| `LLM_API_KEY_ENV_NAME` | `OPENAI_API_KEY` | 只是变量名，不是密钥 |
| `OPENAI_API_KEY` | 服务器本地保存 | 真实密钥；不入库、不写文档 |

#### 3.5.1 真正生效的是数据库里的“模型档案”（重要）

平台的 LLM 运行时**优先读取数据库 `model_profiles` 里 `enabled=true` 的那条档案**，
只有档案不存在时才回退到上面的 `.env` 变量（`GET /api/ai-runtime/status` 返回的就是档案值）。
生产库当前有两档案：

| id | profile_name | provider_type | base_url | model | api_key_env_name | enabled |
| --- | --- | --- | --- | --- | --- | --- |
| 2 | Product 5.1 Demo LLM | openai_compatible | https://www.hejuapi.com/v1 | gpt-5.6-sol | **HEJU_API_KEY** | ✅ |
| 1 | deepseek | openai_compatible | https://api.deepseek.com | deepseek-chat | DEEPSEEK_API_KEY | ❌ |

两个已知问题：

1. **档案 2 引用的 `HEJU_API_KEY` 在服务器 `.env` 里并不存在**（`.env` 里只有 `OPENAI_API_KEY`），
   所以档案的连接自检一直是 failed；即使网络可达，会先卡在“密钥环境变量未配置”。
2. **档案 1（deepseek）已建好但未启用，且 `DEEPSEEK_API_KEY` 未配置**。

接入真实模型有两种做法，二选一（都要重启 `backend worker beat`）：

```bash
# 做法 A：沿用 hejuapi 档案，把密钥变量名对齐（.env 里已有 OPENAI_API_KEY）
P='docker compose exec -T postgres psql -U ybt -d ybt -Atc'
$P "update model_profiles set api_key_env_name='OPENAI_API_KEY', updated_at=now() where id=2;"

# 做法 B：切到 DeepSeek（服务器可直连 api.deepseek.com）
#   1) .env 增加 DEEPSEEK_API_KEY=<真实密钥>（不入库、不写文档）
#   2) 启用档案 1、停用档案 2
$P "update model_profiles set enabled=(id=1), updated_at=now() where id in (1,2);"
```

改完用 `curl -s http://127.0.0.1:18000/health/ready` 与前端“智能运行状态”页确认：
provider 应为 `openai_compatible`、`configuration_status=configured`，并做一次
`POST /api/ai-runtime/test-chat` 的真实连通性自检。

## 4. 常见配置变更怎么生效

| 变更内容 | 生效方式 |
| --- | --- |
| 普通变量（超时、开关、CORS） | `docker compose up -d backend worker beat` |
| `NEXT_PUBLIC_API_BASE_URL` | 必须重建前端镜像：`docker build -t ybt-frontend:<tag> --build-arg NEXT_PUBLIC_API_BASE_URL=… ./frontend`，再 `up -d frontend` |
| 镜像标签 `YBT_RELEASE_TAG` | `docker compose up -d`（回滚也用它） |
| Nginx 超时/域名 | 改 `/etc/nginx/sites-enabled/ybt.conf` 后 `nginx -t && systemctl reload nginx` |
| 密钥轮换 | 改 `.env` 后 `up -d backend worker beat`；数据库密码还要同步 `PRODUCTION_DATABASE_URL` 与 `POSTGRES_PASSWORD` |
| 模型档案（provider/model/base_url/密钥变量名） | 只改数据库：`update model_profiles ...`，然后 `up -d backend worker beat`（`backend/worker/beat` 不重启也可能生效，但重启最稳） |

## 5. 已知配置约束

1. **服务器出网受限**：`www.hejuapi.com` 解析到 Cloudflare，服务器访问超时（`Network is unreachable`）。
   已确认可达的国内模型入口：`api.deepseek.com`（返回 401，说明链路通）。接入国产模型时把
   密钥写入 `.env` 后按 §3.5.1 的做法 B 切档案（只改 `.env` 不改档案是**不会生效**的）。
2. **代理超时**：`proxy_read_timeout` / `proxy_send_timeout` 必须 ≥ 600s，否则真实模型的长耗时生成会被 Nginx 截断成 504。
3. **端口**：PostgreSQL/Redis/Milvus 不发布到宿主机；只有 18000/13000 绑定 `127.0.0.1`。
4. **数据目录**：`/data/ybt/{milvus,milvus-minio,object-storage,fastembed-cache}` 是业务数据，不得删除；
   数据库、Redis、后端存储使用 named volume，同样不随发布重建。
5. **验收用的共享夹具目录**：容器化端到端验收要用 `SMOKE_FIXTURE_DIR` 把 SQLite 夹具放到
   backend/worker 也能读到的路径（复用 `backend_storage` 卷的 `/app/storage/_acceptance`），
   否则元数据同步只能 `partially_completed`。验收结束记得删除该目录（见《服务器上线操作手册》§3.3.1）。
6. **磁盘**：根分区 29G（当前 ~79%，可用 ~6G），业务数据与备份在独立盘 `/data`（9.8G，占用 ~5%）。
   `docker image prune` 可回收悬空镜像；保留最近两版 `ybt-*` 以便回滚。
