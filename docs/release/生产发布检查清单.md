# 生产发布检查清单

状态图例：`[x]` 已有可复现证据；`[~]` 部分完成或仅在本地环境验证；`[ ]` 未开始。
证据汇总见 `docs/upgrade/R5-生产验证与上线候选报告.md`；下版本待办见 `docs/upgrade/R6-发布演练缺陷与下版本迭代计划.md`。

## 阻塞项（未全部完成前不得发布）

- [x] P0 迁移历史可信度：初始迁移不再调用 `Base.metadata.create_all`，历史结构由 git 历史重建并冻结（`backend/app/schema_freeze`、`scripts/migration/`）。
  - 证据：`backend/tests/test_migration_schema_freeze.py`（3 passed）、`docs/upgrade/R1-迁移矩阵报告.md`
- [x] 真实 PostgreSQL 迁移矩阵：空库→head、head→202608290021→head、幂等升级、完全降级再重建，6 步退出码 0，4 个 head 状态结构指纹一致
  - 证据：`docs/upgrade/R1-迁移矩阵报告.md`，脚本 `scripts/migration/verify_migration_matrix.py`
- [~] 失败恢复验证：已覆盖降级/重建路径，尚未演练"迁移中途失败后恢复"（R6 P0-6）
- [x] 真实 LLM 连通性（脱敏最小探针）：HTTP 200、结构化 JSON 校验通过；无需切换 DeepSeek
  - 证据：`docs/upgrade/R5-生产验证与上线候选报告.md` 第 1 节
- [x] 真实 LLM 经**应用接口**的验证
  - 证据（三套环境各 `SMOKE_EXIT=0`）：① Windows 原生 + SQLite；② Windows 原生 + PostgreSQL 18.4（501 s、23 次调用 0 失败，1 次由兜底模型 `gpt-6-astra` 接管）；③ **生产同主版本 PostgreSQL 16.4 + 真实模型 + 5 分钟令牌**（2026-09-13，1360 s）：`ai_runtime_provider=openai_compatible`、`model_call_count=23`（22 成功 / 1 交互式超时并降级）、累计模型耗时 1304.3 s、UAT 报告 11 798 B / 10 Sheet / 6 证据 / 60 内置用例、`cross_bank=404`、`viewer_execute=403`
  - 修复支撑：失败诊断字段（`http_status` / `error_detail`）、json 模式提示词、兜底模型链（`LLM_FALLBACK_*`）、可配置超时（`LLM_TIMEOUT_SECONDS`）、交互式预算与降级（`LLM_INTERACTIVE_TIMEOUT_SECONDS`、`answer_status=degraded`）、会话续期（D12）——见 R6 D7–D12
  - 遗留：须按 P0-6 在**脱敏生产副本**上复核迁移与结构指纹；**并发/队列积压/P95 与告警阈值仍未验证**（规模基线见 R6 第 2.1 节，不能替代并发结论）；生成类接口仍为同步等待（P1-6）
- [x] Redis、Celery worker、Celery beat 在真实环境中的健康检查与调度
  - 证据：worker 真实消费 5 个 `knowledge_ingestion` 任务全部 succeeded；beat 产生 `lineage-repository-monitor` 调度日志；`/api/health/ready` 在 worker 启动后 `task_queue=healthy`
- [~] 真实 `APP_SECRET_KEY` / `JWT_SECRET_KEY` / CORS / Cookie 配置
  - 已完成：Compose 演练用随机密钥的本地 `.env` 跑通全栈；未完成：生产取值与轮换流程（R6 P0-5）
- [x] `AUTH_MODE=required` 下的登录、项目/机构权限隔离与审计日志端到端
  - 证据：真实 PG Smoke 覆盖登录、跨机构访问 404、只读角色执行 403，并断言 11 项审计动作（含 `download_uat_report`、`signoff_uat_run`）
  - 待加固：独立权限矩阵用例（R6 P0-3）
- [x] 对象存储与数据库备份、恢复演练
  - 证据：`pg_dump`/`pg_restore` 后目标库 118 张表、alembic `202609120026`、R1 结构指纹 `8562802ca513…` 与源库完全一致，ORM 契约 0 问题；`backend_storage` 卷打包恢复后 8 个文件聚合哈希一致
  - 注意：本轮新增迁移 `202609120027` 后，head 状态的结构指纹更新为 `c58431fba9aa6accccccb95bf91285480f2fdfd825cf8ebaf9269db9a22345ce`（118 表，见 R1 报告）
  - 未覆盖：PITR、跨主机恢复、备份加密与保留策略
- [ ] 真实规模脱敏仓库批量同步（单批次原子发布、失败保留上一 published）（R6 P0-1）
- [~] 在 PostgreSQL 环境复跑 Smoke、UAT、后端与前端全量回归
  - 已完成：真实 PG Smoke `SMOKE_EXIT=0`（含 UAT 全流程，mock provider）；真实模型全流程 Smoke `SMOKE_EXIT=0`（原生栈，23 次真实调用 0 失败）；前端 `npm test` 120 passed；前端生产镜像 `next build` 通过
  - 已完成：后端全量 pytest **589 passed, 0 skipped, 5 warnings in 1057.44s**（退出码 0，Windows 原生解释器，含本轮 D7–D10 新用例）
  - 说明：后端测试套件本身仍跑内存 SQLite（`backend/tests/conftest.py`），PostgreSQL 语义覆盖依赖 Smoke + 真实环境探针，能力补齐见 R6 P1-4
- [ ] 并发、容量、队列积压与告警阈值（R6 P0-2）
- [~] 回滚方案、值班联系人与维护窗口（R6 P0-5）
  - 已完成：`docs/deployment/发布与恢复Runbook.md` 补齐维护窗口、值班与升级路径、回滚判定与 RTO/RPO，并在有数据的真实 PostgreSQL 副本上完成一次回滚演练（降级 → 行数 1/23/160 不变 → 升回 head，指纹恢复一致）
  - 未完成：Runbook 中三行「当值人」仍为 `待填写`；未填写前该门禁保持未勾选

## 本轮新增门禁

- [x] 生产 Compose 打包演练：五个镜像全部构建成功，一次性 `migrate` 服务 `exit=0`，六个服务 healthcheck 全绿，`/api/health/ready` 在编排网络内 200
- [x] 编排拓扑有界端到端探针：真实网络数据源连通性检查 `success`，元数据同步由生产镜像 worker 消费至 `completed`，目录检索命中
- [x] 前端生产镜像可构建（`next build` 类型检查通过）：`frontend/lib/lineage-graph.d.mts` + `frontend/tests/type-declaration-coverage.test.mjs`
- [x] 后端镜像包含全部源码包：`backend/.dockerignore` 运行时目录改根锚定 + `backend/tests/test_deployment_contract.py` 契约测试
- [x] 交互式模型调用的独立预算与优雅降级：`LLM_INTERACTIVE_TIMEOUT_SECONDS`（默认 90、单次重试）+ 问答返回 `answer_status=degraded` 而非 500 + 连接测试 503 附带脱敏提供方诊断（R6 D11）
- [x] 回滚演练：在有数据的真实 PostgreSQL 副本上完成 `downgrade 202609120026` → 行数校验 → `upgrade head`，结构指纹恢复一致（见 `docs/deployment/发布与恢复Runbook.md`）
- [x] 长耗时业务会话续期：受保护请求 401 时前端用刷新令牌静默续期并重放一次（并发 401 只续期一次），续期失败才清会话跳登录；`/auth/login`、`/auth/refresh` 的 401 原样返回（R6 D12）。证据：`frontend/tests/http-response.test.mjs` 3 例（`npm test` 123 passed）、`npx tsc --noEmit` exit 0、`next build` 通过
- [x] 反向代理与会话自检：Runbook「发布前预检」第 6 条要求确认 `POST /api/auth/refresh` 可通行，并按 `ACCESS_TOKEN_MINUTES=1` 做一次长耗时链路自检（见《环境变量说明》「会话与令牌续期」）
- [x] 长耗时请求预算与代理超时：前端按路径区分请求预算（默认 60 s / 生成、画像、同步、评测、渲染、下载、问答等 600 s），并要求反向代理 `proxy_read_timeout` ≥ 600 s（R6 D14；`frontend/tests/request-timeout.test.mjs` 4 例）
- [x] 规模基线可跑在真实 PostgreSQL 上：`scripts/performance_baseline.py` 支持 `--database-url`（拒绝非空目标库）并补齐 fixture 外键链；PG16.4 上 SMALL 与 FULL 规模 `exit=0`（R6 D13、第 2.1 节）

## 已完成的工程基线

- [x] migrate gate 编排：一次性 `migrate` 服务 + `service_completed_successfully`
- [x] backend/worker/beat/frontend healthcheck
- [x] PostgreSQL/Redis/Milvus 不发布宿主机端口；API/前端仅绑定回环，可用 `BACKEND_BIND_ADDRESS` / `FRONTEND_BIND_ADDRESS` 覆盖
- [x] 跨项目字段目录隔离（`project_id` 条件 + 缓存 key）
- [x] 跨数据库 Excel 导出健壮性（`app/core/excel.py::excel_value()` 覆盖交付工作簿、血缘导出、UAT 报告）
- [x] 目录检索在 PostgreSQL 上的大小写正确性（`ilike` + 转义）
- [x] 交付渲染崩溃落 `render_failed` 而不是卡在 `generating`

## 明确的未验证边界

- 容量与并发结论：**未验证**，不得声称生产容量已达标。仅有单进程规模基线（PostgreSQL 16.4 FULL：2000 业务映射 / 1000 技术映射 / 5000 知识单元 / 5000 血缘边，就绪度 2.02 s、Excel 渲染 4.32 s），它不能替代并发、队列积压与 P95 结论。
- 生产数据库副本上的迁移回放：**未验证**（本机真实 PostgreSQL 的 6 步矩阵与有数据副本双向回滚演练已通过，但尚未在脱敏生产副本上执行）。
- 真实模型经应用接口的端到端业务链路：**已验证**；"真实 PostgreSQL + 真实模型"已通过两套环境——PostgreSQL 18.4（`SMOKE_EXIT=0`、501 s、23 次调用 0 失败）与**生产同主版本的 PostgreSQL 16.4**（`ACCESS_TOKEN_MINUTES=5` 强制令牌续期，`SMOKE_EXIT=0`）。仍须在脱敏生产副本上复核迁移与结构指纹。
- 真实规模脱敏仓库批量同步：**未验证**（逻辑已实现并有单测）。
- 生产密钥取值与轮换：**未验证**（当前为本地演练密钥）。
