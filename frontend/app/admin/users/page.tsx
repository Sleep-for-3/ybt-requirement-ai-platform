"use client";

import { FormEvent, useEffect, useState } from "react";
import { WorkspaceHeader } from "@/components/WorkspaceHeader";
import { apiGet, apiPost } from "@/lib/api";
type Institution={id:number;institution_name:string};
export default function Page(){const[institutions,setInstitutions]=useState<Institution[]>([]);const[msg,setMsg]=useState("");useEffect(()=>{apiGet<Institution[]>("/admin/institutions").then(setInstitutions).catch(()=>setMsg("无权读取机构"));},[]);async function create(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);try{await apiPost("/admin/users",{username:form.get("username"),display_name:form.get("display_name"),email:form.get("email"),password:form.get("password"),institution_id:Number(form.get("institution_id")),institution_role:form.get("role")});event.currentTarget.reset();setMsg("用户已创建，密码仅保存为 Argon2 哈希");}catch(error){setMsg(error instanceof Error?error.message:"创建失败");}}return <main><WorkspaceHeader title="用户管理" meta="本地账号与机构成员角色"/><form className="panel mx-auto mt-6 max-w-2xl p-6" onSubmit={create}><div className="grid gap-3 sm:grid-cols-2"><input className="control" name="username" placeholder="用户名" required/><input className="control" name="display_name" placeholder="显示名称" required/><input className="control" type="email" name="email" placeholder="邮箱" required/><input className="control" type="password" name="password" placeholder="初始密码（至少 12 位）" minLength={12} required/><select className="control" name="institution_id" required><option value="">选择机构</option>{institutions.map(item=><option key={item.id} value={item.id}>{item.institution_name}</option>)}</select><select className="control" name="role"><option value="member">普通成员</option><option value="institution_admin">机构管理员</option><option value="security_admin">安全管理员</option><option value="auditor">审计人员</option></select></div><button className="button-primary mt-4">创建用户</button>{msg?<p className="mt-3 text-sm text-slate-600">{msg}</p>:null}</form></main>}
