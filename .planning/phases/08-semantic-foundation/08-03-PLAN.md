---
phase: 08-semantic-foundation
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - backend/app/services/semantic/resolver.py
  - backend/app/services/semantic/binding_service.py
  - backend/app/api/semantic.py
  - backend/tests/test_semantic_layer.py
  - backend/tests/test_semantic_migration.py
autonomous: true
requirements: [SEM-04, SEM-05, SEM-07]
user_setup: []
must_haves:
  truths:
    - "D-04: AI suggestions cannot become confirmed without explicit human transition and audit"
    - "D-11: Resolver uses deterministic evidence before any future LLM inference"
    - "Status transitions reject invalid edges and preserve confirmed_by/confirmed_at"
    - "Post-change full backend regression passes"
  artifacts:
    - path: backend/app/services/semantic/resolver.py
      provides: Deterministic scored semantic candidates with reasons and evidence
    - path: backend/tests/test_semantic_migration.py
      provides: Upgrade/downgrade compatibility proof
  key_links:
    - from: backend/app/api/semantic.py
      to: backend/app/services/governance/audit.py
      via: audit each semantic mutation and transition
    - from: backend/app/services/semantic/resolver.py
      to: backend/app/services/semantic/binding_service.py
      via: confirmed historical binding priority
---

<objective>
Complete Phase 8 governance, deterministic resolution and regression proof.

Purpose: Ensure semantic data is trustworthy and useful before ContextBuilder consumes it.
Output: Status transition policy, audit provenance, resolver candidates, migration lifecycle and full tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-semantic-foundation/08-CONTEXT.md
@.planning/phases/08-semantic-foundation/08-02-SUMMARY.md
@backend/app/services/governance/audit.py
@backend/app/services/retrieval/hybrid_retriever.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Enforce governance transitions and audit provenance</name>
  <files>backend/app/services/semantic/binding_service.py, backend/app/api/semantic.py, backend/tests/test_semantic_layer.py</files>
  <read_first>backend/app/services/governance/audit.py, backend/app/services/governance/workflow.py, backend/app/api/review_tasks.py</read_first>
  <action>Define explicit allowed status transitions for all three semantic resources. Reject AI-origin confirmed creation; require business.edit for suggestions/drafts and task.manage for confirmation/rejection/deprecation; fill confirmation actor/time; record before/after audit entries. Reuse generic ReviewTask targets without adding a review engine.</action>
  <verify>Run status, permission and audit tests.</verify>
  <acceptance_criteria>
    - `ai_suggested -> confirmed` only occurs through explicit privileged status endpoint.
    - Invalid transitions return 409 and leave row unchanged.
    - AuditLog contains resource type/id, project and before/after status.
  </acceptance_criteria>
  <done>Human governance is enforced and traceable.</done>
</task>
<task type="auto">
  <name>Task 2: Add deterministic resolver and final compatibility tests</name>
  <files>backend/app/services/semantic/resolver.py, backend/app/api/semantic.py, backend/tests/test_semantic_layer.py, backend/tests/test_semantic_migration.py</files>
  <read_first>backend/app/services/recommendation/source_field_recommender.py, backend/app/services/retrieval/hybrid_retriever.py, backend/tests/conftest.py</read_first>
  <action>Return candidate concept id, score, match_reason, evidence and suggested status. Implement exact normalized code/name, alias, comment containment and confirmed binding evidence in descending deterministic priority. Do not call LLM. Add real Alembic lifecycle test and regression assertions that formal semantic index remains present.</action>
  <verify>Run semantic tests, full backend pytest and import/compile checks.</verify>
  <acceptance_criteria>
    - Exact code outranks name/alias/comment.
    - Results are stable for identical inputs.
    - No LLM/network call is made.
  </acceptance_criteria>
  <done>Phase 8 acceptance examples pass and the complete backend suite has no regression.</done>
</task>
</tasks>

<verification>
- [ ] `python -m pytest -q tests/test_semantic_layer.py tests/test_semantic_migration.py`
- [ ] `python -m pytest -q`
- [ ] `python -m alembic heads` returns one head
- [ ] `python -m compileall -q app`
</verification>

<success_criteria>
- All seven Phase 8 requirements have executable proof.
- Governance and provenance are explicit.
- Resolver is deterministic and non-LLM.
- Existing backend behavior remains green.
</success_criteria>

<output>After completion, create `.planning/phases/08-semantic-foundation/08-03-SUMMARY.md`.</output>
