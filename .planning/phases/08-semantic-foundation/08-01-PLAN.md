---
phase: 08-semantic-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/semantic.py
  - backend/app/models/__init__.py
  - backend/app/schemas/semantic.py
  - backend/app/api/semantic.py
  - backend/app/main.py
  - backend/alembic/versions/202608200015_regulatory_semantic_layer.py
  - backend/tests/test_semantic_layer.py
autonomous: true
requirements: [SEM-01, SEM-05, SEM-06]
user_setup: []
must_haves:
  truths:
    - "D-01: Semantic concepts are project-scoped and institution_id is derived from Project"
    - "D-02: Concept identity is unique by project_id, concept_type and normalized concept_code; names are not globally unique"
    - "D-03: Only the six initial concept types are accepted"
    - "D-09: Existing API routes and formal semantic index remain unchanged"
    - "D-10: The additive migration upgrades and downgrades on SQLite"
    - "D-12: ContextBuilder, generator, frontend, dashboard, quality and impact changes remain outside Phase 8"
  artifacts:
    - path: backend/app/models/semantic.py
      provides: SemanticConcept persistence with constraints and indexes
    - path: backend/app/api/semantic.py
      provides: Project-aware concept CRUD tracer slice
    - path: backend/alembic/versions/202608200015_regulatory_semantic_layer.py
      provides: Portable semantic schema migration
  key_links:
    - from: backend/app/api/semantic.py
      to: backend/app/services/auth/permission_service.py
      via: project permission enforcement
    - from: backend/app/main.py
      to: backend/app/api/semantic.py
      via: additive router registration
---

<objective>
Deliver a production-quality tracer slice for SemanticConcept from migration and model through authenticated API and tests.

Purpose: Prove the new semantic layer fits existing database, authorization and API conventions before expanding relationships.
Output: Concept table/model/schema, additive CRUD router, migration and initial isolation/duplicate tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-semantic-foundation/08-CONTEXT.md
@.planning/phases/08-semantic-foundation/08-RESEARCH.md
@backend/app/models/entities.py
@backend/app/services/auth/permission_service.py
@backend/alembic/versions/202607300014_formal_semantic_index.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add concept persistence and portable migration</name>
  <files>backend/app/models/semantic.py, backend/app/models/__init__.py, backend/alembic/versions/202608200015_regulatory_semantic_layer.py</files>
  <read_first>backend/app/core/database.py, backend/app/models/entities.py, backend/alembic/versions/202607300014_formal_semantic_index.py</read_first>
  <action>Create SemanticConcept with project/institution FKs, controlled string type/status, code/name/definition/description/domain/owner, aliases JSON, confidence, version, provenance/confirmation fields and timestamps. Add portable unique/index constraints. Migration initially creates all three phase tables so later plans do not mutate migration history, but only concept behavior is exposed in this tracer task. Do not alter embedding_index_versions.</action>
  <verify>Run `python -m alembic heads` and the migration upgrade/downgrade test.</verify>
  <acceptance_criteria>
    - New revision has down_revision `202607300014`.
    - Existing tables are never dropped or renamed in upgrade.
    - Semantic concept unique identity includes project_id, concept_type and concept_code.
  </acceptance_criteria>
  <done>Models import through app.models and SQLite can build/rollback the new schema.</done>
</task>
<task type="auto">
  <name>Task 2: Add concept CRUD tracer API</name>
  <files>backend/app/schemas/semantic.py, backend/app/api/semantic.py, backend/app/main.py</files>
  <read_first>backend/app/api/knowledge_items.py, backend/app/api/governance.py, backend/app/services/auth/permission_service.py, backend/app/main.py</read_first>
  <action>Add list/detail/create/update endpoints under `/projects/{project_id}/semantic-concepts` plus detail by id within that scope. Derive institution_id from Project; validate enums; normalize code; use project.view for reads and business.edit for writes; return 409 for duplicates. Register the router additively.</action>
  <verify>Run focused concept tests with TestClient.</verify>
  <acceptance_criteria>
    - Cross-project detail returns 404 and list never leaks rows.
    - Client cannot choose institution_id.
    - Existing router paths are untouched.
  </acceptance_criteria>
  <done>Authorized CRUD works end-to-end through FastAPI and is project isolated.</done>
</task>
</tasks>

<verification>
- [ ] `python -m pytest -q tests/test_semantic_layer.py -k concept`
- [ ] `python -m alembic heads` reports only `202608200015`
- [ ] Migration upgrade/downgrade test passes
</verification>

<success_criteria>
- Concept tracer slice works end-to-end.
- Existing formal semantic index remains untouched.
- No current API is removed or renamed.
</success_criteria>

<output>After completion, create `.planning/phases/08-semantic-foundation/08-01-SUMMARY.md`.</output>
