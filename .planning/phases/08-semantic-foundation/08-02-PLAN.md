---
phase: 08-semantic-foundation
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - backend/app/models/semantic.py
  - backend/app/schemas/semantic.py
  - backend/app/services/semantic/__init__.py
  - backend/app/services/semantic/binding_service.py
  - backend/app/services/semantic/graph_service.py
  - backend/app/api/semantic.py
  - backend/tests/test_semantic_layer.py
autonomous: true
requirements: [SEM-02, SEM-03, SEM-06]
user_setup: []
must_haves:
  truths:
    - "D-05: Existing business, knowledge and lineage models are referenced, not duplicated"
    - "D-06: Every binding target is allow-listed, exists and belongs to the same project"
    - "D-07: Self-relations and duplicate directed triples are rejected"
    - "D-08: Graph traversal is deterministic, cycle-safe and bounded to depth five"
  artifacts:
    - path: backend/app/services/semantic/binding_service.py
      provides: Central entity registry and isolation validation
    - path: backend/app/services/semantic/graph_service.py
      provides: Bounded neighbor/upstream/downstream/entity/path queries
  key_links:
    - from: backend/app/services/semantic/binding_service.py
      to: backend/app/models/entities.py
      via: allow-listed target model registry
    - from: backend/app/api/semantic.py
      to: backend/app/services/semantic/graph_service.py
      via: project-scoped graph endpoints
---

<objective>
Expand the tracer into governed concept-to-entity bindings and concept graph queries.

Purpose: Connect business meaning to existing assets without copying their data or adding graph infrastructure.
Output: Binding/relation services, CRUD/query endpoints and isolation/graph tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-semantic-foundation/08-CONTEXT.md
@.planning/phases/08-semantic-foundation/08-01-SUMMARY.md
@backend/app/models/entities.py
@backend/app/models/lineage.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement validated semantic bindings</name>
  <files>backend/app/services/semantic/binding_service.py, backend/app/api/semantic.py, backend/app/schemas/semantic.py</files>
  <read_first>backend/app/models/entities.py, backend/app/services/auth/resource_guard.py, backend/app/api/mapping_evidence.py</read_first>
  <action>Implement the 12 locked entity types, target existence/project checks, duplicate handling, list by concept/entity and CRUD endpoints. Derive institution from Project and never accept cross-scope source references.</action>
  <verify>Run binding tests including every entity family and cross-project rejection.</verify>
  <acceptance_criteria>
    - Unsupported entity_type returns 422/400.
    - Missing or cross-project entity returns 404/400 without insert.
    - Binding list is deterministic and project scoped.
  </acceptance_criteria>
  <done>Concepts can bind to acceptance-example entities with safe project isolation.</done>
</task>
<task type="auto">
  <name>Task 2: Implement bounded semantic graph</name>
  <files>backend/app/services/semantic/graph_service.py, backend/app/api/semantic.py, backend/app/schemas/semantic.py</files>
  <read_first>backend/app/services/lineage/impact_analyzer.py, backend/app/models/semantic.py</read_first>
  <action>Add relation CRUD with allowed types and project checks. Implement neighbors, upstream, downstream, entity semantics and shortest semantic path using batched bounded BFS, visited-set cycle prevention, max_depth 1..5 and max_nodes guard.</action>
  <verify>Run relation/graph tests with cycles, direction, missing path and project isolation.</verify>
  <acceptance_criteria>
    - Self edge and duplicate triple return 409/400.
    - Cycle terminates and no concept repeats in one path.
    - Depth above five is rejected.
  </acceptance_criteria>
  <done>Acceptance example relations and finite graph queries work without a graph database.</done>
</task>
</tasks>

<verification>
- [ ] `python -m pytest -q tests/test_semantic_layer.py -k "binding or relation or graph"`
- [ ] SQL query paths always include project_id predicates
</verification>

<success_criteria>
- Bindings cover required existing models safely.
- Graph APIs are bounded and deterministic.
- No duplicate Metadata, Knowledge or Lineage storage is introduced.
</success_criteria>

<output>After completion, create `.planning/phases/08-semantic-foundation/08-02-SUMMARY.md`.</output>

