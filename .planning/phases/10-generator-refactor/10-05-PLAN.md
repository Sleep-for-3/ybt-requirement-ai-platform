---
phase: 10-generator-refactor
plan: 05
type: execute
wave: 5
depends_on: ["10-04"]
files_modified:
  - backend/app/services/governance/double_layer_review.py
  - backend/app/services/mapping/source_to_mart_generator.py
  - backend/app/services/mapping/mart_to_ybt_generator.py
  - backend/app/services/mapping/context_adapters.py
  - backend/app/api/mapping_rules.py
  - backend/tests/test_double_layer_mapping.py
  - backend/tests/test_generator_context_adapters.py
autonomous: true
requirements: [GEN-01, GEN-02, GEN-04]
gap_closure: true
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "GEN-01 / GEN-02 / D-04 / D-17 / D-18: Source-to-Mart 与 Mart-to-YBT 仅允许可编辑 draft 行进入 Context/model 工作；approved、含 final_content 或处于 active review 的行在模型前稳定阻断。"
    - "在模型返回后的新事务中，actor validation 与 technical.edit 复核之后按 Project→task 加锁，再次执行同一 editability policy；并发批准、采纳 final 或进入 review 的行零 draft mutation、零 success audit。"
    - "GEN-04 / D-11 / D-19: SourceToMartContextAdapter 只投影 resolution_state=open 的 Context questions；resolved questions 不进入 prompt、merge、trace 或 pending-confirmation。"
    - "D-02 / D-05 / D-20 / D-22: 阻断是可诊断的 409 governance response，旧 generate-draft 成功 schema 保持不变，且治理阻断发生时 builder/model 调用数为零。"
  artifacts:
    - path: backend/app/services/governance/double_layer_review.py
      provides: "Double-layer mapping lifecycle/editability policy and typed governed block"
      exports: ["MappingGenerationNotEditable", "ensure_double_layer_mapping_editable"]
    - path: backend/app/services/mapping/source_to_mart_generator.py
      provides: "Source-to-Mart pre-model and post-lock lifecycle enforcement"
    - path: backend/app/services/mapping/mart_to_ybt_generator.py
      provides: "Mart-to-YBT pre-model and post-lock lifecycle enforcement"
    - path: backend/app/services/mapping/context_adapters.py
      provides: "Open-only Source-to-Mart Context question projection"
    - path: backend/tests/test_double_layer_mapping.py
      provides: "Approved/final/review-locked direct and race regression coverage"
    - path: backend/tests/test_generator_context_adapters.py
      provides: "Open/resolved question lifecycle regression coverage"
  key_links:
    - from: backend/app/services/mapping/source_to_mart_generator.py
      to: backend/app/services/governance/double_layer_review.py
      via: "same policy before Context/model work and after fresh Project→task lock"
      pattern: "ensure_double_layer_mapping_editable"
    - from: backend/app/services/mapping/mart_to_ybt_generator.py
      to: backend/app/services/governance/double_layer_review.py
      via: "symmetric lifecycle policy at both authority boundaries"
      pattern: "ensure_double_layer_mapping_editable"
    - from: backend/app/api/mapping_rules.py
      to: backend/app/services/governance/double_layer_review.py
      via: "typed lifecycle block maps to stable HTTP 409 diagnostics"
      pattern: "generation-governance-blocked"
    - from: backend/app/services/mapping/context_adapters.py
      to: backend/app/schemas/regulatory_context.py
      via: "Source projection filters typed ContextOpenQuestion.resolution_state before capping"
      pattern: "resolution_state.*open"
---

<objective>
关闭 double-layer lifecycle 绕过与 Source question 重开缺口，使 direct generate-draft 在模型前和 fresh lock 后都遵守同一治理边界。

Purpose: 防止 AI 在 approved/final/review-locked 行上运行或写入，并保证已解决的治理问题不会再次变成待确认事项。
Output: 可复用的 double-layer editability policy、Source/Mart 双重执行点、稳定 API 错误映射及精确回归测试。
</objective>

<flagged_assumptions>
- `GEN-01` concurrency 仍是需执行验证的假设：并发 approve/adopt/review 转换必须由 fresh Project→task lock 后的同一 policy 捕获，最多提交一个仍被允许的完整 draft 状态。
- `GEN-02` concurrency 采用相同保证；Context/model 阶段不持锁，治理状态在 authoritative write boundary 重新判定。
- `GEN-04` unclassified 假设绑定到 open/resolved 混合测试：resolved question 不得通过任何生成输出重新进入 pending state。
- SQLite 仅证明应用级顺序、重载和原子结果；本计划不声称验证 PostgreSQL 的真实阻塞与并发提交语义。
</flagged_assumptions>

<execution_context>
@C:/Users/李儒伟/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/李儒伟/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-generator-refactor/10-CONTEXT.md
@.planning/phases/10-generator-refactor/10-REVIEW.md
@.planning/phases/10-generator-refactor/10-VERIFICATION.md
@.planning/phases/10-generator-refactor/10-02-SUMMARY.md
@backend/app/services/governance/scenario_review.py
@backend/app/services/governance/workflow.py
@backend/app/services/mapping/generator_context.py
@backend/app/services/mapping/source_to_mart_generator.py
@backend/app/api/mapping_rules.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace Source-to-Mart lifecycle denial from unchanged route through the fresh write boundary</name>
  <files>backend/app/services/governance/double_layer_review.py, backend/app/services/mapping/source_to_mart_generator.py, backend/app/api/mapping_rules.py, backend/tests/test_double_layer_mapping.py</files>
  <read_first>backend/app/services/governance/scenario_review.py, backend/app/services/governance/workflow.py, backend/app/models/entities.py, backend/app/models/governance.py, backend/app/services/mapping/source_to_mart_generator.py, backend/app/services/mapping/generator_context.py, backend/app/api/mapping_rules.py, backend/tests/test_double_layer_mapping.py</read_first>
  <behavior>
    - Test 1: Per D-04/D-17/D-18，初始 mapping_status 非 draft、final_content 非空、或最新 double_layer_mapping_review WorkflowInstance 为 in_progress 时，direct Source generate-draft 返回 409 `generation-governance-blocked`，ContextBuilder、runtime/model 与 task mutation 均为零。
    - Test 2: 普通 draft 行仍通过原 route、prompt key、SourceToMartOutput 与 response schema 生成 AI draft；final_content 和 mapping_status 保持原值。
    - Test 3: 模型 barrier 期间第二 Session 将行批准、采纳 final 或启动 review；模型返回后 fresh transaction 先验证 actor/technical.edit，再锁 Project→task，再执行同一 policy，结果为治理阻断且不写 draft/success audit。
    - Test 4: 中断或治理阻断只保留已允许的 attempt trace；既有 AI draft、human final、questions、review metadata 和 concurrent row bytes 不变。
  </behavior>
  <action>先写失败测试。新增 `MappingGenerationNotEditable(reason_code)` 与 `ensure_double_layer_mapping_editable(db, mapping_type, mapping)`；唯一可生成状态是 `mapping_status == "draft"`、`final_content` 为空且该 target 不存在 `workflow_key == "double_layer_mapping_review"` 且 `status == "in_progress"` 的 review lock。该 helper 只读取当前 task 与 review lifecycle，不构造共享事实。Source service 在 snapshot/Context 前调用一次，并在 fresh transaction 完成 actor validation、PermissionService technical.edit、Project→task 加锁和 scope 校验后再次调用；第二次失败回滚 task write 并记录 bounded governed outcome。API 将 typed error 映射为稳定 409 detail，不改变成功 response model、route、optional as_of 或 adopt/review 端点。遵循 D-02、D-04、D-17、D-18、D-20 与 D-22；不得用 snapshot equality 替代 editability policy。</action>
  <verify>
    <automated>cd backend; python -m pytest -q tests/test_double_layer_mapping.py -k "source_to_mart and (approved or final or review_locked or governance_race)" -x</automated>
  </verify>
  <acceptance_criteria>
    - Approved/final/review-locked Source mapping 在模型前阻断，builder/model/mutation 都为零。
    - 运行期间发生的治理转换在 fresh locked row 上阻断，concurrent human data 与 prior draft 保持不变。
    - 可编辑 draft 的旧成功 API、structured output 与 adopt boundary 无回归。
  </acceptance_criteria>
  <done>Source-to-Mart direct generation 的 lifecycle 与 optimistic write 边界端到端闭合。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply symmetric Mart lifecycle enforcement and keep resolved Context questions closed</name>
  <files>backend/app/services/governance/double_layer_review.py, backend/app/services/mapping/mart_to_ybt_generator.py, backend/app/services/mapping/context_adapters.py, backend/tests/test_double_layer_mapping.py, backend/tests/test_generator_context_adapters.py</files>
  <read_first>backend/app/services/mapping/mart_to_ybt_generator.py, backend/app/services/mapping/context_adapters.py, backend/app/services/mapping/generation_readiness.py, backend/app/schemas/regulatory_context.py, backend/tests/test_double_layer_mapping.py, backend/tests/test_generator_context_adapters.py</read_first>
  <behavior>
    - Test 1: Mart-to-YBT approved/final/review-locked 初始行在 Context/model 前阻断；model barrier 期间发生同类变化在 fresh Project→task lock 后阻断，零 task mutation 和零 success audit。
    - Test 2: SourceToMartContextAdapter 输入同时含 open 与 resolved questions 时，只有 open code/text 进入 projection.prompt_text、context_questions、merged task text、trace appended codes 与 pending_confirmation。
    - Test 3: 只有 resolved questions 且其他证据完整时，不因该 resolved item 降低 readiness/confidence，也不产生 `[CTX:*]` 追加段；重复生成保持 resolved 状态关闭。
    - Test 4: Source/Mart 的 direct lifecycle error code、HTTP 409 shape、零模型断言和 snapshot race 结果对称。
  </behavior>
  <action>将 Task 1 的同一 policy 对称接入 Mart service 的 pre-Context 与 post-lock 两处，保留冻结的 approved upstream Context projection 与原 API/output。修正 `SourceToMartContextAdapter.project`，在 deterministic sort 与 MAX_CONTEXT_QUESTIONS cap 之前仅保留 `resolution_state == "open"`，与 `_projection_inputs` 一致；不要把 resolution state 丢失后再猜测。扩展 merge/trace/pending assertions，证明 resolved item 不出现在 prompt、constraint、Audit trace code 或 persisted open_questions。遵循 GEN-01、GEN-02、GEN-04 以及 D-10、D-11、D-14、D-17、D-18、D-19、D-22。</action>
  <verify>
    <automated>cd backend; python -m pytest -q tests/test_generator_context_adapters.py tests/test_double_layer_mapping.py -k "resolution_state or resolved_question or mart_to_ybt and (approved or final or review_locked or governance_race)" -x</automated>
  </verify>
  <acceptance_criteria>
    - 两类 double-layer mapping 使用完全相同、可测试的 lifecycle policy 两次校验。
    - Resolved Context question 在 prompt、merge、trace、pending state 四个表面均不可达。
    - 无 schema migration、Phase 9 Contract 改动或 final/approved 自动写入。
  </acceptance_criteria>
  <done>CR-02 与 WR-01 被生产代码和精确回归测试共同关闭。</done>
</task>

</tasks>

<threat_model>
ASVS Level: L1. Block on: high severity.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mapping lifecycle to model execution | Governed state decides whether expensive/untrusted model work may begin. |
| model result to fresh locked row | Concurrent governance changes must be authoritative before mutation. |
| typed Context questions to prompt/persistence | Resolved governance facts must not be reclassified as open by projection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05-01 | Tampering / Elevation of privilege | approved/final mapping generation | high | mitigate | Draft-only policy before model and after Project→task lock; block high severity until zero-call/zero-mutation tests pass. |
| T-10-05-02 | Tampering | concurrent review transition | high | mitigate | Fresh actor/permission validation, fixed lock order and second editability check before apply. |
| T-10-05-03 | Repudiation | governed block audit | medium | mitigate | Stable reason codes and bounded audit metadata distinguish pre-model and post-model blocks without raw content. |
| T-10-05-04 | Tampering | resolved question lifecycle | medium | mitigate | Filter typed resolution state before cap and prove absence from all prompt/merge/trace/pending surfaces. |
</threat_model>

<verification>
- `cd backend; python -m pytest -q tests/test_generator_context_adapters.py tests/test_double_layer_mapping.py -x`
- `cd backend; python -m compileall -q app`
- Static test asserts both generator functions invoke the lifecycle helper before Context build and inside the fresh locked-row branch.
</verification>

<success_criteria>
- Approved/final/review-locked Source/Mart rows cannot invoke the model or mutate through generate-draft.
- Concurrent lifecycle changes are rejected after fresh authorization and Project→task locking.
- Only unresolved Context questions remain actionable throughout projection, merge, trace and persistence.
</success_criteria>

## Artifacts this phase produces

- New internal symbols: `MappingGenerationNotEditable`, `ensure_double_layer_mapping_editable` in `backend/app/services/governance/double_layer_review.py`.
- Modified artifacts: both double-layer generator services, `mapping_rules.py`, `context_adapters.py`, and their focused tests.
- No public schema, database object, migration, package, frontend artifact, or Phase 9 Contract symbol is created.

<output>Create `.planning/phases/10-generator-refactor/10-05-SUMMARY.md` when done.</output>
