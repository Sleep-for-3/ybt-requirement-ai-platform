---
phase: 10-generator-refactor
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - backend/app/services/mapping/scenario_draft_generator.py
  - backend/app/api/scenario_mappings.py
  - backend/app/api/jobs.py
  - backend/app/api/deliverables.py
  - backend/tests/test_scenario_traceability.py
autonomous: true
requirements: [GEN-03, GEN-04]
user_setup: []
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "GEN-03 / D-01 / D-10 / D-13: Scenario business and technical generators consume the same authorized RegulatoryContext seam but retain distinct task snapshots, prompt keys, structured outputs, fields, audits, and draft renderers."
    - "D-04 / D-18: scenario task-row/status/editability/apply/audit/commit/refresh work remains local; generation never overwrites final_content or confirmed state and explicit adoption/review remains the only promotion path."
    - "GEN-04 / D-14 through D-17 / D-19: missing evidence produces merged questions and a confidence cap, core authoritative conflict/governance blocks before model execution, and unknown physical tuples are refused without discarding safe non-physical processing output."
    - "D-02 / D-05 / D-21: direct Target/Scenario/peer/evidence/RAG/catalog shared reads and ORM prompt dumps are removed; builder failure has no production fallback or draft write."
    - "D-06 / D-09 / D-20: both direct generate routes retain their response schemas and optional as_of is additive; jobs and Deliverable queued calls pass the job/package authorized Project, actor, and omitted date explicitly."
    - "D-22: direct, background, Deliverable, governance, confidentiality, physical hallucination, concurrency/interruption, audit privacy, and final-content regressions are automated."
    - "Scenario generation holds no lock across Context or model execution; a fresh fixed-order Project/task write transaction re-authorizes, reruns editability/governance, and rejects stale snapshots before direct or queued draft mutation."
    - "ASVS V4: direct callers pass complete frozen CurrentPrincipal; queued callers recover only active persisted Users as non-legacy Principal; fresh writes revalidate actor and permission, so disablement/revocation/falsey-id spoofing writes no draft."
  artifacts:
    - path: backend/app/services/mapping/scenario_draft_generator.py
      provides: "Context-only Scenario business/technical generation with distinct outputs and exact physical whitelist safety"
    - path: backend/app/api/scenario_mappings.py
      provides: "Backward-compatible direct Scenario generate/adopt/governance routes with optional as_of and authorized Project handoff"
    - path: backend/app/api/jobs.py
      provides: "Batch Scenario callers using explicit job-scoped Project/actor/date and blocked accounting"
    - path: backend/app/api/deliverables.py
      provides: "Deliverable queued Scenario callers using package/job scope while preserving governed-content skips"
    - path: backend/tests/test_scenario_traceability.py
      provides: "Scenario direct/background/Deliverable, physical safety, governance, concurrency, audit, and compatibility regressions"
  key_links:
    - from: backend/app/api/scenario_mappings.py
      to: backend/app/services/auth/permission_service.py
      via: "business.edit or technical.edit returns the Project passed into the relevant generator"
      pattern: "require_project_permission.*(business.edit|technical.edit)"
    - from: backend/app/services/mapping/scenario_draft_generator.py
      to: backend/app/services/mapping/context_adapters.py
      via: "ScenarioContextAdapter produces separate business and technical projections plus technical allowed_physical_sources"
      pattern: "ScenarioContextAdapter"
    - from: backend/app/api/jobs.py
      to: backend/app/services/mapping/scenario_draft_generator.py
      via: "_draft_handler reloads an active User from job.created_by, constructs a non-legacy frozen Principal, and passes it for initial plus fresh-transaction permission checks"
      pattern: "Principal.*is_legacy_system=False"
    - from: backend/app/api/deliverables.py
      to: backend/app/services/mapping/scenario_draft_generator.py
      via: "queued handler validates package/job equality, preserves governed-content skips, and passes the package Project explicitly"
      pattern: "generate_(business|technical)_draft"
  prohibitions:
    - "[flagged-unverified] Production Scenario generation must not fall back to Target/Scenario/peer/evidence/RAG/catalog queries or ORM dictionary prompt assembly."
    - "[flagged-unverified] Concurrent, interrupted, blocked, or successful generation must not overwrite human confirmed/final content or a confirmed governance status."
    - "[flagged-unverified] An LLM-proposed database/schema/table/column or formal governance state must not be persisted without exact governed Context proof and the existing human workflow."
    - "No frontend, SQL Generator, Semantic Impact, DataQualityExpectation, ReportingPeriod persistence, package, external service, or Phase 9 Contract redesign."
---

<objective>
Cut Scenario business and technical generators—and every direct/background/Deliverable entry point—over to the shared RegulatoryContext path while preserving human-final and physical-source safety.

Purpose: Deliver GEN-03 and GEN-04 across all invocation modes, not only direct routes, with deterministic missing-evidence behavior and no prompt or governance regression.
Output: Context-only Scenario services, compatible direct APIs, explicit scoped queued callers, and complete Scenario traceability/security tests.
</objective>

<flagged_assumptions>
- `GEN-03` — classification `unclassified`, resolution `unresolved`, verification `flagged-unverified`: all direct/queued variants operate on the same single governed row; concurrency or interruption leaves a complete AI-draft state or the unchanged prior state, and confirmed/final fields remain immutable.
- `GEN-04` — classification `unclassified`, resolution `unresolved`, verification `flagged-unverified`: when Context lacks proof, the deterministic result is a merged question, low confidence and unchanged physical fields—not a model-invented identifier or formal state.
- These boundaries apply equally to direct API, batch background jobs, and Deliverable queued generation and remain unresolved until their tests pass.
- Canonical security/compliance behavior is anchored in the ASVS L1 threat register and Phase 9 security report; only the bespoke safety prohibitions in frontmatter are marked flagged-unverified, with no invented `check_*` fields.
</flagged_assumptions>

<execution_context>
@C:/Users/李儒伟/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/李儒伟/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-generator-refactor/10-CONTEXT.md
@.planning/phases/10-generator-refactor/10-RESEARCH.md
@.planning/phases/10-generator-refactor/10-PATTERNS.md
@.planning/phases/10-generator-refactor/10-01-SUMMARY.md
@.planning/phases/10-generator-refactor/10-02-SUMMARY.md
@.planning/phases/09-regulatory-context/09-SECURITY.md
@backend/app/services/mapping/scenario_draft_generator.py
@backend/app/api/scenario_mappings.py
@backend/app/api/jobs.py
@backend/app/api/deliverables.py
@backend/tests/test_scenario_traceability.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace Scenario business generate-draft through authorized Context without final-content mutation</name>
  <files>backend/app/services/mapping/scenario_draft_generator.py, backend/app/api/scenario_mappings.py, backend/tests/test_scenario_traceability.py</files>
  <read_first>backend/app/services/mapping/scenario_draft_generator.py, backend/app/api/scenario_mappings.py, backend/app/services/governance/scenario_review.py, backend/app/services/auth/dependencies.py, backend/app/services/auth/resource_guard.py, backend/app/services/auth/permission_service.py, backend/app/models/entities.py, backend/app/services/mapping/generator_context.py, backend/app/services/mapping/context_adapters.py, backend/app/services/llm/structured_outputs.py, backend/tests/test_scenario_traceability.py</read_first>
  <behavior>
    - Test 1: The existing business generate-draft route works without a date and with optional as_of, returns ScenarioBusinessMappingRead, preserves prompt key/output fields, and carries the resolved date/version in redacted audit trace.
    - Test 2: The route loads the task, runs the existing editability guard, requires `business.edit`, and passes the exact authorized Project/actor/date into one candidate Context build scoped by target_field_id and scenario_id.
    - Test 3: Existing final_content, human questions and confirmation fields remain byte-for-byte unchanged; Context/model questions merge idempotently, and builder/readiness/confidentiality/runtime failure invokes no fallback and leaves all task fields unchanged.
    - Test 4: Context may commit its retrieval trace and a concurrent human may change business inputs/questions/final/confirmation/Project governance during the model call; the fresh post-model Project/task lock and canonical comparison returns `409 stale-task` with no draft or success audit.
    - Test 5: The route passes its exact frozen CurrentPrincipal; disabling that User is rejected by fresh actor validation, while revoking/changing ProjectMembership status/role is rejected by fresh `PermissionService(db, actor).require_project_permission(..., "business.edit")`, both before Project→task locks or mutation.
  </behavior>
  <action>Write direct-route and service tests first. Change `generate_business_draft` to require frozen actor Principal plus authorized Project/as_of/date provider; no id-only overload. The route passes all CurrentPrincipal fields unchanged. Initially validate actor and authorize business.edit, load task/Project without a lock, run editability and capture the local snapshot; build one Context and execute the existing runtime without a lock. After model success persist attempt-only logs, clear stale state, then in a fresh transaction first call `validate_generation_actor(db, actor)`, next `PermissionService(db, actor).require_project_permission(snapshot.project_id, "business.edit")`, then lock Project→task and compare security/editability/governance/final/local snapshot fields. Disabled User or revoked/changed permission fails closed before mutation; mismatch/deletion is stale non-success. Only exact match permits draft/success audit. Direct legacy requires actor.is_legacy_system=True; no falsey-id inference. Preserve route/output/governance/no-shared-query boundaries.</action>
  <verify>
    <automated>cd backend; python -m pytest -q tests/test_scenario_traceability.py -k "business and (generate or context or final or questions)" -x</automated>
  </verify>
  <acceptance_criteria>
    - Direct Scenario business generation uses one authorized Context and the existing distinct prompt/output contract with no generator-side shared-fact query.
    - Human final/confirmed/question content survives success and every failure path; only the AI draft/task-local allowed fields change atomically.
    - Old route clients remain compatible and optional as_of is additive and traceable.
  </acceptance_criteria>
  <done>Scenario business generation is fully Context-backed and preserves its existing governance and API behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cut Scenario technical generation over with exact Context physical whitelist safety</name>
  <files>backend/app/services/mapping/scenario_draft_generator.py, backend/app/api/scenario_mappings.py, backend/tests/test_scenario_traceability.py</files>
  <read_first>backend/app/services/mapping/scenario_draft_generator.py, backend/app/services/mapping/context_adapters.py, backend/app/services/mapping/generation_readiness.py, backend/app/services/governance/scenario_review.py, backend/app/services/auth/dependencies.py, backend/app/services/auth/permission_service.py, backend/app/models/entities.py, backend/app/schemas/regulatory_context.py, backend/tests/test_scenario_traceability.py</read_first>
  <behavior>
    - Test 1: Technical generation keeps prompt key `scenario_technical_lineage`, ScenarioTechnicalOutput, response fields, editability and adoption while sharing the same Context seam and a distinct technical projection.
    - Test 2: Existing physical values may remain unchanged; a new exact tuple projected from same-task catalog evidence/scoped lineage is accepted; an unknown or foreign tuple is skipped, current values remain, a deterministic question is merged, and safe processing logic may still be applied when readiness allows.
    - Test 3: Missing evidence/knowledge/lineage caps confidence and never permits a formal status; authoritative core conflict, governance prohibition, builder failure, restricted external send, or interrupted/concurrent call leaves AI draft/final/confirmed/physical fields in one complete governed state.
    - Test 4: Concurrent physical tuple, target/scenario scope, task status/final/questions/draft, Project security/governance or updated_at changes during runtime are found only by the fresh authoritative reload and reject the stale technical output without overwriting the concurrent row.
  </behavior>
  <action>Apply the same frozen Principal protocol to technical generation: initial actor validation/technical.edit and local snapshot without a lock; one Context/projection/runtime; then clear stale state and in a fresh transaction validate active actor, re-run PermissionService technical.edit, and only then lock Project→task for security/editability/governance/final/snapshot comparison. Disabled/revoked/stale actors yield non-success with no draft/success audit. Preserve exact physical whitelist/warning/cap behavior, distinct runtime/output/route/adopt contracts, zero direct shared-fact queries and no model formal state. Direct legacy still requires explicit is_legacy_system=True.</action>
  <verify>
    <automated>cd backend; python -m pytest -q tests/test_scenario_traceability.py -k "technical and (generate or physical or context or confidence)" -x</automated>
  </verify>
  <acceptance_criteria>
    - Scenario technical generation issues zero direct catalog/evidence/retrieval/shared-fact queries and uses only the Context-derived whitelist plus unchanged current values.
    - Unproved physical identifiers are never persisted, always produce a stable question, and do not erase otherwise safe non-physical output.
    - Final/confirmed fields and route/structured-output compatibility hold under success, block, failure, concurrency, and interruption.
  </acceptance_criteria>
  <done>Both Scenario task variants are Context-backed, task-distinct, governance-safe, and resistant to physical identifier hallucination.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate batch and Deliverable queued callers with explicit Project scope and blocked accounting</name>
  <files>backend/app/api/jobs.py, backend/app/api/deliverables.py, backend/tests/test_scenario_traceability.py</files>
  <read_first>backend/app/api/jobs.py, backend/app/api/deliverables.py, backend/app/models/entities.py, backend/app/models/governance.py, backend/app/services/auth/dependencies.py, backend/app/services/auth/permission_service.py, backend/app/services/mapping/generator_context.py, backend/app/services/mapping/scenario_draft_generator.py, backend/tests/test_scenario_traceability.py</read_first>
  <behavior>
    - Test 1: Batch business/technical jobs reject `created_by` None/0, missing or disabled User; an active persisted user is reloaded and converted exactly to non-legacy `Principal(id, username, display_name, False)`, then Project and required business.edit/technical.edit permission are checked for every generator.
    - Test 2: Deliverable generation validates package/job project equality, keeps final/confirmed/existing-AI skip guards, passes package Project/actor/as_of=None, and records readiness blocks as blocked rather than successful or failed draft generation.
    - Test 3: Builder/readiness/runtime failure affects only that BackgroundJobItem, creates no task draft mutation, does not trigger a legacy signature/fallback, and leaves resumable/cancelled job semantics and existing response keys compatible; additive blocked_count is stable.
    - Test 4: A direct, batch, or Deliverable task changed during model execution is classified as stale/blocked for that item, preserves the concurrent human/governance update, writes no draft/success audit, and never retries through a legacy path.
    - Test 5: While queued generation waits at the model barrier, a second Session disables User or revokes/changes ProjectMembership status/role; fresh actor validation or PermissionService respectively denies the item. Queued None/0 cannot impersonate legacy, while direct explicit legacy remains unchanged.
  </behavior>
  <action>Update `_draft_handler` and `_deliverable_generate_handler` to recover identity through the 10-01 helper from the persisted creator id before generation. Reject None, 0, missing User and `User.status != "active"`; for an active row construct only `Principal(user.id, user.username, user.display_name, False)`. Do not synthesize legacy and do not add persistence. Resolve Project/institution and require the task-family permission with PermissionService initially; pass the frozen Principal and authorized Project to the generator, which repeats PermissionService inside its fresh write transaction before Project→task locks. If queued legacy identity cannot be safely recovered, reject that item as stable blocked/failed non-success with bounded reason code, zero draft and zero success audit; direct legacy remains limited to explicit current-request Principal. Treat identity loss, permission revocation, readiness and stale errors as redacted blocked outcomes without automatic retry/fallback; preserve rollback/reload/cancel/resume, package/fingerprint/governed-content skips, mapping compilation and response counters. Tests pause at the model boundary and use a second Session to disable User or revoke/change active ProjectMembership status/role, proving fresh denial across batch and Deliverable paths.</action>
  <verify>
    <automated>cd backend; python -m pytest -q tests/test_scenario_traceability.py -k "batch or deliverable or background or queued" -x</automated>
  </verify>
  <acceptance_criteria>
    - Direct API, batch jobs, and Deliverable queued handlers all call the same explicit authorized generator contract; no old callable signature remains.
    - Existing governed-content skips and cancellation/resume behavior remain intact, while Context/readiness blocks are distinguishable and produce no draft.
    - Background audit/error summaries contain only bounded codes and ids, not prompt, evidence, knowledge, draft, or final text.
  </acceptance_criteria>
  <done>Every Scenario generation entry point now uses the same scoped Context path and preserves queued-workflow compatibility.</done>
</task>

</tasks>

<threat_model>
ASVS Level: L1. Block on: high severity.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| direct route/job/package to Project | Resource ids and queued state must resolve to one authorized project/institution before Context build. |
| Context/task snapshot to business/technical prompt | Potentially confidential governed facts cross into two distinct model contracts. |
| model/readiness result to Scenario row | Physical identifiers, AI content and confidence approach governed final/confirmed state under concurrent and queued execution. |
| queued diagnostics to job/audit storage | Failure/block evidence must remain actionable without persisting sensitive prompt/content bodies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03-01 | Spoofing / Information disclosure | direct/background/Deliverable Principal and Project handoff | high | mitigate | Pass direct frozen Principal intact; recover queued active User as non-legacy; reject None/0/missing/disabled identities; repeat business.edit/technical.edit authorization after model return and test revoked membership/role plus foreign scope. |
| T-10-03-02 | Tampering / Information disclosure | Scenario prompt adapters | high | mitigate | Use explicit task snapshots, quoted bounded Context data, every selected confidentiality label and distinct schemas; test restricted external denial and absence of ORM dumps. |
| T-10-03-03 | Tampering | Context/readiness/fallback path | high | mitigate | Delete direct shared queries/helpers, propagate builder errors, block core conflicts/governance before model, and assert no fallback or draft mutation for all callers. |
| T-10-03-04 | Tampering / Elevation of privilege | stale/final/confirmed/physical Scenario fields | high | mitigate | Snapshot without a lock, then fresh fixed-order Project/task re-lock, re-authorization and comparison before atomic capped/whitelisted apply; stale direct/queued results write no draft and preserve adoption/review authority. |
| T-10-03-05 | Repudiation / Information disclosure | Scenario/job/deliverable audits | high | mitigate | Persist only ids/codes/counts/date/readiness/hash/proof ids/status and negative-test raw prompt/evidence/knowledge/draft/final content. |
| T-10-03-06 | Denial of service | queued task fan-out | medium | mitigate | One Context build per row, bounded adapters, cancellation checks, per-item rollback, blocked accounting, and fixed query-growth qualification. |
</threat_model>

<verification>
- `cd backend; python -m pytest -q tests/test_generator_context_adapters.py tests/test_scenario_traceability.py -x`
- `cd backend; python -m pytest -q tests/test_llm_runtime.py tests/test_governance.py tests/test_deliverables.py tests/test_productization.py -x`
- `cd backend; python -m compileall -q app`
- Static review confirms Scenario generator no longer imports/query-builds Target/Scenario/peer/evidence/RAG/catalog shared facts or serializes ORM dictionaries.
</verification>

<success_criteria>
- GEN-03 and GEN-04 are satisfied for Scenario business and technical direct routes, batch jobs, and Deliverable queued handlers.
- Final/confirmed content, editability, explicit adoption/review, task-specific instructions/outputs, confidentiality, physical safety, question merge, confidence cap, and audit provenance remain intact.
- Every caller builds Context once, performs no generator-side shared-fact fallback, and leaves no draft on block/failure/interruption.
</success_criteria>

## Artifacts this phase produces

- `backend/app/services/mapping/scenario_draft_generator.py`: Context-only distinct Scenario business/technical generation.
- `backend/app/api/scenario_mappings.py`: compatible optional-as_of direct generation with explicit Project authorization.
- `backend/app/api/jobs.py`: scoped batch generator caller and blocked accounting.
- `backend/app/api/deliverables.py`: scoped queued Scenario caller with governed-content preservation.
- `backend/tests/test_scenario_traceability.py`: GEN-03/GEN-04 direct/queued/governance/physical/concurrency/audit proof.

<output>Create `.planning/phases/10-generator-refactor/10-03-SUMMARY.md` when done.</output>
