---
phase: 10-generator-refactor
plan: 07
type: execute
wave: 5
depends_on: ["10-04"]
files_modified:
  - backend/app/api/target_fields.py
  - backend/app/services/mapping_generator.py
  - backend/tests/test_legacy_mapping_retirement.py
autonomous: true
requirements: [GEN-01, GEN-02, GEN-04]
gap_closure: true
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "GEN-01 / GEN-02 / D-01 / D-05 / D-21: POST /fields/{field_id}/generate-mapping remains an explicit route but is retired with stable HTTP 410 after resource authorization; it cannot build facts, call a model or write FieldAnalysisTask/FieldMappingDraft/EvidenceReference."
    - "Legacy RAG/SQL/template/NaturalLanguageTask composite constructor is removed from production, so no second Source-to-Mart/Mart-to-YBT shared-fact authority remains."
    - "D-18 / D-20: existing FieldMappingDraft records remain readable through latest-draft and reviewable through the existing review route; retirement deletes no persisted data or schema."
    - "GEN-04 / D-17 / D-22: retired requests never manufacture candidate physical fields, evidence-supported claims or formal review state, and tests prove zero runtime mutation."
  artifacts:
    - path: backend/app/api/target_fields.py
      provides: "Authorized explicit retirement response for the legacy composite mapping route while preserving draft read/review routes"
    - path: backend/tests/test_legacy_mapping_retirement.py
      provides: "Route, authorization, zero-model, zero-mutation, data-preservation and static isolation regressions"
  key_links:
    - from: backend/app/api/target_fields.py
      to: backend/app/services/auth/permission_service.py
      via: "field project is resolved and technical.edit is required before retirement detail is disclosed"
      pattern: "require_project_permission.*technical.edit"
    - from: backend/app/api/target_fields.py
      to: backend/app/services/mapping_generator.py
      via: "no production import or call remains; the obsolete module is removed"
      pattern: "legacy-mapping-generator-retired"
    - from: backend/tests/test_legacy_mapping_retirement.py
      to: backend/app/models/entities.py
      via: "before/after counts and row bytes prove no analysis/draft/evidence mutation while existing drafts stay readable"
      pattern: "FieldAnalysisTask|FieldMappingDraft|EvidenceReference"
---

<objective>
明确退役仍在生产中的 legacy `/fields/{field_id}/generate-mapping` 共享事实构造器，同时保留已有草稿数据的读/审能力。

Purpose: 消除 RAG、SQL parse、template 与 database-probe 组成的竞争性 Source/Mart 生成链，满足 sole Context authority 与 no-fallback 合同。
Output: 授权后的稳定 410 retirement façade、删除 obsolete service、现存数据兼容测试与静态隔离证明。
</objective>

<flagged_assumptions>
- `GEN-04` unclassified 假设通过 retirement 行为收敛：该入口不再从未经 Context authority/readiness 分类的来源生成物理候选或 evidence-supported 声明。
- 退役只改变该 POST 的生成成功行为；latest draft 与 review 数据合同、数据库表和历史记录保持。
- 此选择的撤销成本为 coordinated route/service restoration，但不需要数据迁移；在实现 task 上记录为 costly。
</flagged_assumptions>

<execution_context>
@C:/Users/李儒伟/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/李儒伟/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-generator-refactor/10-CONTEXT.md
@.planning/phases/10-generator-refactor/10-VERIFICATION.md
@backend/app/api/target_fields.py
@backend/app/services/mapping_generator.py
@backend/app/services/auth/permission_service.py
@backend/app/models/entities.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace an authorized legacy generation request to an explicit non-mutating retirement response</name>
  <reversibility rating="costly">The route and stored drafts remain, but restoring generation later requires a coordinated governed implementation and client requalification.</reversibility>
  <files>backend/app/api/target_fields.py, backend/tests/test_legacy_mapping_retirement.py</files>
  <read_first>backend/app/api/target_fields.py, backend/app/services/mapping_generator.py, backend/app/services/auth/dependencies.py, backend/app/services/auth/permission_service.py, backend/app/models/entities.py, backend/app/schemas/__init__.py</read_first>
  <behavior>
    - Test 1: Authenticated technical.edit user calling the unchanged POST path receives HTTP 410 detail code `legacy-mapping-generator-retired` with canonical replacement route families; request payload remains parseable.
    - Test 2: Missing field remains 404 and foreign/unauthorized project access fails through PermissionService before retirement detail; route does not leak another project's field existence/content.
    - Test 3: Spies on `RegulatoryContextBuilder`, prompt runtime, `execute_runtime_chat` and former service are untouched; FieldAnalysisTask/FieldMappingDraft/EvidenceReference counts and existing row bytes remain identical.
    - Test 4: Existing GET latest draft and PATCH review routes still return/update pre-existing FieldMappingDraft data after POST retirement.
  </behavior>
  <action>先创建 dedicated regression file。保留 route path 与 request parsing，但在 `target_fields.py` 中按 field id 加载当前 TargetField，使用 exact CurrentPrincipal 和 PermissionService 要求该 field.project_id 的 `technical.edit`，随后返回稳定 HTTP 410 detail，明确客户端应使用 governed Source-to-Mart 与 Mart-to-YBT generate-draft routes。不得调用 builder、model 或旧 service，不得创建 task/draft/evidence。保留 latest-draft 和 review_draft 行为以及数据库 schema。该 retirement 是 verification contract 明确允许的 isolation 选项，并执行 D-01、D-02、D-05、D-17、D-18、D-20、D-21、D-22。</action>
  <verify>
    <automated>cd backend; python -m pytest -q tests/test_legacy_mapping_retirement.py -k "route or zero_mutation or existing_draft" -x</automated>
  </verify>
  <acceptance_criteria>
    - 旧 POST route 明确、授权且稳定地退役，不发生任何共享事实或模型工作。
    - Unauthorized/foreign resource 不获得 retirement detail 或 field data。
    - 历史 draft 数据与其 read/review API 不受影响。
  </acceptance_criteria>
  <done>生产入口已被隔离为可诊断、零副作用的 retirement façade。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove the obsolete composite constructor and prove no production reference survives</name>
  <files>backend/app/api/target_fields.py, backend/app/services/mapping_generator.py, backend/tests/test_legacy_mapping_retirement.py</files>
  <read_first>backend/app/services/mapping_generator.py, backend/app/api/target_fields.py, backend/app/services/llm/structured_outputs.py, backend/tests/test_legacy_mapping_retirement.py</read_first>
  <behavior>
    - Test 1: Production AST/import graph has no `app.services.mapping_generator`, `generate_mapping_draft`, `legacy_field_mapping` runtime key or helper calls that query documents/SQL/templates/NL tasks for this endpoint.
    - Test 2: Obsolete module is deleted and application import/startup plus target_fields router import succeeds.
    - Test 3: Route retirement, draft read/review and unrelated governed mapping routes continue to compile and pass focused API tests.
  </behavior>
  <action>删除 `backend/app/services/mapping_generator.py`，并清除 `target_fields.py` 的 import。测试通过 AST/module inventory 检查生产代码，不用注释或 inert aliases 保留旧 symbol。不要删除 `LegacyFieldDraftOutput`、FieldAnalysisTask、FieldMappingDraft 或 EvidenceReference schema/model，因为它们可能承载既有数据或其他兼容读取；本 phase 只移除活跃生成构造器。遵循 scope fence：不新增 replacement universal generator、SQL Generator、frontend 或 migration。</action>
  <verify>
    <automated>cd backend; python -m pytest -q tests/test_legacy_mapping_retirement.py -x</automated>
    <automated>cd backend; python -m compileall -q app</automated>
  </verify>
  <acceptance_criteria>
    - Obsolete service file 和所有 production references 不存在。
    - App compile/router import 成功，retired route 与 existing draft APIs 测试通过。
    - 无数据库对象、历史记录或 unrelated file 被删除。
  </acceptance_criteria>
  <done>竞争性的 legacy RAG/SQL/template shared-fact constructor 已从生产代码彻底移除。</done>
</task>

</tasks>

<threat_model>
ASVS Level: L1. Block on: high severity.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| field id/principal to retirement response | Resource existence and replacement guidance must follow project authorization. |
| retired route to persistence/model runtime | A retired request must have no hidden generation or write side effect. |
| historical FieldMappingDraft to compatibility APIs | Existing user data must remain readable/reviewable after service removal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-07-01 | Information disclosure / Spoofing | retired field route | high | mitigate | Resolve resource and require technical.edit before 410 detail; test foreign/unauthorized behavior. |
| T-10-07-02 | Tampering | hidden legacy generator invocation | high | mitigate | Remove module/imports and assert zero builder/model/task/draft/evidence side effects. |
| T-10-07-03 | Tampering / Repudiation | historical draft records | high | mitigate | No schema/data deletion; before/after row-byte and read/review compatibility tests. |
| T-10-07-04 | Denial of service | repeated retired calls | low | accept | Bounded local authorization and 410 response perform no retrieval/model work. |
</threat_model>

<verification>
- `cd backend; python -m pytest -q tests/test_legacy_mapping_retirement.py -x`
- `cd backend; python -m compileall -q app`
- Application import smoke verifies target_fields router no longer imports the removed module.
</verification>

<success_criteria>
- `/fields/{field_id}/generate-mapping` cannot compete with RegulatoryContextBuilder or mutate any generation data.
- Authorized callers receive a stable explicit retirement contract; historical drafts retain read/review compatibility.
- No legacy composite constructor, production fallback, migration or replacement universal generator is introduced.
</success_criteria>

## Artifacts this phase produces

- New test artifact: `backend/tests/test_legacy_mapping_retirement.py`.
- Modified artifact: `backend/app/api/target_fields.py` with an authorized 410 retirement façade; no new public Python symbol is required.
- Removed obsolete artifact: `backend/app/services/mapping_generator.py`; model/schema data artifacts remain intact.

<output>Create `.planning/phases/10-generator-refactor/10-07-SUMMARY.md` when done.</output>
