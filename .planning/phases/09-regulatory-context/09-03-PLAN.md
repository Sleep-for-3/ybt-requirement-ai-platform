---
phase: 09-regulatory-context
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - backend/app/services/semantic/context_builder.py
  - backend/app/services/semantic/context_collectors.py
  - backend/app/services/semantic/context_conflicts.py
  - backend/app/services/semantic/__init__.py
  - backend/tests/test_semantic_layer.py
  - backend/tests/test_regulatory_context_builder.py
autonomous: true
requirements: [CTX-03, CTX-04]
user_setup: []
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-02 and D-03: Builder semantic reads use the shared confirmed-only policy by default and candidate mode is an explicit request, with rejected and deprecated lifecycle rows excluded from trusted output."
    - "D-05 and D-06: Source/Mart candidates and semantic evidence use explicit ranking tiers, stable tie-breakers, match reasons, evidence, and provenance without auto-confirmation."
    - "D-13: Builder selects SemanticConceptVersion by inclusive as_of and carries reporting_period only as a normalized label."
    - "D-16: Every ContextFact uses the AuthorityRank and FactState enums defined by 09-02, derives authority from its registered source_type through authority_for_source(), and keeps authority independent from lifecycle state."
    - "D-17: ContextBuilder is a projection-only orchestration service over existing models and creates no copied context facts, mappings, lineage, cache, or snapshot."
    - "D-18: Missing, stale, historical-only, evidence-insufficient, and contradictory facts become deterministic conflicts or open questions rather than silent winners."
    - "D-19: Candidate collection ranks confirmed bindings/mappings, exact code/name, semantic evidence, metadata, history, lineage neighborhood, and retrieval evidence before applying any output cap."
    - "D-20: PermissionService authorizes the caller and returns the Project before the builder runs; the builder and collectors accept that authorized Project, reject a mismatched request.project_id, and derive institution_id only from Project.institution_id."
    - "RegulatoryKnowledgeItem is collected only by its own project_id and emits source_type regulatory_knowledge_item with AuthorityRank.REGULATORY from authority_for_source() and a non-confirmed FactState; HybridRetriever separately retains its existing project/global/institution KnowledgeUnit visibility and retrieved provenance."
    - "Raw LineageEdge is verified only when the edge is enabled, both nodes are resolved, confidence is high, and its ScriptFileVersion is the enabled ScriptFile's current version; mapping/scenario lineage uses its real lineage_status and lineage_last_verified_at fields without an invented age threshold."
    - "Repeated builds preserve stable domain sections, ordering, conflicts, questions, and normalized facts while built_at and newly-created RetrievalLog ids are validated as legitimate per-build metadata."
    - "D-22: Collectors batch by ids with IN, joins, or select-in patterns and a measured query-count test proves the builder collector path is bounded; the existing HybridRetriever vector/hybrid KnowledgeUnit db.get fallback is measured honestly or avoided via the keyword-only path, and no cache layer is added."
    - "D-24: The acceptance fixture for target table 2.3 同业客户表 and field 客户统一编号 returns date-effective facts, provenance, conflicts, and open questions with stable output."
    - "The CTX-01..CTX-04 spec-less edge probe remains exactly four planning/test metadata records marked unclassified/unresolved; the builder never turns those synthetic records into ContextOpenQuestion or product output."
  artifacts:
    - path: backend/app/services/semantic/context_builder.py
      provides: "Projection-only RegulatoryContextBuilder orchestration and deterministic serialization entry point"
    - path: backend/app/services/semantic/context_collectors.py
      provides: "Batched project-scoped metadata, semantic, mapping, evidence, lineage, knowledge, retrieval, and historical collectors"
    - path: backend/app/services/semantic/context_conflicts.py
      provides: "Stable conflict and open-question catalog and deterministic detector"
    - path: backend/tests/test_regulatory_context_builder.py
      provides: "Acceptance fixture, isolation/confidentiality, gap, projection-only, ranking, planning-only edge non-emission, and honest query-count tests"
    - path: backend/tests/test_semantic_layer.py
      provides: "Contract-shaped additive version-route regression that derives as_of from the listed initial version"
  key_links:
    - from: backend/app/services/auth/permission_service.py
      to: backend/app/services/semantic/context_builder.py
      via: "Caller constructs RegulatoryContextBuilder(db) and passes the Project returned by require_project_permission into build(request, authorized_project=project)"
    - from: backend/app/services/semantic/context_builder.py
      to: backend/app/services/semantic/status_policy.py
      via: "Builder requests trusted or explicit candidate semantic visibility through the shared policy"
    - from: backend/app/services/semantic/context_builder.py
      to: backend/app/services/semantic/version_service.py
      via: "Builder resolves one inclusive confirmed version for the requested as_of"
    - from: backend/app/services/semantic/context_builder.py
      to: backend/app/services/semantic/context_collectors.py
      via: "Orchestration passes the PermissionService-authorized Project to batched collectors instead of trusting caller-supplied scope or embedding SQL in a God Service"
    - from: backend/app/services/semantic/context_collectors.py
      to: backend/app/services/retrieval/hybrid_retriever.py
      via: "Knowledge retrieval retains RetrievalLog id, confidentiality, source location, and retrieved state"
    - from: backend/app/services/semantic/context_conflicts.py
      to: backend/app/schemas/regulatory_context.py
      via: "Stable issue codes become typed ContextConflict and ContextOpenQuestion values"
  prohibitions:
    - "No descriptor-less entity discovery, ORM dumps, unconstrained fact dictionaries, or source text outside bounded section values."
    - "No flagged/unverified, rejected, deprecated, retrieved, or AI candidate is promoted to confirmed truth."
    - "No modification to Phase 10 generators, frontend routes, mapping generators, or generator task instructions."
    - "No RegulatoryContext persistence, cache, snapshot, copied mapping, or replicated lineage is introduced."
    - "No natural-order limit(50) retrieval is used; candidate caps apply only after explicit ranking and stable tie-breakers."
    - "No Phase 8 semantic API is deleted or renamed, and no PendingQuestion row is written by a read-only build."

---

<objective>
Build the projection-only RegulatoryContextBuilder and its batched collectors over the existing semantic, metadata, mapping, evidence, lineage, knowledge, retrieval, and historical models.

Purpose: Satisfy CTX-03 and CTX-04 with one stable, project-isolated acceptance path that exposes authoritative facts, candidates, gaps, provenance, confidentiality, and measured performance without creating a second fact source.
Output: Builder orchestration, separated collector and conflict modules, the acceptance fixture for 2.3 同业客户表 / 客户统一编号, deterministic edge/conflict behavior, and query-count/isolation qualification.
</objective>

<assumptions>
- The exact numeric query budget is absent from source artifacts. The executor must measure the canonical fixture's baseline before locking `ACCEPTANCE_QUERY_BUDGET` in the builder test, then assert the same budget remains bounded as fixture row counts grow; no guessed threshold is acceptable.
- HybridRetriever's existing RetrievalLog commit is retained as retrieval provenance when retrieval is invoked. The builder may not write ContextFact, SemanticConcept, Binding, Relation, Mapping, Lineage, PendingQuestion, cache, or snapshot rows, and tests must distinguish the permitted existing log from authoritative mutation.
- Reporting-period normalization follows the contract from 09-02 and never creates a new persistence system.
- The four spec-less CTX records are planning/test metadata only: the builder may use a test-local table to verify requirement coverage, but it must not create `ContextOpenQuestion`, `ContextFact`, persisted rows, or API-visible output for those synthetic records.
- `RegulatoryKnowledgeItem` is strictly project-scoped through its own `project_id`; it does not inherit HybridRetriever visibility and has no governance status. `KnowledgeUnit` retrieval remains a separate path whose established visibility is project rows plus visible global rows plus institution rows matching the authorized Project, with RetrievalLog provenance.
- The shared 09-03/09-04 call is exactly `builder = RegulatoryContextBuilder(db)` followed by `context = builder.build(request, authorized_project=project)`. `project` must be the Project returned by `PermissionService.require_project_permission(..., "project.view")`; the builder rejects `request.project_id != project.id`, collectors receive that Project, and institution scope is derived only from `project.institution_id`.
- The 09-02 contract resolves authority/state vocabulary: use only its `AuthorityRank`, `FactState`, registered source types, and `authority_for_source()` policy. The only remaining research measurement is the numeric query budget for the canonical fixture.
- `SemanticConceptCreate` does not accept `effective_from`. Keep the initial create payload unchanged; after listing versions, read `initial_effective_from = listed.json()[0]["effective_from"]` and use that returned value as the effective-route `as_of`, without changing production code or request schemas.
</assumptions>

<execution_context>
@C:/Users/李儒伟/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/李儒伟/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-regulatory-context/09-CONTEXT.md
@.planning/phases/09-regulatory-context/09-RESEARCH.md
@.planning/phases/09-regulatory-context/09-PATTERNS.md
@.planning/phases/09-regulatory-context/09-01-SUMMARY.md
@.planning/phases/09-regulatory-context/09-02-SUMMARY.md
@backend/app/schemas/regulatory_context.py
@backend/app/services/semantic/status_policy.py
@backend/app/services/semantic/entity_adapter.py
@backend/app/services/semantic/version_service.py
@backend/app/services/retrieval/hybrid_retriever.py
@backend/app/models/entities.py
@backend/app/models/lineage.py
@backend/app/models/deliverables.py
@backend/app/services/auth/permission_service.py
@backend/tests/test_semantic_layer.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the acceptance target through one projection-only context build</name>
  <files>backend/tests/test_semantic_layer.py, backend/app/services/semantic/context_builder.py, backend/app/services/semantic/context_collectors.py, backend/app/services/semantic/__init__.py, backend/tests/test_regulatory_context_builder.py</files>
  <read_first>backend/app/schemas/regulatory_context.py, backend/app/services/semantic/status_policy.py, backend/app/services/semantic/entity_adapter.py, backend/app/services/semantic/version_service.py, backend/app/models/entities.py, backend/tests/test_semantic_layer.py</read_first>
  <behavior>
    - Test 1: `test_additive_version_routes_preserve_concept_compatibility_and_static_precedence` keeps the existing SemanticConceptCreate payload unchanged, reads `initial_effective_from = listed.json()[0]["effective_from"]` after listing version 1, and requests `/versions/effective` with `as_of=initial_effective_from`, so the test follows the real create contract without wall-clock drift.
    - Test 2: Building a context for target table 2.3 同业客户表 and target field 客户统一编号 with the PermissionService-authorized Project returns a typed RegulatoryContext with project/institution scope, target identity, as_of, semantic facts, metadata facts, and build metadata.
    - Test 3: A request whose project_id differs from the authorized Project is rejected before any collector runs; institution_id is always derived from that Project, and the confirmed SemanticConceptVersion is resolved for the fixed requested date through the existing version service.
    - Test 4: Repeating the same build preserves normalized domain facts, section ordering, conflicts, and questions. The test excludes only built_at and retrieval-log identifiers from the stable comparison, then separately validates timezone-aware per-build built_at values and any newly persisted RetrievalLog ids.
  </behavior>
  <action>Before changing builder production code, stabilize only `backend/tests/test_semantic_layer.py`: in `test_additive_version_routes_preserve_concept_compatibility_and_static_precedence`, leave the initial SemanticConceptCreate payload unchanged, keep the existing list call, assign `initial_effective_from = listed.json()[0]["effective_from"]`, and pass that exact value as the effective-route `as_of`; run that single test first. This repairs test-date drift without sending an unsupported create field or changing `api/semantic.py`, `version_service.py`, or production semantics. Then, per D-02, D-03, D-13, D-14, D-17, D-20, and D-24, implement the shared calling contract exactly: `builder = RegulatoryContextBuilder(db)` and `context = builder.build(request, authorized_project=project)`, where `project` is the Project returned by PermissionService. Assert `request.project_id == project.id` before collector invocation, derive `ContextScope.institution_id` only from `project.institution_id`, pass that Project into collectors, resolve the effective semantic version through the shared policy and `version_service`, normalize outputs into 09-02 ContextFact values, create empty typed remaining sections, and return RegulatoryContext. Keep SQL out of the builder; collectors accept the authorized Project plus bounded ids and explicit project predicates. Build every fact with a registered source_type and `authority_for_source(source_type)`, using only existing AuthorityRank and FactState members. The acceptance path persists no context fact and preserves trusted confirmed-only behavior with explicit candidate mode. For repeatability, compare normalized stable domain sections after removing only built_at and retrieval-log identifiers; separately validate aware built_at and each retrieved fact/log/build-metadata linkage. Do not inject the four planning-only spec-less records.</action>
  <verify>
    <automated>cd backend; python -m pytest -q tests/test_semantic_layer.py::test_additive_version_routes_preserve_concept_compatibility_and_static_precedence -x; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; python -m pytest -q tests/test_regulatory_context_builder.py -k "acceptance or projection or repeat or authorized_project" -x</automated>
  </verify>
  <acceptance_criteria>
    - The additive version-route test passes by using the listed initial version's returned effective_from as as_of; the create payload and production temporal behavior remain unchanged.
    - `builder = RegulatoryContextBuilder(db); context = builder.build(request, authorized_project=project)` returns RegulatoryContext 1.0, where project came from PermissionService; mismatch is rejected before collector reads and institution scope comes only from that Project.
    - The builder delegates SQL to collectors carrying that Project, resolves effective versions by as_of, and never adds a context fact, mapping, lineage, snapshot, cache, or PendingQuestion row.
    - Trusted mode contains only confirmed semantic inputs; candidate mode must be explicit and does not alter the status of any source row.
  </acceptance_criteria>
  <done>The contract-shaped date regression is green first, then the shared authorized Builder call produces deterministic domain content with valid volatile build/retrieval metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add batched source-family collectors and deterministic conflict/open-question detection</name>
  <files>backend/app/services/semantic/context_builder.py, backend/app/services/semantic/context_collectors.py, backend/app/services/semantic/context_conflicts.py, backend/tests/test_regulatory_context_builder.py</files>
  <read_first>backend/app/services/semantic/context_builder.py, backend/app/services/semantic/context_collectors.py, backend/app/schemas/regulatory_context.py, backend/app/models/entities.py, backend/app/models/lineage.py, backend/app/models/deliverables.py, backend/app/api/knowledge_items.py, backend/app/services/recommendation/source_field_recommender.py, backend/app/services/retrieval/hybrid_retriever.py, backend/app/services/deliverables/validation_service.py</read_first>
  <behavior>
    - Test 1: A populated acceptance fixture aggregates semantic, metadata, all four mapping families (`SourceToMartMapping`, `MartToYbtMapping`, `ScenarioBusinessMapping`, `ScenarioTechnicalLineage`), `MappingEvidenceReference`, `LineageNode/Edge`, project-scoped `RegulatoryKnowledgeItem`, `KnowledgeUnit/HybridRetriever`, and `HistoricalCaliberItem` facts with source and evidence references.
    - Test 2: Missing confirmed binding/version, missing Source-to-Mart, missing Mart-to-YBT, missing or stale lineage, missing knowledge, missing evidence, historical-only definition, and contradictory authoritative facts produce stable conflict/open-question codes; `MISSING_SOURCE_MAPPING` and `MISSING_KNOWLEDGE` are exact. `MISSING_KNOWLEDGE` means no visible matching `RegulatoryKnowledgeItem` and no visible enabled matching `KnowledgeUnit`; `MISSING_EVIDENCE` means a fact or knowledge item exists but lacks sufficient supporting evidence.
    - Test 3 (`test_source_type_authority_and_knowledge_scope_are_contract_driven`): Every emitted fact satisfies `fact.authority is authority_for_source(fact.source_type)`. `RegulatoryKnowledgeItem` is queried only with `item.project_id == authorized_project.id`, uses source_type `regulatory_knowledge_item`, AuthorityRank.REGULATORY, and a non-confirmed FactState because the model has no governance field. HybridRetriever KnowledgeUnit results separately preserve its existing project/global/institution visibility; retrieved facts use source_type `retrieved_knowledge`, AuthorityRank.RETRIEVED, FactState.RETRIEVED, matching confidentiality, and a RetrievalLog id.
    - Test 4 (`test_raw_lineage_verification_uses_only_real_model_predicates`): A raw LineageEdge uses source_type `verified_lineage`, AuthorityRank.LINEAGE, and FactState.VERIFIED only when `LineageEdge.enabled is True`, both joined LineageNodes have `unresolved_flag is False`, edge confidence is `high`, the joined ScriptFile is enabled, and the joined ScriptFileVersion.version_no equals ScriptFile.current_version_no. Each failed predicate prevents verified state; no timestamp-age rule is applied to raw edges.
    - Test 5 (`test_mapping_and_scenario_lineage_use_persisted_status_fields`): Mapping facts use their registered mapping source types and actual approval/confirmation fields. A mapping/scenario lineage claim is verified only when its real `lineage_status == "verified"` and `lineage_last_verified_at is not None`; `stale`, `linked`, and `not_linked` remain non-verified, and no age threshold is invented.
  </behavior>
  <action>Per D-16 through D-20 and D-22, split `context_collectors.py` into explicit metadata/semantic, four-family mapping, evidence, raw-lineage, mapping/scenario-lineage, regulatory-knowledge, KnowledgeUnit retrieval, and historical functions. Every function accepts the PermissionService-authorized Project from Task 1 rather than a caller-selected institution id. Collect bounded identifier sets first, then issue project-scoped `IN`, join, or select-in reads for Target/Source/Mart metadata, ProductScenario, all four mapping families, MappingEvidenceReference, LineageNode/LineageEdge, RegulatoryKnowledgeItem, and HistoricalCaliberItem. Construct each ContextFact with a source_type already registered in `context_authority.py`, compute authority only with `authority_for_source(source_type)`, and use only 09-02 FactState members. Mapping facts use `source_to_mart_mapping` or `mart_to_ybt_mapping` with FactState.APPROVED only when `mapping_status == "approved"`; `scenario_business_mapping` uses FactState.CONFIRMED only when `business_confirm_status == "confirmed"`. RegulatoryKnowledgeItem is strictly `RegulatoryKnowledgeItem.project_id == authorized_project.id`; when projected it uses `source_type="regulatory_knowledge_item"`, AuthorityRank.REGULATORY from policy, and FactState.UNVERIFIED because the row has no governance status. Its regulatory reply and source document/location become bounded value/provenance, and absent support triggers `MISSING_EVIDENCE` rather than changing authority or state. Keep it separate from `HybridRetriever.search()`: the retriever alone owns KnowledgeUnit's established project/global/institution visibility; keep `KnowledgeUnit.enabled is True`, source_type `retrieved_knowledge`, AuthorityRank.RETRIEVED, FactState.RETRIEVED, RetrievalLog id, confidentiality, and source location. Use `retrieval_mode="keyword_only"` for the acceptance path and separately measure the existing vector/hybrid per-unit lookup boundary.

For raw SQL lineage, join aliased source/target LineageNode, LineageEdge, ScriptFileVersion, and ScriptFile in one project-bounded query. Emit source_type `verified_lineage` with FactState.VERIFIED only when the edge is enabled, both nodes are resolved, edge confidence is high, ScriptFile is enabled, and `ScriptFileVersion.version_no == ScriptFile.current_version_no`; otherwise keep the observation non-verified. Do not consult `lineage_status`, `lineage_last_verified_at`, or a time-age threshold for raw edges because those fields do not exist there. For SourceToMartMapping, MartToYbtMapping, and ScenarioTechnicalLineage lineage claims, use their persisted `lineage_status` and `lineage_last_verified_at`: only explicit `verified` plus a non-null verification timestamp can produce verified lineage; explicit `stale` emits `STALE_LINEAGE`, while `linked`/`not_linked` stay observed or unverified. ScenarioBusinessMapping has no lineage fields and must not be given them. Implement deterministic conflict/question codes `MISSING_CONFIRMED_SEMANTIC_BINDING`, `MISSING_CONFIRMED_SEMANTIC_VERSION`, `MISSING_SOURCE_MAPPING`, `MISSING_MART_TO_YBT_MAPPING`, `MISSING_LINEAGE`, `STALE_LINEAGE`, `MISSING_KNOWLEDGE`, `MISSING_EVIDENCE`, `HISTORICAL_ONLY_DEFINITION`, and `CONFLICTING_AUTHORITATIVE_FACTS`; sort by code, target, and source ids, and never insert PendingQuestion or synthetic spec-less rows. Extend the builder to assemble the contract sections without persistence.</action>
  <verify>
    <automated>cd backend; python -m pytest -q tests/test_regulatory_context_builder.py -k "mapping or lineage or evidence or history or knowledge or conflict or isolation" -x</automated>
  </verify>
  <acceptance_criteria>
    - Every local collector query constrains the authorized Project and batches identifiers/joins; RegulatoryKnowledgeItem never broadens beyond its project, while KnowledgeUnit visibility follows HybridRetriever's existing project/global/institution rules with confidentiality/source/retrieval-log provenance.
    - Every ContextFact passes `authority_for_source(fact.source_type) == fact.authority` and uses an existing FactState; no collector invents an authority or lifecycle vocabulary.
    - Raw and mapping/scenario lineage use their distinct real predicates, and only facts satisfying the applicable predicates are FactState.VERIFIED.
    - All locked missing/stale/conflict cases have deterministic machine-readable output, including exact `MISSING_SOURCE_MAPPING`, and no authoritative contradiction is silently selected.
    - A two-project/two-institution fixture returns only permitted project facts and leaves authoritative row counts, statuses, and timestamps unchanged.
  </acceptance_criteria>
  <done>The builder aggregates all existing fact families through authorized, batched collectors using only the 09-02 authority/state/source policy, real lineage predicates, deterministic gaps, and confidentiality-aware provenance without persistence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock candidate ranking, spec-less non-emission, confidentiality, and measured query performance</name>
  <files>backend/app/services/semantic/context_builder.py, backend/app/services/semantic/context_collectors.py, backend/app/services/semantic/context_conflicts.py, backend/tests/test_regulatory_context_builder.py</files>
  <read_first>backend/app/services/semantic/context_builder.py, backend/app/services/semantic/context_collectors.py, backend/app/services/semantic/context_conflicts.py, backend/app/schemas/regulatory_context.py, backend/app/services/retrieval/hybrid_retriever.py, backend/tests/test_regulatory_context_builder.py</read_first>
  <behavior>
    - Test 1: Source/Mart candidates sort by confirmed binding or mapping, exact code/name, semantic evidence, metadata keyword, historical mapping, lineage neighborhood, and retrieval evidence, with explicit stable type/id tie-breakers and capping only after ranking.
    - Test 2: The test-only spec-less metadata fixture contains exactly four unresolved unclassified planning records for CTX-01 through CTX-04, while a builder result and its serialized `open_questions` contain none of those synthetic ids; neither retrieved nor inferred candidates appear in confirmed facts.
    - Test 3: SQLAlchemy cursor instrumentation records the canonical builder collector query count, locks the measured acceptance budget, and remains bounded when equivalent rows are added; the keyword-only HybridRetriever path has no per-unit fallback, and a separate vector/hybrid probe records its known `KnowledgeUnit` db.get boundary rather than claiming zero db.get calls globally.
    - Test 4: Two builds compare equal for normalized semantic/regulatory/metadata/candidate/mapping/lineage/knowledge-evidence/historical/quality facts, section order, conflicts, and open questions after excluding only per-build built_at and RetrievalLog identifiers. Each build independently proves aware built_at metadata and consistent fact/provenance/build-metadata RetrievalLog references.
  </behavior>
  <action>Per D-05, D-06, D-18, D-19, D-20, D-22, and D-24, add explicit candidate tier constants and deterministic sort keys to `context_collectors.py`, then apply output caps only after scoring all collected candidates. Keep the four-requirement spec-less probe as test/plan metadata only and omit its synthetic ids from all runtime sections and open questions. Preserve confidential KnowledgeUnit source/citation fields and RetrievalLog identifiers while omitting unauthorized content; retrieved facts remain AuthorityRank.RETRIEVED/FactState.RETRIEVED and inferred facts use the registered inferred source policy without promotion. Attach a local `before_cursor_execute` listener around the acceptance build, measure the keyword-only path's initial query count, record the observed `ACCEPTANCE_QUERY_BUDGET`, and assert equivalent mapping/knowledge/lineage row growth stays bounded. Add a separate vector/hybrid probe for the existing per-unit KnowledgeUnit lookup boundary; do not claim the entire retriever is free of per-unit reads. Remove listeners in `finally` and assert no authoritative/context persistence writes.

For repeatability, add a test helper that produces a comparison projection containing the ordered domain sections, normalized ContextFact values/provenance, conflicts, and open questions, but replaces only `ContextBuildMetadata.built_at`, `ContextBuildMetadata.retrieval_log_ids`, and fact/provenance `retrieval_log_id` with explicit volatile markers. Compare that projection across two builds. Separately assert each built_at is timezone-aware and belongs to its build, each newly created RetrievalLog row has the authorized project_id, every retrieved fact's source/provenance ids agree, and `build_metadata.retrieval_log_ids` is the sorted set used by that build. Do not require built_at or newly allocated RetrievalLog primary keys to be equal across builds.</action>
  <verify>
    <automated>cd backend; python -m pytest -q tests/test_regulatory_context_builder.py -k "candidate or spec_less or query_count or confidential or deterministic" -x</automated>
  </verify>
  <acceptance_criteria>
    - Candidate ranking is explicit, stable, and complete before any cap; database natural order does not determine which candidate survives.
    - Exactly four CTX-01..CTX-04 spec-less planning metadata records remain `unclassified` and `unresolved`, none enter builder/API output, and rejected, deprecated, retrieved, inferred, flagged, and unverified values cannot become confirmed facts.
    - The measured builder query budget and bounded-growth assertion pass for the acceptance fixture, the known HybridRetriever db.get boundary is separately reported, listeners are cleaned up, and no authoritative mutation occurs.
    - Repeated builds match on normalized domain content and ordering while built_at and per-build RetrievalLog ids are accepted only after their independent scope, linkage, and metadata invariants pass.
  </acceptance_criteria>
  <done>RegulatoryContextBuilder is deterministic for stable domain content, explicitly volatile only for valid build/retrieval metadata, confidential, project-isolated, measured, and complete for candidate and gap behavior.</done>
</task>

</tasks>

<threat_model>
ASVS Level: L1. Block on: high severity.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API/service caller to builder | Untrusted project and target identifiers, mode, as_of, reporting label, and candidate limits enter orchestration. |
| builder to existing model queries | Collector SQL crosses into project, institution, mapping, lineage, evidence, historical, and knowledge rows. |
| knowledge retrieval to context | Retrieval results and RetrievalLog metadata cross into the bounded contract with confidentiality rules. |
| context output to downstream consumers | In-memory facts, candidates, conflicts, and questions are serialized without persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03-01 | Information disclosure | builder authorization boundary and collectors | high | mitigate | Require the PermissionService-authorized Project, reject request.project_id mismatch before collection, derive institution_id only from Project, keep RegulatoryKnowledgeItem project-only, and preserve HybridRetriever's existing KnowledgeUnit visibility in identical-code isolation tests. |
| T-09-03-02 | Tampering / repudiation | builder authority/state normalization | high | mitigate | Derive every fact authority through authority_for_source using the 09-02 registry, preserve FactState independently, prevent rejected/deprecated/retrieved/AI promotion, and emit conflicts instead of silent winners. |
| T-09-03-03 | Tampering | effective version and conflict detector | high | mitigate | Resolve inclusive confirmed periods through version_service, surface multiple matches as temporal conflicts, and test immutable/overlap behavior before projection. |
| T-09-03-04 | Information disclosure | regulatory and retrieved knowledge collectors | high | mitigate | Keep RegulatoryKnowledgeItem strictly project-scoped; reuse HybridRetriever project/global/institution visibility for KnowledgeUnit, carry confidentiality/source/retrieval-log provenance, and omit unauthorized content. |
| T-09-03-05 | Denial of service | candidate collection and SQL query fan-out | high | mitigate | Batch with IN/joins/select-in, rank before caps, measure cursor queries with a bounded-growth test, keep the builder on the keyword-only retriever path or batch-preload ids, and separately report the existing vector/hybrid db.get fallback instead of hiding it. |
</threat_model>

<verification>
- `cd backend; python -m pytest -q tests/test_regulatory_context_builder.py`
- `cd backend; python -m pytest -q tests/test_regulatory_context_contract.py tests/test_semantic_layer.py tests/test_regulatory_context_builder.py`
- `cd backend; python -m compileall -q app`
- Confirm no new Context persistence/cache/snapshot/lineage table or Phase 10/frontend/mapping-generator file appears in the implementation diff; every fact passes `authority_for_source`; RegulatoryKnowledgeItem is project-only while KnowledgeUnit uses HybridRetriever visibility; raw lineage and mapping/scenario lineage each use only their real predicates; and the four spec-less records appear only in test/plan metadata.
</verification>

<success_criteria>
- The acceptance target produces a stable Pydantic RegulatoryContext containing semantic, regulatory, candidate, mapping, lineage, knowledge/evidence, historical, quality, conflict, open-question, and build metadata projections.
- Authority, state, provenance, confidentiality, source and retrieval-log information remain machine-readable, every authority matches the 09-02 source registry, and the authorized Project boundary prevents project/institution spoofing.
- Missing, stale, contradictory, historical-only, and evidence-insufficient states produce stable domain conflicts/open questions, while four spec-less CTX metadata records are explicitly excluded from product output.
- Candidate ranking and builder collector query count are measured and deterministic; repeated domain output is stable while built_at and new RetrievalLog ids are validated as volatile metadata; the HybridRetriever vector/hybrid db.get boundary is separately qualified; and no context persistence is introduced.
</success_criteria>

## Artifacts this phase produces

- `backend/app/services/semantic/context_builder.py`: `RegulatoryContextBuilder.build` and orchestration helpers.
- `backend/app/services/semantic/context_collectors.py`: separated metadata/semantic, mapping, evidence, lineage, knowledge/retrieval, historical collectors and candidate tier keys.
- `backend/app/services/semantic/context_conflicts.py`: conflict catalog, `detect_conflicts`, and `build_open_questions` for real source gaps; no synthetic spec-less runtime generator.
- `backend/app/services/semantic/__init__.py`: context service exports while preserving Phase 8 symbols.
- `backend/tests/test_semantic_layer.py`: additive version-route regression that derives as_of from the listed initial version's effective_from.
- `backend/tests/test_regulatory_context_builder.py`: acceptance fixture, deterministic gaps/ranking, four mapping-family authority/state predicates, isolation/confidentiality, projection-only, spec-less non-emission, and measured query-count/retriever-boundary tests.

<output>Create `.planning/phases/09-regulatory-context/09-03-SUMMARY.md` when done.</output>
