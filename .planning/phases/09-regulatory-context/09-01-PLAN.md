---
phase: 09-regulatory-context
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/semantic.py
  - backend/app/models/__init__.py
  - backend/app/schemas/semantic.py
  - backend/app/services/semantic/__init__.py
  - backend/app/services/semantic/binding_service.py
  - backend/app/services/semantic/graph_service.py
  - backend/app/services/semantic/resolver.py
  - backend/app/services/semantic/status_policy.py
  - backend/app/services/semantic/entity_adapter.py
  - backend/app/services/semantic/version_service.py
  - backend/app/services/governance/workflow.py
  - backend/app/api/semantic.py
  - backend/alembic/versions/202608200016_semantic_concept_versions.py
  - backend/tests/test_semantic_layer.py
  - backend/tests/test_governance.py
  - backend/tests/test_semantic_migration.py
autonomous: true
requirements: [CTX-02, CTX-04]
user_setup: []
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-01: Phase 9 hardening and temporal semantic versions are delivered without changing the Phase 10, frontend, generator, quality, impact, graph-infrastructure, or product multi-agent surface."
    - "D-02: Trusted semantic entity, graph, path, resolver, and effective-version reads use confirmed only; candidate mode is explicit and includes only confirmed, draft, and ai_suggested."
    - "D-03: Concept, binding, relation, and version lifecycle predicates are supplied by one status-policy module consumed by graph, resolver, version service, and ContextBuilder's future seam."
    - "D-04: Every allow-listed binding entity is described by an explicit SemanticEntityAdapter descriptor rather than an ORM reflection chain."
    - "D-05: Resolver ordering is deterministic with confirmed binding before exact code, canonical name, alias, regulatory text, and metadata or definition text."
    - "D-06: Resolver candidates carry match reason, bounded evidence, and provenance and remain ai_suggested without any confirmation side effect."
    - "D-07: SemanticConcept remains the project-scoped stable identity and SemanticConceptVersion owns temporal meaning without a duplicated concept identity constraint."
    - "D-08: Version rows are canonical content and legacy Concept fields are a same-transaction compatibility projection, never a second editable truth; Concept create, PATCH, and status routes all delegate their writes to version_service."
    - "D-09: Effective dates are inclusive, and a confirmed version is selected only when its interval contains as_of."
    - "D-10: Draft and ai_suggested versions may coexist, confirmed versions are immutable, and rejected or deprecated versions are never selected; draft/AI PATCH updates the canonical version while confirmed PATCH returns 409 and requires a new version."
    - "D-11: Revision 202608200016 bootstraps exactly one version_no=1 per legacy Concept regardless of the old edit counter."
    - "D-12: Bindings and relations remain identity-level; no semantic_concept_version_id is added because current data and tests provide no version-limited binding evidence."
    - "D-21: Phase 8 Concept, Binding, Relation, status, graph, and resolver endpoints remain available while version fields and routes are additive."
    - "Governance finalization treats SemanticConceptVersion as a semantic workflow target and audits canonical version transitions; the legacy Concept target remains a compatibility delegate."
  artifacts:
    - path: backend/app/services/semantic/status_policy.py
      provides: "Shared trusted, candidate, and audit-only lifecycle visibility API"
    - path: backend/app/services/semantic/entity_adapter.py
      provides: "Explicit twelve-type SemanticEntityAdapter descriptor registry"
    - path: backend/app/services/semantic/version_service.py
      provides: "Transactional Concept create/PATCH/status delegation, temporal version creation, effective selection, governed transition, and legacy projection synchronization"
    - path: backend/app/services/governance/workflow.py
      provides: "SemanticConceptVersion workflow target registration and finalize/audit delegation through version_service"
    - path: backend/app/models/semantic.py
      provides: "SemanticConceptVersion mapped model with temporal constraints and indexes"
    - path: backend/alembic/versions/202608200016_semantic_concept_versions.py
      provides: "Additive schema and bootstrap data migration after 202608200015"
    - path: backend/app/api/semantic.py
      provides: "Backward-compatible semantic CRUD plus additive version endpoints"
    - path: backend/tests/test_semantic_layer.py
      provides: "Policy, adapter, bounded resolver evidence/provenance, version, graph, projection, and API compatibility coverage"
    - path: backend/tests/test_governance.py
      provides: "Semantic Concept/ConceptVersion workflow target, finalization, audit, and compatibility coverage"
    - path: backend/tests/test_semantic_migration.py
      provides: "SQLite migration lifecycle, bootstrap, downgrade, and runtime-model-free checks"
  key_links:
    - from: backend/app/api/semantic.py
      to: backend/app/services/semantic/resolver.py
      via: "POST /api/projects/{project_id}/semantic-resolve passes explicit visibility mode and returns provenance-bearing candidates"
    - from: backend/app/services/semantic/resolver.py
      to: backend/app/services/semantic/entity_adapter.py
      via: "Resolver ranks only the stable descriptor returned for the twelve binding entity types"
    - from: backend/app/services/semantic/graph_service.py
      to: backend/app/services/semantic/status_policy.py
      via: "trusted default and explicit candidate mode use the centralized status predicate"
    - from: backend/app/services/semantic/version_service.py
      to: backend/app/models/semantic.py
      via: "effective-date resolution and transitions operate on SemanticConceptVersion rows"
    - from: backend/app/api/semantic.py
      to: backend/app/services/semantic/version_service.py
      via: "Concept create/PATCH/status and all SemanticConceptVersion routes delegate canonical writes, immutable checks, projection sync, and effective selection to version_service"
    - from: backend/app/services/governance/workflow.py
      to: backend/app/services/semantic/version_service.py
      via: "semantic_concept and semantic_concept_version targets finalize through canonical version transitions and audit in the existing transaction"
    - from: backend/alembic/versions/202608200016_semantic_concept_versions.py
      to: backend/app/models/semantic.py
      via: "explicit Alembic columns and constraints match the runtime version model without importing app models"
  prohibitions:
    - "No descriptor-less entity extraction, ORM getattr accretion, whole-row dump, or unbounded evidence payload."
    - "No flagged/unverified, rejected, deprecated, retrieved, or AI candidate is serialized or selected as confirmed truth."
    - "No modification to Phase 10 generators, frontend routes, or generator-owned instruction/output files."
    - "No RegulatoryContext persistence, cache, snapshot, copied mapping, or replicated lineage is introduced."
    - "No natural-order limit(50) candidate selection is used; all ranking has explicit tiers and stable tie-breakers before any cap."
    - "No Phase 8 API is deleted or renamed, and no optional semantic concept version foreign key is added to Binding or Relation."

---

<objective>
Harden the Phase 8 semantic read boundary and establish the temporal SemanticConceptVersion seam before any ContextBuilder work.

Purpose: Make trusted semantic facts safe and date-effective while retaining stable Concept identity, identity-level bindings and relations, and additive Phase 8 APIs.
Output: Central policy and twelve-type adapter, deterministic resolver with bounded evidence/provenance, temporal model/service/schema/API, governance workflow integration, reversible 202608200016 bootstrap migration, and focused semantic/governance/migration proof.

Execution boundary: This plan is standalone and independently summarizable. After its summary is produced, stop implementation and wait for an explicit user request before executing 09-02.
Execution gates: Task 1 must pass its resolver/policy tests before Task 2 starts; Task 2 must pass temporal, graph, governance, and projection tests before Task 3 starts. These are serial automated gates inside this single plan, not human approval checkpoints.

<atomic_slice>
09-01 intentionally remains one implementation plan. Rejected-contamination hardening, the explicit EntityAdapter/Resolver boundary, and real transactional temporal versions must ship as one publishable semantic slice before ContextBuilder work; splitting them would expose an unsafe policy-only or model-only intermediate state. Risk is controlled by three serial automated checkpoints, explicit task symbol ownership, and immediate stop on any failed checkpoint.
</atomic_slice>

<serial_checkpoints>
- Checkpoint 1 after Task 1: resolver/policy/adapters focused tests pass; otherwise stop before temporal work.
- Checkpoint 2 after Task 2: temporal, graph, projection, governance target/finalize/audit tests pass; otherwise stop before API/migration wiring.
- Checkpoint 3 after Task 3: compatibility/API, migration lifecycle, static-route-order, and focused semantic tests pass; otherwise stop before 09-02.
All three checkpoints are executable test gates, not additional human approvals.
</serial_checkpoints>

<file_ownership>
- Task 1 owns `status_policy.py`, `entity_adapter.py`, resolver logic, and the resolver-specific symbols/tests in `schemas/semantic.py` and `test_semantic_layer.py`.
- Task 2 owns `models/semantic.py`, `models/__init__.py`, `version_service.py`, `binding_service.py`, `graph_service.py`, `governance/workflow.py`, and their temporal/graph/governance test cases; it does not own API route delegate wiring.
- Task 3 owns additive version schemas/exports, all `api/semantic.py` Concept/version/graph/resolver delegate wiring, migration 016, and migration/API compatibility tests. Shared files are serial handoffs, never parallel edits.
</file_ownership>

<resolved_decisions>
- Adopt the resolved research choices: bootstrap from legacy `created_at` calendar date with fixed `2026-08-20` fallback, identity-level Binding/Relation, static `/versions/effective` before dynamic `/{version_id}`, inclusive reporting semantics, and PostgreSQL row-lock/SQLite serialized overlap qualification. No additional planning Open Question is introduced here.
</resolved_decisions>
</objective>

<execution_context>
@C:/Users/李儒伟/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/李儒伟/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-regulatory-context/09-CONTEXT.md
@.planning/phases/09-regulatory-context/09-RESEARCH.md
@.planning/phases/09-regulatory-context/09-PATTERNS.md
@.planning/phases/08-semantic-foundation/08-03-SUMMARY.md
@.planning/phases/08-semantic-foundation/08-VERIFICATION.md
@backend/app/models/semantic.py
@backend/app/models/entities.py
@backend/app/schemas/semantic.py
@backend/app/services/semantic/binding_service.py
@backend/app/services/semantic/graph_service.py
@backend/app/services/semantic/resolver.py
@backend/app/services/governance/workflow.py
@backend/app/api/semantic.py
@backend/alembic/versions/202608200015_regulatory_semantic_layer.py
@backend/tests/test_semantic_layer.py
@backend/tests/test_governance.py
@backend/tests/test_semantic_migration.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace confirmed semantic resolution through policy, adapters, and the existing API</name>
  <files>backend/app/services/semantic/status_policy.py, backend/app/services/semantic/entity_adapter.py, backend/app/services/semantic/resolver.py, backend/app/schemas/semantic.py, backend/tests/test_semantic_layer.py</files>
  <read_first>backend/app/services/semantic/binding_service.py, backend/app/models/entities.py, backend/app/models/semantic.py, backend/app/schemas/semantic.py, backend/app/api/semantic.py, backend/tests/test_semantic_layer.py</read_first>
  <behavior>
    - Test 1: POST /api/projects/{project_id}/semantic-resolve returns repeatable candidates whose confirmed binding tier precedes exact code, canonical name, alias, regulatory text, and metadata text.
    - Test 2: The twelve allow-listed entity types each produce a descriptor containing stable identity, code/name, semantic text, bounded metadata, and source references.
    - Test 3: Trusted mode excludes rejected and deprecated concepts and bindings; explicit candidate mode admits only confirmed, draft, and ai_suggested rows, and every candidate remains ai_suggested.
  </behavior>
  <action>Per D-02, D-03, D-04, D-05, and D-06, create `SemanticVisibilityMode` and the functions `trusted_statuses()`, `candidate_statuses()`, `audit_only_statuses()`, `status_predicate(column, mode)`, and `is_visible(status, mode)` in `status_policy.py`; candidate mode retains confirmed rows and adds only draft/ai_suggested, never rejected/deprecated. Create a frozen `SemanticEntityDescriptor` and `SemanticEntityAdapter.describe(db, project_id, entity_type, entity_id)` with explicit handlers for exactly `target_table`, `target_field`, `mart_table`, `mart_field`, `source_table`, `source_field`, `scenario`, `knowledge_unit`, `source_to_mart_mapping`, `mart_to_ybt_mapping`, `scenario_business_mapping`, and `scenario_technical_lineage`; extract the real regulatory, mapping-rule, knowledge, lineage, and scenario fields named in `entities.py`, enforce the existing project guard, and return bounded source references. Refactor `SemanticResolver.resolve()` and `_match()` to consume that descriptor, use policy-filtered concepts and confirmed bindings, rank with a stable tier/reason/id key in the locked order, and attach `list[SemanticMatchEvidence]` capped at three entries (each with `match_reason`, `matched_field`, bounded excerpt, source type/id, and score) plus `SemanticCandidateProvenance` with derived project/institution, resolver rule, source/evidence ids, and bounded retrieval metadata; keep lifecycle status as `ai_suggested`. Extend `SemanticResolveRequest` with an explicit trusted/candidate mode defaulting to trusted and extend `SemanticResolveCandidate` with these typed bounded shapes without removing existing fields or the `/semantic-resolve` route. Follow the explicit adapter and resolver projection analogs assigned in `09-PATTERNS.md` for `binding_service.py` and `resolver.py`.</action>
  <verify>
    <automated>cd backend; python -m pytest -q tests/test_semantic_layer.py -k "resolver or adapter or policy" -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/services/semantic/status_policy.py` is the only source of trusted/candidate/audit status tuples used by resolver-facing code.
    - All twelve adapter handlers expose real domain text and never serialize an ORM `__dict__`.
    - Confirmed binding evidence ranks above exact text tiers, repeated calls have identical JSON, and no candidate changes a Concept or Binding status.
    - Rejected and deprecated rows are absent from trusted and candidate resolver results, and evidence/provenance cannot contain an ORM row or unbounded dictionary.
  </acceptance_criteria>
  <precondition>Existing Phase 8 resolver tests and the twelve-type binding allow-list are present; this task's focused resolver/policy command passes before Task 2 begins.</precondition>
  <done>The existing authenticated resolver endpoint has one real, tested path through the shared policy and explicit twelve-type descriptor registry with deterministic provenance-bearing ai_suggested output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add temporal versions, transactional Concept delegation, governance finalization, and complete graph policy coverage</name>
  <files>backend/app/models/semantic.py, backend/app/models/__init__.py, backend/app/services/semantic/version_service.py, backend/app/services/semantic/binding_service.py, backend/app/services/semantic/graph_service.py, backend/app/services/governance/workflow.py, backend/tests/test_semantic_layer.py, backend/tests/test_governance.py</files>
  <read_first>backend/app/models/semantic.py, backend/app/models/deliverables.py, backend/app/services/semantic/status_policy.py, backend/app/services/semantic/binding_service.py, backend/app/services/semantic/graph_service.py, backend/app/services/governance/workflow.py, backend/tests/test_semantic_layer.py, backend/tests/test_governance.py</read_first>
  <behavior>
    - Test 1: A Concept with confirmed v1 effective in 2026 and confirmed v2 effective from 2027 resolves v1 and v2 at inclusive boundary dates.
    - Test 2: Confirmed interval overlap and confirmed-version in-place edits fail atomically, while draft and ai_suggested rows can coexist.
    - Test 3: `create_concept_with_initial_version()`, `patch_concept_via_version_service()`, and `transition_concept_status()` create/update/transition the canonical version and legacy projection atomically; confirmed patch semantics return the machine-readable 409 and require a new version.
    - Test 4: Governance workflow can target SemanticConceptVersion, finalize through version_service, record the canonical audit, and preserve the legacy Concept target as a compatibility delegate.
    - Test 5: Trusted graph/entity/path service reads exclude rejected and deprecated Concept, Binding, and Relation rows; explicit candidate mode keeps confirmed plus draft/ai_suggested only, and existing bindings and relations remain identity-level.
  </behavior>
  <action>Per D-07 through D-12 and D-21, add `SemanticConceptVersion` beside `SemanticConcept` with `semantic_concept_id`, `project_id`, `institution_id`, `version_no`, canonical meaning fields, `provenance_json`, lifecycle and confirmation fields, inclusive `effective_from` and nullable `effective_to`, timestamps, a unique `(semantic_concept_id, version_no)` constraint, the portable date-order check, and project/concept/status/effective-date indexes; keep the existing Concept identity constraint and do not add `semantic_concept_version_id` to `SemanticBinding` or `SemanticRelation`. Export the model through `app.models`. Implement `create_concept_with_initial_version()`, `patch_concept_via_version_service()`, `create_concept_version()`, `resolve_effective_version()`, `transition_concept_status()`, `transition_version_status()`, `_assert_confirmed_interval_available()`, and `sync_legacy_concept_projection()` in `version_service.py` as the only Concept/Version write path: Concept create flushes the stable identity and creates canonical version_no=1 in the same transaction; draft/ai_suggested PATCH changes canonical version content and synchronizes legacy Concept fields; confirmed PATCH raises the machine-readable immutable 409 and requires `create_concept_version()`; Concept status and version status handlers call the same transition functions, and legacy status/version fields are only same-transaction projections. For PostgreSQL, lock the stable SemanticConcept row with `SELECT ... FOR UPDATE` before the portable inclusive overlap query; for SQLite run the identical overlap check inside its serialized write transaction; allow candidate overlap, exclude rejected/deprecated versions, and qualify concurrent confirmation rejection in staging. Update `graph_service.py` so `concepts()`, `_concept()`, `entity_concepts()`, `traverse()` root/neighbors/upstream/downstream, `shortest_path()`/`_adjacent()` apply the explicit mode and filter Concept, SemanticBinding, and SemanticRelation statuses together. Trusted mode is confirmed only; candidate mode is confirmed plus draft/ai_suggested, where draft/ai_suggested are the only non-trusted additions; rejected/deprecated never reach either business-fact service path. Add `SemanticConceptVersion` to `TARGET_MODELS` and `SEMANTIC_TARGET_TYPES` in `backend/app/services/governance/workflow.py`; make `_finalize_semantic_target()` delegate `semantic_concept` and `semantic_concept_version` confirmation to version_service, synchronize the legacy projection, and record the canonical audit without direct Concept/Version status assignment. Follow the temporal version-row and governance workflow analogs in `09-PATTERNS.md` (`deliverables.py` version rows and existing `workflow.py` finalize/audit flow).</action>
  <verify>
    <automated>cd backend; python -m pytest -q tests/test_semantic_layer.py tests/test_governance.py -k "version or overlap or projection or graph or rejected or semantic" -x</automated>
  </verify>
  <acceptance_criteria>
    - `resolve_effective_version(concept_id, as_of)` returns zero or one confirmed row using inclusive interval predicates and never orders away an overlap.
    - A confirmed version cannot be patched; a changed meaning is represented by a new version, and a failed overlap leaves all rows and projections unchanged.
    - Legacy Concept fields and its compatibility version value are written from the canonical version within the same transaction.
    - Graph `concepts()`, `_concept()`, `entity_concepts()`, root/neighbors/upstream/downstream, shortest path, and `_adjacent()` apply one mode policy to Concept, Binding, and Relation; candidate mode adds only draft/ai_suggested to confirmed and never admits rejected/deprecated.
    - The version_service Concept create/patch/status functions, version status transition, and governance finalization commit canonical version content and legacy projection atomically; API delegate wiring is reserved for Task 3.
  </acceptance_criteria>
  <precondition>Task 1 resolver/policy tests are green; this task must pass its focused temporal, graph, projection, and governance tests before Task 3 begins.</precondition>
  <done>The runtime model and services provide governed temporal meaning with shared lifecycle filtering while Phase 8 graph, binding, and relation identity contracts remain intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose additive version API and ship the reversible 202608200016 bootstrap migration</name>
  <reversibility rating="costly">The new version table is a data migration, but downgrading to 202608200015 removes only this table and can restore the pre-version schema.</reversibility>
  <files>backend/app/schemas/semantic.py, backend/app/services/semantic/__init__.py, backend/app/api/semantic.py, backend/alembic/versions/202608200016_semantic_concept_versions.py, backend/tests/test_semantic_migration.py</files>
  <read_first>backend/app/schemas/semantic.py, backend/app/api/semantic.py, backend/app/services/semantic/version_service.py, backend/app/services/semantic/__init__.py, backend/alembic/versions/202608200015_regulatory_semantic_layer.py, backend/tests/test_semantic_migration.py</read_first>
  <behavior>
    - Test 1: Version list/create/detail/effective/status routes are project-scoped, additive, permission checked, and return deterministic 409 responses for temporal conflicts; the static `/versions/effective` route is declared before dynamic `/{version_id}` and resolves the literal segment.
    - Test 2: Legacy Concept create/PATCH/status HTTP calls preserve their Phase 8 paths and response fields while delegating all writes to version_service, including atomic Concept plus v1 creation and confirmed PATCH 409 behavior.
    - Test 3: Existing graph root/neighbor/upstream/downstream/entity/path/resolver API handlers pass explicit trusted/candidate mode to the hardened graph/resolver services while preserving default response fields.
    - Test 4: A 015 database with legacy Concepts receives one version_no=1 bootstrap row per Concept, including a legacy Concept whose edit counter is greater than one.
    - Test 5: Downgrade from 016 to 015 removes only semantic_concept_versions and preserves semantic concepts, bindings, relations, embedding_index_versions, and business data.
  </behavior>
  <action>Per D-08, D-09, D-11, and D-21, add strict `SemanticConceptVersionCreate`, `SemanticConceptVersionRead`, and `SemanticConceptVersionStatusTransition` schemas with bounded fields, inclusive date validation, and additive effective-version fields on `SemanticConceptRead`. Export the service symbols without removing existing semantic exports. Add `GET/POST /api/projects/{project_id}/semantic-concepts/{concept_id}/versions`, `GET /api/projects/{project_id}/semantic-concepts/{concept_id}/versions/effective`, `GET /api/projects/{project_id}/semantic-concepts/{concept_id}/versions/{version_id}`, and `POST /api/projects/{project_id}/semantic-concepts/{concept_id}/versions/{version_id}/status` in that exact declaration order so the static effective route precedes the dynamic id route; use `PermissionService`, audit version transitions, return 409 for overlap or immutable-confirmed violations, and keep every Phase 8 path and response field. Make Concept POST, PATCH, and status routes call `create_concept_with_initial_version()`, `patch_concept_via_version_service()`, and `transition_concept_status()` respectively; draft/ai_suggested writes update canonical version content and the same-transaction legacy projection, while confirmed PATCH returns 409 and requires a new version. In the same API handoff, wire the existing root/neighbor/upstream/downstream/entity/path/resolver handlers to pass explicit trusted/candidate mode into `SemanticGraphService`/`SemanticResolver`, preserving their default modes and response fields. Create revision `202608200016_semantic_concept_versions.py` with `down_revision = "202608200015"`, explicit Alembic `sa.Column`/constraint/index operations, no runtime-model import, and exactly one `version_no=1` row per existing Concept. Set bootstrap `effective_from` to the legacy `created_at` calendar date; when that value is absent use fixed date `2026-08-20`; set `source_type` to `bootstrap` and `provenance_json` to a bootstrap record containing the legacy Concept id and fallback decision. Preserve the old Concept version counter as non-historical compatibility data, never manufacture extra history, make a retry non-duplicating, and make downgrade remove only the new table. Extend migration and semantic API tests for 015-to-016, 016-to-015-to-016, populated bootstrap, date fallback, constraints/indexes, formal index survival, static-before-dynamic route order, Concept compatibility delegation, graph/resolver mode propagation, and runtime-model-free source inspection. Follow the explicit Alembic 015 and existing semantic router/schema analogs in `09-PATTERNS.md`.</action>
  <verify>
    <automated>cd backend; python -m pytest -q tests/test_semantic_layer.py tests/test_semantic_migration.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -m alembic heads` reports `202608200016` as the single head and the migration has `down_revision` `202608200015`.
    - Populated legacy upgrade creates exactly one canonical bootstrap version per Concept, uses version_no=1 even when Concept.version is greater than one, and records the created_at-date or fixed 2026-08-20 fallback in bootstrap provenance.
    - Downgrade to 202608200015 drops only semantic_concept_versions; Phase 8 tables, formal embedding index tables, and unrelated business data remain present.
    - Existing semantic CRUD, status, graph, path, and resolver endpoints continue to respond with their prior fields while version routes are additive and all canonical writes delegate to version_service.
    - Existing graph/root/neighbor/path/resolver API handlers pass the explicit mode without moving API wiring into the temporal service task.
  </acceptance_criteria>
  <precondition>Task 2 temporal, graph, projection, and governance gates are green; this final task is the only migration/API expansion gate in 09-01.</precondition>
  <done>Temporal version schema, transactional Concept delegation, governance target integration, additive API with static route precedence, and reversible bootstrap lifecycle are tested on SQLite with explicit PostgreSQL-portable migration operations.</done>
</task>

</tasks>

<threat_model>
ASVS Level: L1. Block on: high severity.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller to semantic API | Untrusted project, entity, mode, date, and version identifiers enter authenticated routes. |
| API/service to ORM | Service predicates and transition operations cross into project/institution data. |
| migration to stored database | Legacy Concept rows are transformed into canonical version rows. |
| semantic resolver to descriptor | Existing ORM entities become bounded, non-ORM candidate evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01-01 | Information disclosure | semantic API, BindingService, GraphService | high | mitigate | Require project permission, derive institution from Project, include project predicates in every semantic query, and run two-project/two-institution isolation tests. |
| T-09-01-02 | Tampering / repudiation | status_policy, resolver, governance transitions | high | mitigate | Centralize trusted/candidate policy, keep rejected/deprecated out of trusted paths, preserve ai_suggested candidate status, and audit human transitions. |
| T-09-01-03 | Tampering | SemanticConceptVersion and version_service | high | mitigate | Validate inclusive dates, reject confirmed interval overlap in the transaction, reject confirmed-row edits, and test atomic rollback on conflicts. |
| T-09-01-04 | Information disclosure | entity_adapter and SemanticResolveCandidate | medium | mitigate | Use explicit bounded descriptors, source references, provenance, and tests that reject ORM dumps and reflection-based field discovery. |
| T-09-01-05 | Denial of service | graph traversal and resolver candidate collection | medium | mitigate | Preserve depth/node/limit bounds, rank with explicit stable keys, cap after ranking, and test repeated deterministic bounded queries. |
</threat_model>

<verification>
- `cd backend; python -m pytest -q tests/test_semantic_layer.py tests/test_governance.py tests/test_semantic_migration.py`
- `cd backend; python -m alembic heads`
- `cd backend; python -m compileall -q app`
- Confirm the focused tests cover Concept create/PATCH/status delegation, SemanticConceptVersion workflow target/finalize/audit, every graph root/neighbor/path function, static-before-dynamic effective route declaration, and PostgreSQL row-lock/SQLite serialized overlap semantics.
- Confirm the diff for this plan contains only the listed semantic hardening/version/migration/test files; no ContextBuilder, generator, or frontend file is touched.
</verification>

<success_criteria>
- Trusted semantic reads and explicit candidate reads have one centralized, tested lifecycle policy.
- All twelve binding entity types expose deterministic descriptors and resolver provenance without auto-confirmation; candidate evidence/provenance are bounded typed structures.
- Temporal version selection, governance, projection synchronization, and inclusive interval constraints are executable on SQLite and expressed with PostgreSQL-portable Alembic operations.
- Concept create/PATCH/status and governance finalization have one transactional version_service path, while Phase 8 routes and fields remain compatible.
- Graph concepts, root/neighbor/path, entity, and binding/relation reads share the same trusted/candidate status policy.
- Revision 202608200016 bootstraps and downgrades safely without deleting Phase 8 artifacts.
- Phase 8 APIs remain additive-compatible, and implementation stops for user direction before 09-02.
</success_criteria>

## Artifacts this phase produces

- `backend/app/services/semantic/status_policy.py`: `SemanticVisibilityMode`, `trusted_statuses`, `candidate_statuses`, `audit_only_statuses`, `status_predicate`, `is_visible`.
- `backend/app/services/semantic/entity_adapter.py`: `SemanticEntityDescriptor`, `SemanticEntityAdapter`, and twelve explicit entity adapters.
- `backend/app/services/semantic/version_service.py`: `create_concept_version`, `resolve_effective_version`, `transition_version_status`, `_assert_confirmed_interval_available`, `sync_legacy_concept_projection`.
- `backend/app/models/semantic.py`: `SemanticConceptVersion` and temporal constraints/indexes.
- `backend/app/schemas/semantic.py`: additive version schemas and provenance-bearing resolver schemas.
- `backend/app/api/semantic.py`: additive version list/create/detail/effective/status routes and compatibility delegation.
- `backend/alembic/versions/202608200016_semantic_concept_versions.py`: reversible bootstrap migration.
- `backend/tests/test_semantic_layer.py`: hardening, adapter, resolver, temporal, policy, projection, and compatibility tests.
- `backend/tests/test_semantic_migration.py`: 016 SQLite lifecycle and bootstrap tests.

<output>Create `.planning/phases/09-regulatory-context/09-01-SUMMARY.md` when done, then stop and wait for explicit user direction before 09-02.</output>
