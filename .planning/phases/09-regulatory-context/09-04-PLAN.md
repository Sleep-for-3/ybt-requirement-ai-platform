---
phase: 09-regulatory-context
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
files_modified:
  - backend/app/api/regulatory_context.py
  - backend/app/api/semantic.py
  - backend/app/main.py
  - backend/tests/test_regulatory_context_api.py
  - backend/tests/test_semantic_migration.py
  - .planning/phases/09-regulatory-context/09-04-POSTGRES-QUALIFICATION.md
autonomous: true
requirements: [CTX-01, CTX-02, CTX-03, CTX-04]
user_setup: []
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-01: Phase 9 API and qualification work remains backend-only and leaves Phase 10 generators, frontend routes, SQL generation, quality, impact, graph infrastructure, and product multi-agent features untouched."
    - "D-20: Context API reads enforce project membership and Project.institution ownership, and confidential knowledge never crosses an authorized scope."
    - "D-21: Existing Concept, Binding, Relation, status, graph, path, and resolver endpoints remain available with their prior response fields; version and context routes are additive."
    - "D-22: API builds use the measured batched builder path and preserve the query-count/N+1 qualification without adding a cache."
    - "D-23: The context/debug endpoint is read-only with respect to authoritative facts and returns a typed RegulatoryContext."
    - "D-24: The project/date-sensitive acceptance target and its deterministic missing/conflict/open-question output are reachable through the project-aware API."
    - "CTX-01 through CTX-04: The API exposes stable scope/target inputs, separate authority/state, aggregated projection sections, and deterministic domain gaps; the four unclassified/unresolved spec-less records remain planning metadata and are absent from product output."
  artifacts:
    - path: backend/app/api/regulatory_context.py
      provides: "Authenticated project-scoped read-only RegulatoryContext debug/build endpoint"
    - path: backend/app/api/semantic.py
      provides: "Existing semantic routes with explicit graph/resolver visibility mode propagation and additive compatibility fields"
    - path: backend/app/main.py
      provides: "Secured additive regulatory_context router registration"
    - path: backend/tests/test_regulatory_context_api.py
      provides: "Context endpoint auth, isolation, deterministic response, gap, candidate, and read-only tests"
    - path: .planning/phases/09-regulatory-context/09-04-POSTGRES-QUALIFICATION.md
      provides: "SQLite lifecycle, PostgreSQL offline SQL, and staging migration/permission/performance qualification record"
  key_links:
    - from: backend/app/main.py
      to: backend/app/api/regulatory_context.py
      via: "router is included under settings.api_prefix with the existing secured project dependency"
    - from: backend/app/api/regulatory_context.py
      to: backend/app/services/auth/permission_service.py
      via: "project.view authorization and Project-derived institution scope precede ContextBuilder.build"
    - from: backend/app/api/regulatory_context.py
      to: backend/app/services/semantic/context_builder.py
      via: "typed query parameters become one read-only RegulatoryContext projection"
    - from: backend/app/api/semantic.py
      to: backend/app/services/semantic/status_policy.py
      via: "existing graph/entity/resolver routes preserve trusted defaults and expose candidate mode explicitly"
  prohibitions:
    - "No descriptor-less or ORM-dump response, raw context persistence, cache, snapshot, copied mapping, or replicated lineage is exposed by the API."
    - "No flagged/unverified, rejected, deprecated, retrieved, or AI candidate is returned as confirmed truth."
    - "No modification to Phase 10 generators, frontend routes, mapping generators, or generator instruction/output contracts."
    - "No natural-order limit(50) candidate behavior is introduced at the route or collector boundary."
    - "No Phase 8 API is deleted or renamed; old semantic routes and fields remain regression-tested."
    - "No external API, service, package, graph database, SQL Generator, DataQualityExpectation, impact propagation, or product Multi-Agent integration is introduced."

---

<objective>
Expose and qualify the projection through an additive project-aware read/debug API while preserving all Phase 8 semantic compatibility.

Purpose: Make CTX-01 through CTX-04 callable by authorized users, prove isolation and read-only behavior at the HTTP boundary, and record SQLite/PostgreSQL qualification honestly where no local PostgreSQL service is available.
Output: Secured RegulatoryContext router and registration, explicit mode propagation in existing semantic routes, API/isolation/regression tests, and offline/staging qualification documentation.
</objective>

<assumptions>
- The exact route shape is discretionary under D-23; this plan chooses `GET /api/projects/{project_id}/regulatory-context` with bounded query parameters because the request is read-only and the identifiers fit the existing project-path style.
- No PostgreSQL server is available locally. SQLite online lifecycle and direct/offline PostgreSQL compilation are executable gates; the qualification document must mark live staging checks as required evidence rather than claim them completed locally.
- The existing HybridRetriever RetrievalLog behavior is provenance, not Context persistence; API tests compare authoritative rows before and after the request and allow only the established retrieval-log side effect when retrieval is exercised.
- The API must not serialize the four planning-only spec-less edge metadata records; endpoint tests verify their synthetic ids are absent while real domain conflicts/open questions remain typed.
- Research Open Questions are resolved for planning: use the selected route shape with static `/versions/effective` before dynamic `/{version_id}`, the PostgreSQL row-lock/SQLite qualification split, and the documented 247-versus-255 baseline explanation without reopening those choices.
- Phase 8's 247 passes plus two unchanged Windows failures is the pre-semantic baseline; 255 passed plus the same two failures is the post-Phase-8 total after eight semantic tests were added. Phase 9 compares test identities and failure signatures, not raw totals, and reports any new failure.

</assumptions>

<execution_context>
@C:/Users/李儒伟/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/李儒伟/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-regulatory-context/09-CONTEXT.md
@.planning/phases/09-regulatory-context/09-RESEARCH.md
@.planning/phases/09-regulatory-context/09-PATTERNS.md
@.planning/phases/09-regulatory-context/09-01-SUMMARY.md
@.planning/phases/09-regulatory-context/09-02-SUMMARY.md
@.planning/phases/09-regulatory-context/09-03-SUMMARY.md
@backend/app/api/semantic.py
@backend/app/main.py
@backend/app/services/auth/permission_service.py
@backend/app/services/semantic/context_builder.py
@backend/app/schemas/regulatory_context.py
@backend/tests/test_semantic_layer.py
@backend/tests/test_semantic_migration.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the project-aware RegulatoryContext GET endpoint to the builder</name>
  <files>backend/app/api/regulatory_context.py, backend/app/main.py, backend/tests/test_regulatory_context_api.py</files>
  <read_first>backend/app/api/semantic.py, backend/app/main.py, backend/app/services/auth/permission_service.py, backend/app/services/semantic/context_builder.py, backend/app/schemas/regulatory_context.py, backend/tests/test_semantic_layer.py</read_first>
  <behavior>
    - Test 1: An authorized GET /api/projects/{project_id}/regulatory-context with target_table_id, target_field_id, as_of, and optional scenario/mart/concept inputs returns HTTP 200 and context_schema_version "1.0".
    - Test 2: A caller without project.view receives the existing authorization response and the builder is not invoked.
    - Test 3: The acceptance target response contains typed facts, authority/state/provenance/build metadata, and deterministic conflicts or open questions without exposing ORM row dictionaries.
  </behavior>
  <action>Per D-20, D-23, and D-24, create `backend/app/api/regulatory_context.py` with a project-path router and `GET /projects/{project_id}/regulatory-context` accepting bounded `target_table_id`, `target_field_id`, `scenario_id`, `mart_field_id`, `semantic_concept_id`, `as_of`, `reporting_period`, and explicit `mode` query parameters. Require `CurrentPrincipal`, `get_db`, and `PermissionService(db, principal).require_project_permission(project_id, "project.view")`; derive institution scope from the returned Project, validate every optional target belongs to that project, construct `RegulatoryContextRequest`, call `RegulatoryContextBuilder.build()`, and return the typed `RegulatoryContext` without committing authoritative changes. Import the router in `main.py` beside `semantic` and register it with `settings.api_prefix` and the existing `secured` dependency list. Add a TestClient tracer fixture that seeds the 2.3 同业客户表 / 客户统一编号 target and asserts status, stable JSON, permission denial, zero authoritative mutation, and absence of the four planning-only spec-less ids from serialized `open_questions`. Follow the project-aware router and TestClient analogs assigned in `09-PATTERNS.md` (`main.py` and `test_semantic_layer.py`).</action>
  <verify>
    <automated>cd backend; python -m pytest -q tests/test_regulatory_context_api.py -k "context or authorization or read_only" -x</automated>
  </verify>
  <acceptance_criteria>
    - The endpoint is additive, project-scoped, authenticated, and returns a Pydantic RegulatoryContext 1.0 for valid acceptance inputs.
    - Client input cannot override institution ownership or bypass target project validation.
    - The request does not insert or update SemanticConcept, SemanticConceptVersion, SemanticBinding, SemanticRelation, mapping, lineage, PendingQuestion, cache, or context rows.
  </acceptance_criteria>
  <done>An authorized caller can build the real acceptance Context through one secured, read-only project API path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Qualify isolation, candidate mode, temporal errors, gaps, and Phase 8 API compatibility</name>
  <files>backend/app/api/semantic.py, backend/tests/test_regulatory_context_api.py</files>
  <read_first>backend/app/api/semantic.py, backend/app/services/semantic/status_policy.py, backend/app/services/semantic/graph_service.py, backend/app/services/semantic/resolver.py, backend/app/services/semantic/context_builder.py, backend/app/schemas/regulatory_context.py, backend/tests/test_semantic_layer.py</read_first>
  <behavior>
    - Test 1: Two institutions and projects with identical codes cannot read each other's Context, semantic rows, mappings, lineage, or confidential knowledge; target ids from another project produce the existing scoped error.
    - Test 2: Trusted API mode excludes rejected/deprecated and candidate API mode is explicit while draft/ai_suggested/retrieved/inferred values retain non-confirmed status and provenance.
    - Test 3: Inclusive 2026/2027 version selection, temporal overlap 409, missing mapping/lineage/evidence conflicts, and conflicting authoritative facts serialize deterministically; the four planning-only CTX edge ids are absent from HTTP output.
    - Test 4: Existing Concept/Binding/Relation list/detail/status/graph/path/resolver endpoints and response fields remain callable after adding mode propagation and version fields.
  </behavior>
  <action>Per D-02, D-03, D-18, D-20, and D-21, preserve the existing semantic router paths and response models while passing an explicit trusted/candidate mode from graph neighbors, upstream, downstream, entity semantics, shortest path, and resolver calls into the shared policy; trusted remains the default and audit-only statuses cannot be requested as business facts. Keep additive version/current-effective fields and route conflict mapping in the existing semantic API, preserve project permission checks, and never accept an institution id from the request. Add API tests for same-code cross-project/institution isolation, confidential KnowledgeUnit provenance and unauthorized-content omission, candidate/trusted behavior, inclusive effective dates, 409 temporal conflicts, all deterministic domain gap codes, absence of the four planning-only CTX ids from HTTP `open_questions`, and no authoritative status/timestamp/content mutation. Re-run the complete Phase 8 semantic route matrix to prove no endpoint is removed or renamed. Follow the existing semantic route/mode propagation analogs assigned in `09-PATTERNS.md` (`api/semantic.py` and `graph_service.py`).</action>
  <verify>
    <automated>cd backend; python -m pytest -q tests/test_regulatory_context_api.py tests/test_semantic_layer.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Project and institution isolation holds at the API boundary for every target and source family, including knowledge confidentiality and retrieval provenance.
    - Trusted/candidate mode behavior, temporal 409s, deterministic domain conflicts/questions, and non-emission of the four spec-less planning ids are represented in response JSON and covered by tests.
    - All Phase 8 semantic CRUD, governance, graph, path, resolver, and migration-facing routes remain available with prior fields.
  </acceptance_criteria>
  <done>The additive API and existing semantic API jointly enforce scope, lifecycle, temporal, provenance, and deterministic gap behavior without compatibility regressions.</done>
</task>

<task type="auto">
  <name>Task 3: Record SQLite and PostgreSQL offline/staging qualification with full regression evidence</name>
  <files>backend/tests/test_semantic_migration.py, .planning/phases/09-regulatory-context/09-04-POSTGRES-QUALIFICATION.md</files>
  <read_first>backend/tests/test_semantic_migration.py, backend/alembic/versions/202608200016_semantic_concept_versions.py, backend/alembic/versions/202608200015_regulatory_semantic_layer.py, .planning/phases/08-semantic-foundation/08-VERIFICATION.md, .planning/STATE.md, .planning/phases/09-regulatory-context/09-RESEARCH.md</read_first>
  <action>Per D-11, D-20, D-22, D-23, and D-24, extend migration qualification with a direct/offline PostgreSQL dialect compilation check for the 016 revision's explicit operations, assert no SQLite-only expression leaks, and retain the known historical-chain limitation at `202607070002` as a baseline qualification rather than masking it. Run SQLite empty, 015-to-head, populated bootstrap, head-to-015-to-head, and formal-index preservation checks; run the focused Context/API suites and the full backend regression. Compare Phase 9 by test identity and failure signature: Phase 8 had 247 passes plus two pre-existing Windows failures before its eight semantic tests, while its delivered suite had 255 passes plus the same two failures; any additional failure is a Phase 9 regression and must be surfaced. Create `09-04-POSTGRES-QUALIFICATION.md` with executed command, revision, dialect, result, known limitation, and a staging checklist covering PostgreSQL upgrade/downgrade, foreign keys, date checks, `SELECT FOR UPDATE` confirmed-interval overlap/concurrency rejection, project/institution permission isolation, confidentiality, measured query budget, and API response compatibility. Record SQLite's serialized-write check, offline PostgreSQL evidence, and live staging concurrency as separate gates; mark live staging required when unavailable locally and never claim a server test was run if no server answered. Follow the explicit migration and qualification analogs in `09-PATTERNS.md` and Phase 8 `08-VERIFICATION.md`.</action>
  <verify>
    <automated>cd backend; python -m pytest -q tests/test_semantic_migration.py -k "postgres or migration or 016" -x</automated>
  </verify>
  <acceptance_criteria>
    - SQLite migration lifecycle and populated bootstrap tests pass, and the formal embedding index remains present after every round trip.
    - Offline PostgreSQL compilation of revision 016 is captured with no SQLite-only SQL leak; the existing historical-chain MockConnection limitation is explicitly documented.
    - Full backend regression results are compared with Phase 8 and no new failure is hidden by a fallback command or altered baseline.
    - The qualification document gives a reproducible staging checklist without inventing live PostgreSQL evidence.
  </acceptance_criteria>
  <done>Read/debug API, compatibility and isolation tests are green, SQLite and offline PostgreSQL evidence is recorded, and remaining live staging work is explicit and bounded.</done>
</task>

</tasks>

<threat_model>
ASVS Level: L1. Block on: high severity.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP caller to project route | Untrusted query ids, mode, dates, labels, and authorization context enter the API. |
| API to PermissionService/builder | Authorization and project/institution scope must be established before any collector runs. |
| API response to client | Bounded context facts, provenance, confidentiality, conflicts, and open questions cross the wire. |
| migration qualification to database | SQLite/PostgreSQL DDL and data bootstrap are tested against existing stored data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04-01 | Information disclosure | regulatory_context router and semantic router | high | mitigate | Require project.view through PermissionService, derive institution from Project, validate target ids against project, and run two-project/two-institution API tests. |
| T-09-04-02 | Tampering / repudiation | context response and candidate mode | high | mitigate | Preserve shared trusted/candidate policy, keep rejected/deprecated/retrieved/AI states non-confirmed, expose provenance and conflicts, and assert no authoritative mutation. |
| T-09-04-03 | Tampering | version effective/status endpoints and migration | high | mitigate | Return deterministic 409 for overlap or immutable confirmed writes, retain inclusive date checks, and qualify upgrade/downgrade on SQLite and offline PostgreSQL. |
| T-09-04-04 | Information disclosure | ContextFact serialization and knowledge response | high | mitigate | Use bounded Pydantic response models, carry confidentiality/source/retrieval-log provenance, and omit unauthorized knowledge content. |
| T-09-04-05 | Denial of service | context GET and builder query path | high | mitigate | Enforce bounded query parameters, reuse measured batched collectors and query budget, and avoid natural-order candidate truncation or cache-dependent behavior. |
</threat_model>

<verification>
- `cd backend; python -m pytest -q tests/test_regulatory_context_contract.py tests/test_regulatory_context_builder.py tests/test_regulatory_context_api.py tests/test_semantic_layer.py tests/test_semantic_migration.py`
- `cd backend; python -m pytest -q` and compare by test identity/signature with Phase 8's 247-pass pre-semantic baseline and 255-pass delivered total, preserving only the same two Windows failures.
- `cd backend; python -m compileall -q app`
- Read `.planning/phases/09-regulatory-context/09-04-POSTGRES-QUALIFICATION.md` and verify the SQLite/offline evidence, known baseline failures, and staging gate are explicit.
- Confirm no frontend, Phase 10 generator, external service, or new dependency file is in the phase implementation diff.
</verification>

<success_criteria>
- Authorized callers can obtain a stable, project/institution-isolated RegulatoryContext for the acceptance target through an additive read/debug endpoint.
- Authority/state, provenance/confidentiality, temporal version selection, deterministic domain gaps, candidate mode, and non-emission of planning-only spec-less metadata survive HTTP serialization.
- All Phase 8 semantic APIs remain available, migration and query-count qualifications pass, and full regression results are evidence-backed against the documented baseline.
- PostgreSQL live/staging limitations are transparent; no unavailable environment is represented as verified.
</success_criteria>

## Artifacts this phase produces

- `backend/app/api/regulatory_context.py`: `GET /api/projects/{project_id}/regulatory-context` with bounded typed query inputs and project.view enforcement.
- `backend/app/api/semantic.py`: mode propagation and compatibility-preserving additive version/current-effective behavior.
- `backend/app/main.py`: secured router import and registration.
- `backend/tests/test_regulatory_context_api.py`: endpoint, permission, isolation, candidate, gap, provenance, temporal, and compatibility tests.
- `backend/tests/test_semantic_migration.py`: 016 PostgreSQL offline/dialect qualification coverage.
- `.planning/phases/09-regulatory-context/09-04-POSTGRES-QUALIFICATION.md`: SQLite, offline PostgreSQL, staging, performance, and baseline qualification record.

<output>Create `.planning/phases/09-regulatory-context/09-04-SUMMARY.md` when done.</output>
