---
phase: 11-semantic-catalog-ui
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - frontend/app/semantics/page.tsx
  - frontend/components/semantic-catalog/CatalogToolbar.tsx
  - frontend/components/semantic-catalog/GroupedSemanticDirectory.tsx
  - frontend/components/semantic-catalog/SemanticComparisonTable.tsx
  - frontend/components/semantic-catalog/SemanticStatus.tsx
  - frontend/components/AppShell.tsx
  - frontend/lib/types.ts
  - frontend/lib/api.ts
  - frontend/lib/http-response.mjs
  - frontend/lib/http-response.d.mts
  - frontend/lib/semantic-catalog-view-model.mjs
  - frontend/lib/semantic-catalog-view-model.d.mts
  - frontend/tests/semantic-catalog-view-model.test.mjs
  - frontend/tests/http-response.test.mjs
autonomous: true
requirements: [SUI-01]
estimate:
  tokens: 44000
  raw_tokens: 22000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Users can search and filter the full server-backed catalog, switch directory/table views, and restore valid state from a bookmark."
    - "The default directory groups by business domain with 未分类 last; comparison rows expose all required summary fields and confirmed-only counts."
    - "Loading, unfiltered/filtered empty, forbidden, retryable error, pending-review, candidate, and audit-filter states remain visually and semantically distinct at desktop and mobile widths."
  artifacts:
    - "frontend/components/semantic-catalog/CatalogToolbar.tsx"
    - "frontend/components/semantic-catalog/GroupedSemanticDirectory.tsx"
    - "frontend/components/semantic-catalog/SemanticComparisonTable.tsx"
    - "frontend/components/AppShell.tsx"
    - "frontend/tests/semantic-catalog-view-model.test.mjs"
  key_links:
    - "CatalogToolbar URL commits -> page request key -> server totals/facets"
    - "directory/table components -> semantic catalog DTO partitions"
    - "AppShell navigation -> /semantics route with existing project selector"
---

<objective>
Expand the tracer into the complete `/semantics` catalog browsing experience. Implement D-01 through D-08 and D-16, D-17, D-28, and D-30 using the existing shell and design tokens, while retaining server authority for search, filtering, totals, facets, pagination, effective dates, and audit isolation.

Purpose: Make the catalog usable for repeated enterprise browsing without turning it into CRUD or a client-side replica of the semantic store.
Output: URL-backed toolbar, grouped directory, comparison table, status presentation, narrow navigation insertion, responsive/accessibility behavior, and state-matrix tests.
</objective>

<execution_context>
@C:/Users/李儒伟/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/李儒伟/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-semantic-catalog-ui/11-CONTEXT.md
@.planning/phases/11-semantic-catalog-ui/11-UI-SPEC.md
@.planning/phases/11-semantic-catalog-ui/11-RESEARCH.md
@.planning/phases/11-semantic-catalog-ui/11-PATTERNS.md
@frontend/app/catalog/page.tsx
@frontend/components/AppShell.tsx
@frontend/components/ProjectContext.tsx
@frontend/lib/semantic-catalog-view-model.mjs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Establish shared catalog DTO, URL, status, and error seams</name>
  <files>frontend/lib/types.ts, frontend/lib/api.ts, frontend/lib/http-response.mjs, frontend/lib/http-response.d.mts, frontend/lib/semantic-catalog-view-model.mjs, frontend/lib/semantic-catalog-view-model.d.mts, frontend/tests/semantic-catalog-view-model.test.mjs, frontend/tests/http-response.test.mjs</files>
  <read_first>frontend/lib/api.ts, frontend/lib/http-response.mjs, frontend/lib/http-response.d.mts, frontend/lib/types.ts, frontend/lib/workspace-view-model.mjs, frontend/lib/workspace-view-model.d.mts, frontend/tests/http-response.test.mjs, frontend/tests/workspace-view-model.test.mjs, .planning/phases/11-semantic-catalog-ui/11-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: valid/default/invalid catalog and detail query parameters canonicalize, preserve durable URL state, and reject unsafe returnTo values.
    - Test 2: lifecycle, review workflow, trusted/candidate/audit partitions, confirmed counts, effective-version formal-definition selection, and current-only labels are deterministic.
    - Test 3: readable versus restricted references are redacted before render-model creation; only existing canonical destinations are produced and unsupported lawful references use non-navigable fallback text.
    - Test 4: 403 and 500 responses expose status-bearing ApiError values while protected 401 still clears both tokens and redirects exactly as before.
  </behavior>
  <action>Extend the existing authenticated client narrowly so callers can pass AbortSignal and receive a backward-compatible ApiError status for non-401 failures. Add semantic catalog DTO/type declarations and pure transforms for URL parsing/serialization, grouping, status partitions, confirmed-only counts, effective-version selection, current-only flags, safe return paths, destination resolution, and restricted-reference redaction. The destination resolver uses the existing canonical-route allow-list and returns non-navigable fallback text when no route is supported; it never invents a route. Preserve exact 401 behavior and encode D-08, D-16, D-17, D-26, D-27, D-28, and D-30.</action>
  <verify><automated>npm --prefix frontend test -- --test-name-pattern="semantic-catalog|http-response"</automated></verify>
  <acceptance_criteria>Shared types and pure transforms pass table-driven URL, status, redaction, destination, and temporal tests; 403/500 are distinguishable without localized-message matching; existing 401 behavior remains green.</acceptance_criteria>
  <done>The shared frontend seam is ready for catalog and detail routes without adding a package or client-side fact source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build URL-backed catalog controls and server request orchestration</name>
  <files>frontend/app/semantics/page.tsx, frontend/components/semantic-catalog/CatalogToolbar.tsx, frontend/components/AppShell.tsx, frontend/lib/semantic-catalog-view-model.mjs, frontend/lib/semantic-catalog-view-model.d.mts, frontend/tests/semantic-catalog-view-model.test.mjs</files>
  <read_first>frontend/app/semantics/page.tsx, frontend/app/catalog/page.tsx, frontend/components/ProjectContext.tsx, frontend/lib/api.ts, frontend/lib/semantic-catalog-view-model.mjs, frontend/lib/semantic-catalog-view-model.d.mts, .planning/phases/11-semantic-catalog-ui/11-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: q commits only on Enter/Search; enum, date, boolean, view, page, and page_size controls update immediately and reset page where required.
    - Test 2: refresh hydrates valid URL state, omits defaults, canonicalizes invalid values, and preserves project scope; unsafe returnTo is rejected.
    - Test 3: request keys include project and every catalog parameter; project changes abort/ignore prior results and never show prior-project rows or counts.
    - Test 4: successful total/facets are rendered only after response; 403 and 500 select dedicated unauthorized/error states rather than empty.
  </behavior>
  <action>Implement a real form toolbar with primary and collapsible advanced filters, active chips, clear action, directory/table segmented control, and URL synchronization per D-05, D-06, and D-08. Keep search draft local until explicit submission, use router.replace for immediate controls, preserve selected project as the only scope seam, include all filter values in request keys, and model loading/empty/error/unauthorized states without false totals. Insert one `语义目录` item under the existing 数据资产 navigation group using the closest Lucide icon, preserving current AppShell restructuring and active-prefix behavior. Extend fixtures/assertions to cover SUI-01 through SUI-10, SUI-24, SUI-25, and SUI-26, including retry, audit filtering, stale project responses, responsive long text, keyboard operation, and safe catalog return links. Apply D-03, D-07, D-08, D-16, D-17, D-18, D-22, D-26, D-28, and D-30; require audit.read before requesting explicit audit-only status filters per the resolved research decision.</action>
  <verify><automated>npm --prefix frontend test -- --test-name-pattern="semantic-catalog"; npm --prefix frontend run build</automated></verify>
  <acceptance_criteria>All catalog URL/query behaviors and request-race protections have deterministic tests; the toolbar is keyboard-operable, labeled, and retains URL state through reload; 403, error, loading, and successful-empty branches are distinct.</acceptance_criteria>
  <done>Catalog controls and shell navigation drive authoritative requests with durable, canonical URL state and the catalog state matrix is covered.</done>
</task>

<task type="auto">
  <name>Task 3: Render grouped directory, comparison table, and status hierarchy</name>
  <files>frontend/components/semantic-catalog/GroupedSemanticDirectory.tsx, frontend/components/semantic-catalog/SemanticComparisonTable.tsx, frontend/components/semantic-catalog/SemanticStatus.tsx, frontend/app/semantics/page.tsx</files>
  <read_first>frontend/app/catalog/page.tsx, frontend/components/LineageGraph.tsx, frontend/app/review-tasks/page.tsx, frontend/components/WorkspaceHeader.tsx, frontend/app/globals.css, frontend/lib/types.ts, .planning/phases/11-semantic-catalog-ui/11-UI-SPEC.md</read_first>
  <action>Render the default unframed domain directory with stable 68px rows, null/blank domains grouped under 未分类 last, semantic name/code links, effective version, lifecycle, separate Pending Review indicator, Owner, confirmed related-asset count, and update time per D-01, D-02, D-03, and D-04. Add the medium-density accessible comparison table with the exact required columns, deliberate horizontal scrolling, stable 56px rows, full accessible values for ellipsized text, and no decorative nonfunctional sorting. Centralize restrained lifecycle/review labels so Confirmed, Draft, AI Suggested, Rejected, Deprecated, and Pending Review remain distinguishable; hide audit-only rows/counts unless explicit audit filtering per D-07, D-16, D-17, and D-22. Preserve current user-owned files and existing token vocabulary.</action>
  <verify><automated>npm --prefix frontend run build; npm --prefix frontend test -- --test-name-pattern="semantic-catalog"</automated></verify>
  <acceptance_criteria>Directory and table render server-provided data without client-side global filtering; grouping/order/count semantics match the DTO; lifecycle and workflow are separate; audit rows are explicit and marked; 320/768/1280/1440px layout builds without invalid JSX or page-level overflow.</acceptance_criteria>
  <done>Users can scan or compare real catalog results with stable responsive rows and truthful governance indicators.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser URL -> catalog request | User-controlled query state can alter filters, page, audit mode, and return links. |
| ProjectContext -> browser state | Project changes can race with in-flight responses. |
| API DTO -> rendered directory/table | Lifecycle candidates and restricted counts must not be promoted to trusted facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-11-06 | Tampering | CatalogToolbar query state | medium | mitigate | Parse/canonicalize enums, dates, booleans, page, and view; submit q explicitly. |
| T-11-07 | Information Disclosure | Project switch response race | high | mitigate | Abort and captured-key compare before state commit; clear prior project rows during load. |
| T-11-08 | Elevation of Privilege | audit filter UI | high | mitigate | Require explicit audit mode from server response; keep audit rows out of trusted counts and paths. |
| T-11-09 | Information Disclosure | directory/table render models | high | mitigate | Consume server redaction and restricted counts; never reconstruct hidden asset metadata in the browser. |
| T-11-SC | Tampering | npm installs | low | accept | No package installation or registry use is planned. |
</threat_model>

<verification>
<automated>npm --prefix frontend test; npm --prefix frontend run build; npm --prefix frontend run lint</automated>
Manually inspect the required 320x720, 768x1024, 1280x800, and 1440x900 states during phase verification.
</verification>

<success_criteria>
`/semantics` is a real-data, URL-shareable, accessible catalog with grouped and comparison views, server-authoritative totals/facets/pagination, correct lifecycle/workflow/audit semantics, and distinct loading/empty/error/unauthorized/pending states.
</success_criteria>

<output>
Create `.planning/phases/11-semantic-catalog-ui/11-02-SUMMARY.md` when done.
</output>
