---
phase: 11-semantic-catalog-ui
plan: 06
type: execute
wave: 6
depends_on: [11-05]
files_modified:
  - frontend/app/semantics/page.tsx
  - frontend/components/semantic-catalog/CatalogToolbar.tsx
  - frontend/lib/semantic-catalog-controller.mjs
  - frontend/lib/semantic-catalog-controller.d.mts
  - frontend/lib/semantic-catalog-view-model.mjs
  - frontend/lib/semantic-catalog-view-model.d.mts
  - frontend/lib/semantic-entity-types.mjs
  - frontend/lib/semantic-entity-types.d.mts
  - frontend/tests/semantic-catalog-view-model.test.mjs
autonomous: true
gap_closure: true
requirements: [SUI-01]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Switching projects cannot paint a response requested for the previous project, even when requests resolve out of order."
    - "The real /semantics route consumes the same production catalog controller/reducer that deferred-response tests exercise; effects and render authorization are not modeled by an SSR-only fixture."
    - "Audit/status URL state is canonical and accepted by the API; uncategorized is human-readable while retaining its stable wire token."
    - "Users can navigate directly to the first or last server page as well as previous/next, and every restricted entity type has a consistent label."
  artifacts:
    - path: "frontend/app/semantics/page.tsx"
      provides: "request-keyed production catalog state and four-way pagination"
    - path: "frontend/lib/semantic-catalog-controller.mjs"
      provides: "production catalog state reducer and request-authority contract consumed by the real route"
    - path: "frontend/lib/semantic-catalog-view-model.mjs"
      provides: "single canonical audit/status normalization contract"
    - path: "frontend/lib/semantic-entity-types.mjs"
      provides: "exhaustive shared restricted-entity labels"
  key_links:
    - from: "frontend/app/semantics/page.tsx"
      to: "frontend/lib/semantic-catalog-controller.mjs"
      via: "the route derives visible state through the production request-key reducer before rendering any apiGet completion"
      pattern: "requestKey"
    - from: "frontend/app/semantics/page.tsx"
      to: "frontend/lib/api.ts"
      via: "project-scoped semantic-catalog requests use the existing apiGet client with AbortSignal forwarding"
      pattern: "apiGet"
    - from: "frontend/components/semantic-catalog/CatalogToolbar.tsx"
      to: "frontend/lib/semantic-catalog-view-model.mjs"
      via: "canonical query state and sentinel label"
      pattern: "__uncategorized__"
---

<objective>
Close the catalog-page correctness warnings after the backend isolation boundary is repaired. Per D-01 through D-06, D-08, D-09, and D-30, URL state, server pagination, loading/error behavior, and the active project must remain the authoritative catalog contract.

Purpose: Prevent stale cross-project presentation and make every catalog control truthful, reachable, and understandable.
Output: Request-keyed catalog state, canonical filters, readable uncategorized presentation, first/last pagination, shared exhaustive entity labels, and focused regressions.
</objective>

## Artifacts this phase produces

- Production catalog route guarded by a project-and-query request identity.
- Canonical query/filter helpers and readable sentinel presentation.
- Shared exhaustive restricted-entity label contract for catalog and detail consumers.

<execution_context>
@C:/Users/李儒伟/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/李儒伟/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-semantic-catalog-ui/11-CONTEXT.md
@.planning/phases/11-semantic-catalog-ui/11-UI-SPEC.md
@.planning/phases/11-semantic-catalog-ui/11-REVIEW.md
@.planning/phases/11-semantic-catalog-ui/11-05-SUMMARY.md
@frontend/app/semantics/page.tsx
@frontend/components/semantic-catalog/CatalogToolbar.tsx
@frontend/lib/semantic-catalog-controller.mjs
@frontend/lib/semantic-catalog-view-model.mjs
@frontend/tests/semantic-catalog-view-model.test.mjs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Keep one out-of-order project switch from painting stale catalog data</name>
  <files>frontend/app/semantics/page.tsx, frontend/lib/semantic-catalog-controller.mjs, frontend/lib/semantic-catalog-controller.d.mts, frontend/tests/semantic-catalog-view-model.test.mjs</files>
  <read_first>frontend/app/semantics/page.tsx, frontend/lib/api.ts, frontend/components/ProjectContext.tsx, frontend/components/semantic-catalog/AsyncRegion.tsx, frontend/lib/semantic-catalog-view-model.mjs, frontend/tests/semantic-catalog-view-model.test.mjs, .planning/phases/11-semantic-catalog-ui/11-VERIFICATION.md</read_first>
  <behavior>
    - Test 1: request A for project A followed by request B for project B cannot display A when A resolves last.
    - Test 2: the previous project's successful rows are hidden synchronously when the active project/query key changes.
    - Test 3: stale success and stale error completions cannot overwrite B's loading, success, empty, or error state.
    - Test 4: current-key loading, success, empty, forbidden, and retryable error transitions remain distinct and a retry creates a new request attempt without changing URL state.
    - Test 5: the real pagination renders first/previous/next/last controls for more than five pages, retains every canonical filter, and disables boundary actions without producing an out-of-range request.
  </behavior>
  <action>Extract a production `CatalogState` reducer/controller whose inputs are begin, resolve, reject, retry, and scope-change events and whose state always carries the immutable project-plus-canonical-query request identity. Make the real `/semantics` route consume that reducer around its existing `apiGet` effect; authorize loading, success, empty, forbidden, and error rendering only when the state's request key equals the route's current key. Guard every asynchronous completion, not just success, and derive a loading view synchronously when the active scope changes so the prior project cannot paint before effects run. The Node reducer tests use deferred completions to prove ordering, while Plan 11-08 verifies the same route in a browser. In the same route-owned change, add D-05 first/last pagination for result sets over five pages using server total/page_size and preserve canonical URL filters. Preserve the real API and D-08/D-09 state distinctions. Do not modify AppShell or the workspace WIP.</action>
  <verify><automated>npm --prefix frontend test -- --test-name-pattern="request key|out-of-order|stale catalog|pagination"</automated></verify>
  <acceptance_criteria>Controlled A-then-B completions prove that only B can become visible; project changes do not flash A; current loading/error behavior remains explicit; first/last targets and disabled boundaries are correct for first, middle, and last pages.</acceptance_criteria>
  <done>The catalog production state is race-safe and its complete server pagination is reachable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Canonicalize audit/status state and present uncategorized truthfully</name>
  <files>frontend/components/semantic-catalog/CatalogToolbar.tsx, frontend/lib/semantic-catalog-view-model.mjs, frontend/lib/semantic-catalog-view-model.d.mts, frontend/tests/semantic-catalog-view-model.test.mjs</files>
  <read_first>frontend/components/semantic-catalog/CatalogToolbar.tsx, frontend/lib/semantic-catalog-view-model.mjs, frontend/lib/semantic-catalog-view-model.d.mts, frontend/tests/semantic-catalog-view-model.test.mjs, frontend/lib/api.ts, .planning/phases/11-semantic-catalog-ui/11-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: audit=true with no explicit status canonicalizes to an audit-valid status URL/API request and never produces a backend 422.
    - Test 2: leaving audit mode removes or transforms incompatible status state deterministically.
    - Test 3: `__uncategorized__` remains the query value but is announced and displayed as a localized human label.
  </behavior>
  <action>Replace independent audit/status normalization with one canonical state transition used by URL parsing, link generation, toolbar controls, and API params. Preserve D-03 audit-only rejected/deprecated visibility and never expose an inline governance mutation. Map the server sentinel to the UI-SPEC label without changing the wire value, and ensure accessible names describe the human meaning.</action>
  <verify><automated>npm --prefix frontend test -- --test-name-pattern="audit|status|uncategorized"</automated></verify>
  <acceptance_criteria>Every generated catalog URL maps to an API-valid filter tuple, browser navigation rehydrates the same state, and no literal sentinel is user-visible.</acceptance_criteria>
  <done>Catalog filter state is canonical from URL through API and presentation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Centralize one exhaustive entity-label source</name>
  <files>frontend/lib/semantic-entity-types.mjs, frontend/lib/semantic-entity-types.d.mts, frontend/lib/semantic-catalog-view-model.mjs, frontend/lib/semantic-catalog-view-model.d.mts, frontend/tests/semantic-catalog-view-model.test.mjs</files>
  <read_first>frontend/app/semantics/page.tsx, frontend/lib/semantic-catalog-view-model.mjs, frontend/lib/semantic-catalog-dom-contract.mjs, frontend/tests/semantic-catalog-view-model.test.mjs, frontend/components/semantic-catalog/BindingList.tsx, .planning/phases/11-semantic-catalog-ui/11-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: source_to_mart_mapping, mart_to_ybt_mapping, scenario_business_mapping, and scenario_technical_lineage have stable Chinese labels alongside all existing types.
    - Test 2: every API EntityType resolves through the same production table used by catalog/detail readable and restricted references; no duplicate label map can drift.
  </behavior>
  <action>Extract the complete entity-type label table to one production module that both catalog and later detail/link code consume; update declarations and tests so an unknown readable type falls back safely but every API-known restricted type is explicit. Do not duplicate the table in the DOM contract or detail route. Per D-27, the label function must accept a type value alone for restricted references and must not require an identifier or protected metadata.</action>
  <verify><automated>npm --prefix frontend test -- --test-name-pattern="entity label|restricted reference"</automated></verify>
  <acceptance_criteria>All known entity types render a stable human label from one module, and restricted-label construction requires only entity_type.</acceptance_criteria>
  <done>Catalog and detail references share one exhaustive, permission-safe label contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Active project/query -> asynchronous response | A response is untrusted for display unless its request identity still matches. |
| URL state -> backend filter contract | Independently valid fields can form an invalid audit/status pair. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-06-01 | Information Disclosure | `/semantics` catalog route state | critical | mitigate | Drive the real route through a production reducer and key every visible state transition to the active project and canonical query. |
| T-11-06-02 | Tampering | URL filter normalization | medium | mitigate | Use one canonical audit/status state machine for parse, links, controls, and API params. |
| T-11-06-03 | Denial of Service | pagination controls | low | mitigate | Bound targets to server metadata and disable boundary actions semantically. |
| T-11-06-SC | Tampering | package installation | low | accept | No package installation is planned. |
</threat_model>

<verification><automated>npm --prefix frontend test -- --test-name-pattern="catalog|request key|audit|pagination|entity label"</automated></verification>

<success_criteria>No stale response can cross a project/query boundary; filters are API-valid and human-readable; first/last navigation and exhaustive restricted labels are production-wired.</success_criteria>

<output>Create `.planning/phases/11-semantic-catalog-ui/11-06-SUMMARY.md` when done.</output>
