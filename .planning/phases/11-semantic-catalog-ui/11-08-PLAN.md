---
phase: 11-semantic-catalog-ui
plan: 08
type: execute
wave: 8
depends_on: [11-06, 11-07]
files_modified:
  - frontend/tests/semantic-catalog-browser-harness.mjs
  - frontend/tests/semantic-catalog-browser.test.mjs
autonomous: true
gap_closure: true
requirements: [SUI-01, SUI-02]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A headless Edge/Chrome session visits the real /semantics route, drives the actual ProjectSelector, defers project A and B apiGet requests, and proves stale success/error completions never paint A in B scope."
    - "The production catalog route has executable browser assertions for loading, successful empty, retryable error/retry, unauthorized/no-protected-content, canonical audit/status URL state, uncategorized labeling, and first/previous/next/last pagination."
    - "The production detail route has actual keyboard/focus assertions for tabs and disclosures plus executable loading, empty, error/retry, unauthorized, restricted-DOM, conflict-source, and long-content cases."
    - "Two evidence disclosures expose stable distinct aria-controls IDs and independent expanded state; restricted references contain only translated type plus 受限 in both DOM and accessibility output."
  artifacts:
    - path: "frontend/tests/semantic-catalog-browser-harness.mjs"
      provides: "dependency-free Next dev server, Chrome DevTools Protocol, API interception, DOM, accessibility-tree, focus, and keyboard driver"
    - path: "frontend/tests/semantic-catalog-browser.test.mjs"
      provides: "real-route Phase 11 production interaction and visible-state regression matrix"
  key_links:
    - from: "frontend/tests/semantic-catalog-browser.test.mjs"
      to: "frontend/app/semantics/page.tsx"
      via: "headless browser navigation to /semantics with intercepted real apiGet network requests and actual ProjectSelector changes"
      pattern: "/semantics"
    - from: "frontend/tests/semantic-catalog-browser.test.mjs"
      to: "frontend/app/semantics/[id]/page.tsx"
      via: "headless browser navigation to /semantics/{id}, real tab/disclosure controls, DOM and accessibility-tree assertions"
      pattern: "aria-controls"
    - from: "frontend/app/semantics/page.tsx"
      to: "frontend/lib/semantic-catalog-controller.mjs"
      via: "the browser test exercises the route that consumes the production request-key reducer created in Plan 11-06"
      pattern: "requestKey"
---

<objective>
Close the fixture-only production-interaction gap with an executable browser harness over the actual Phase 11 routes. Per D-01, D-05, D-08, D-09, D-14, D-21, D-27, D-28, and D-30, prove real effects, deferred completions, project switches, keyboard/focus behavior, disclosures, and protected-content absence rather than inferring them from static markup.

Purpose: Detect route/component regressions that pure reducers and renderToStaticMarkup cannot exercise while adding no package or lockfile change.
Output: A dependency-free CDP harness and a production browser suite covering the complete gap-state matrix.
</objective>

## Artifacts this phase produces

- A first-party Node test harness that starts the installed Next runtime, launches installed Edge or Chrome headlessly, and drives Chrome DevTools Protocol without an npm dependency.
- Production-route tests with deterministic API interception and deferred responses.
- DOM, accessibility-tree, keyboard/focus, retry, stale-scope, restricted-reference, conflict, and multi-item disclosure assertions included by the existing `tests/*.test.mjs` command.

<execution_context>
@C:/Users/李儒伟/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/李儒伟/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-semantic-catalog-ui/11-CONTEXT.md
@.planning/phases/11-semantic-catalog-ui/11-UI-SPEC.md
@.planning/phases/11-semantic-catalog-ui/11-REVIEW.md
@.planning/phases/11-semantic-catalog-ui/11-VERIFICATION.md
@.planning/phases/11-semantic-catalog-ui/11-06-SUMMARY.md
@.planning/phases/11-semantic-catalog-ui/11-07-SUMMARY.md
@frontend/app/semantics/page.tsx
@frontend/app/semantics/[id]/page.tsx
@frontend/components/ProjectContext.tsx
@frontend/lib/api.ts
@frontend/lib/semantic-catalog-controller.mjs
@frontend/tests/semantic-catalog-dom.test.mjs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Drive the real catalog route through deferred project switches and every route-level state</name>
  <files>frontend/tests/semantic-catalog-browser-harness.mjs, frontend/tests/semantic-catalog-browser.test.mjs</files>
  <read_first>frontend/package.json, frontend/app/semantics/page.tsx, frontend/components/ProjectContext.tsx, frontend/components/semantic-catalog/CatalogToolbar.tsx, frontend/lib/api.ts, frontend/lib/semantic-catalog-controller.mjs, frontend/lib/semantic-catalog-view-model.mjs, frontend/tests/semantic-catalog-view-model.test.mjs</read_first>
  <behavior>
    - Test 1: launch the installed Next development server and an installed headless Edge/Chrome, visit `/semantics?projectId=1`, resolve project A, and visibly assert A's unique row name and total count. Hold project B's request, change the actual `当前项目` ProjectSelector to B, and before resolving B immediately assert A's row/count are absent and the production `aria-busy` loading boundary is visible; then resolve B and assert only B's row/count appear.
    - Test 2: in a separate late-success case, hold A's request, switch through the actual ProjectSelector to B, resolve B and then the stale A success, and assert A's unique name/count never paints in B scope before or after the late completion.
    - Test 3: in a separate late-error case, hold A's request, switch through the actual ProjectSelector to B, reject A after the switch, and prove the stale rejection cannot replace B loading, successful empty, populated, forbidden, or retryable-error state.
    - Test 4: a held current catalog request exposes the production `aria-busy` loading shell with no false zero/empty copy; successful zero renders the correct unfiltered and filtered empty variants.
    - Test 5: HTTP 500 renders the real retry action, activating it issues a fresh same-scope request and succeeds without losing URL controls; HTTP 403 renders unauthorized copy with no facet, total, cached row name, or generic empty output.
    - Test 6: direct incompatible audit/status URLs canonicalize before the API request; `__uncategorized__` remains the intercepted wire value but only `未分类` is visible; first/previous/next/last controls preserve filters and actual disabled boundaries.
  </behavior>
  <action>Build a narrow Node ESM CDP harness using only built-in modules and the repository's installed Next/React runtime. Spawn `frontend/node_modules/next/dist/bin/next` on an available loopback port, wait for readiness with a bounded timeout, locate Edge/Chrome from PATH and the verified Windows install locations, launch it headlessly with an isolated temporary user-data directory, read its DevTools endpoint, and connect with Node's built-in WebSocket. Use CDP `Fetch` interception to fulfill `/api/projects` and semantic API requests with deterministic JSON/CORS headers, including promises held until the test releases them. Expose helpers for navigation, request queues, clicks, select changes, DOM text/attribute queries, accessibility-tree reads, key dispatch, focus checks, and guaranteed child-process/browser/temp cleanup. Do not silently skip when a browser or server is unavailable. In the CR-01 prior-success scenario, fulfill A first and assert its unique row and count are visibly painted, hold B, switch with the actual ProjectSelector, and observe the immediate post-switch DOM before fulfilling B so the test proves A is cleared at the loading boundary. Implement stale late success and stale late error as independent named cases with separate deferred A completions, not two branches of the prior-success case. Use the actual `/semantics` route, ProjectProvider/ProjectSelector, toolbar, pagination, `apiGet`, and the Plan 11-06 production controller; do not copy their JSX or claim SSR covers effects/events.</action>
  <verify><automated>node --test --test-name-pattern="production browser catalog" frontend/tests/semantic-catalog-browser.test.mjs</automated></verify>
  <acceptance_criteria>The real route first visibly paints A, then proves the actual ProjectSelector clears A's row/count and shows loading before held B resolves; independent late-success and late-error cases cannot repaint stale A state; loading, empty, error/retry, unauthorized, URL, sentinel, and pagination assertions also pass, and removing the request-key guard or any state branch makes a named browser test fail.</acceptance_criteria>
  <done>The production catalog is covered by executable effects, network-order, DOM, and control interaction tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exercise real detail tabs, focus, retries, restricted DOM, conflicts, and independent disclosures</name>
  <files>frontend/tests/semantic-catalog-browser.test.mjs</files>
  <read_first>frontend/tests/semantic-catalog-browser-harness.mjs, frontend/app/semantics/[id]/page.tsx, frontend/components/semantic-catalog/SemanticTabs.tsx, frontend/components/semantic-catalog/AsyncRegion.tsx, frontend/components/semantic-catalog/BindingList.tsx, frontend/components/semantic-catalog/SemanticDetailHeader.tsx, frontend/components/semantic-catalog/EvidenceDisclosure.tsx, frontend/lib/semantic-detail-contract.mjs, frontend/lib/types.ts</read_first>
  <behavior>
    - Test 1: a held detail shell and held lazy-region request expose separate production loading boundaries; successful empty, HTTP 500 with object-specific retry/success, and HTTP 403 remain distinct while the loaded header stays mounted.
    - Test 2: ArrowLeft/ArrowRight/Home/End sent through CDP to real tab buttons update `aria-selected`, move `document.activeElement`, retain one matching tabpanel, and do not steal focus when a deferred region finishes.
    - Test 3: a restricted binding payload renders translated entity type plus `受限`; seeded identifier/name/code/href/title/source/metadata markers are absent from `outerHTML`, document text, attributes, links, and the CDP accessibility tree, while a readable nonnavigable reference retains lawful display text without a link.
    - Test 4: a two-source conflict renders both attributed sources; a long summary is bounded initially and its keyboard-operated control reveals the exact full summary without dismissing the alert or changing formal definition.
    - Test 5: two long evidence items have stable distinct `aria-controls`; keyboard expansion of one reveals its exact full text and leaves the second collapsed, then the second can expand independently; short/empty evidence creates no unnecessary disclosure.
    - Test 6: a 12k definition and long source content remain present and selectable, the disclosure path is available, and no hidden restricted marker appears; browser viewport geometry remains owned by the later dedicated UI-review gate.
  </behavior>
  <action>Extend the same browser session/scenario server to drive `/semantics/42` and the real detail shell/lazy endpoints. Interact with production tabs and buttons using CDP key and mouse events, then assert active element, ARIA state, panel identity, retry requests, exact text, DOM attributes, links, and `Accessibility.getFullAXTree`. Seed adversarial protected markers only in the intercepted restricted test object to prove the client never renders them, while Plan 11-05 proves the real DTO omits them before serialization per D-27. Exercise two conflict sources and two evidence items so list ordering, stable IDs, independent state, and exact expanded text are observable. Keep existing fixture/contract tests as supporting pure-unit coverage only; no static-render assertion counts as evidence for an effect, keyboard event, focus move, retry, or disclosure transition.</action>
  <verify><automated>node --test --test-name-pattern="production browser detail" frontend/tests/semantic-catalog-browser.test.mjs</automated></verify>
  <acceptance_criteria>Every loading/empty/error/unauthorized/stale-scope/keyboard/restricted-DOM/long-content requirement has a named real-route browser assertion; tab focus and two-item disclosure state are exercised through actual events; protected markers are absent from DOM and accessibility output.</acceptance_criteria>
  <done>The production detail route and repaired components have executable browser interaction coverage for every fourth-gap state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Intercepted API scenario -> production route | The harness may control external responses but must load the real Next routes, contexts, effects, components, and shared apiGet code. |
| Deferred response -> current project/query | Completion order is adversarial and cannot confer display authority. |
| Restricted payload -> DOM/accessibility tree | Protected fields must remain absent even when an adversarial test object carries them. |
| Browser process -> test runner | Child processes, ports, CDP sessions, and temporary profiles must be bounded and cleaned on pass or failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-08-01 | Spoofing | fixture-only UI evidence | high | mitigate | Navigate to real `/semantics` and `/semantics/{id}` routes and assert production DOM, focus, accessibility tree, effects, and network requests. |
| T-11-08-02 | Information Disclosure | out-of-order project fetch | critical | mitigate | Hold A/B requests, switch the actual project selector, resolve in adversarial order, and assert no stale content at every transition. |
| T-11-08-03 | Information Disclosure | restricted references | high | mitigate | Inspect text, outerHTML, attributes, links, and CDP accessibility output for seeded protected markers. |
| T-11-08-04 | Denial of Service | test browser/server lifecycle | medium | mitigate | Use loopback-only ports, bounded readiness/test timeouts, isolated profiles, and unconditional cleanup. |
| T-11-08-SC | Tampering | package installation | low | accept | No package or lockfile change; the harness uses installed Next plus installed Edge/Chrome and Node built-ins. |
</threat_model>

<verification><automated>node --test frontend/tests/semantic-catalog-browser.test.mjs</automated></verification>

<success_criteria>The real Phase 11 routes pass production browser tests for loading, empty, error/retry, unauthorized, stale scope, canonical URL/pagination, actual tab keyboard/focus, restricted DOM/accessibility absence, two-source conflicts, and two-item long-content disclosures without a new dependency.</success_criteria>

<output>Create `.planning/phases/11-semantic-catalog-ui/11-08-SUMMARY.md` when done.</output>
