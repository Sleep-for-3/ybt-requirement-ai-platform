---
phase: 11-semantic-catalog-ui
plan: 07
type: execute
wave: 7
depends_on: [11-05, 11-06]
files_modified:
  - frontend/app/semantics/[id]/page.tsx
  - frontend/components/semantic-catalog/BindingList.tsx
  - frontend/components/semantic-catalog/SemanticDetailHeader.tsx
  - frontend/components/semantic-catalog/EvidenceDisclosure.tsx
  - frontend/lib/semantic-detail-contract.mjs
  - frontend/lib/semantic-detail-contract.d.mts
  - frontend/tests/semantic-detail-contract.test.mjs
autonomous: true
gap_closure: true
requirements: [SUI-02]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every authorized canonical `/lineage` query target is navigable; a readable reference without a supported destination remains readable but nonnavigable; a restricted reference renders only translated type plus 受限."
    - "Conflict records show a bounded accessible list of every backend-provided source summary, including two-source and long-summary cases, without implying that audit evidence changes formal truth."
    - "Two or more long evidence items have stable per-item disclosure IDs, independent expanded state, and exact full text available through keyboard- and screen-reader-operable controls."
  artifacts:
    - path: "frontend/components/semantic-catalog/BindingList.tsx"
      provides: "exact canonical lineage-query allowlisting"
    - path: "frontend/components/semantic-catalog/SemanticDetailHeader.tsx"
      provides: "bounded conflict source summaries with accessible full-source disclosure"
    - path: "frontend/components/semantic-catalog/EvidenceDisclosure.tsx"
      provides: "accessible bounded evidence rendering"
  key_links:
    - from: "frontend/components/semantic-catalog/BindingList.tsx"
      to: "/lineage?scenarioTechnicalLineageId=...&from=semantics&semanticConceptId=... and /lineage?nodeId=...&from=semantics&semanticConceptId=..."
      via: "exact pathname, exactly one selector, required provenance parameters, current-concept match, and byte-preserving href validation"
      pattern: "lineage"
    - from: "frontend/app/semantics/[id]/page.tsx"
      to: "frontend/components/semantic-catalog/EvidenceDisclosure.tsx"
      via: "stable evidence item IDs and independent per-item disclosure instances in lazy-region rendering"
      pattern: "EvidenceDisclosure"
    - from: "frontend/components/semantic-catalog/BindingList.tsx"
      to: "frontend/lib/semantic-entity-types.mjs"
      via: "restricted references are rendered from entity_type alone; readable nonnavigable references retain only lawfully supplied display fields"
      pattern: "restricted"
---

<objective>
Close the detail-page discoverability, provenance, and accessibility warnings. Per D-12 through D-21, D-23 through D-27, and D-30, the production detail view must preserve formal/candidate/audit separation while making lawful lineage links, conflicts, restricted references, and bounded evidence complete.

Purpose: Make every backend-authorized detail affordance visible and usable without weakening governance or disclosure boundaries.
Output: Exact lineage-query links, conflict source rendering, exhaustive restricted labels, accessible evidence disclosure, and focused contract tests.
</objective>

## Artifacts this phase produces

- Exact allowlist contract for canonical lineage query links.
- Conflict header rendering for backend-provided source summaries.
- Reusable bounded evidence disclosure with accessible expanded content.

<execution_context>
@C:/Users/李儒伟/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/李儒伟/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-semantic-catalog-ui/11-CONTEXT.md
@.planning/phases/11-semantic-catalog-ui/11-UI-SPEC.md
@.planning/phases/11-semantic-catalog-ui/11-REVIEW.md
@.planning/phases/11-semantic-catalog-ui/11-05-SUMMARY.md
@.planning/phases/11-semantic-catalog-ui/11-06-SUMMARY.md
@frontend/app/semantics/[id]/page.tsx
@frontend/components/semantic-catalog/BindingList.tsx
@frontend/components/semantic-catalog/SemanticDetailHeader.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Navigate one canonical lineage query link end to end</name>
  <files>frontend/components/semantic-catalog/BindingList.tsx, frontend/lib/semantic-detail-contract.mjs, frontend/lib/semantic-detail-contract.d.mts, frontend/tests/semantic-detail-contract.test.mjs</files>
  <read_first>frontend/components/semantic-catalog/BindingList.tsx, frontend/lib/api.ts, frontend/lib/semantic-catalog-dom-contract.mjs, frontend/tests/semantic-catalog-dom.test.mjs, frontend/app/lineage/page.tsx, .planning/phases/11-semantic-catalog-ui/11-VERIFICATION.md</read_first>
  <behavior>
    - Test 1: the exact production-emitted strings `/lineage?scenarioTechnicalLineageId=37&from=semantics&semanticConceptId=42` and `/lineage?nodeId=91&from=semantics&semanticConceptId=42` render as links whose anchor href equals the supplied backend string byte-for-byte.
    - Test 2: a lineage target is lawful only when it has exactly one selector key (`scenarioTechnicalLineageId` xor `nodeId`), exactly one `from=semantics`, and exactly one `semanticConceptId` equal to the current detail concept; duplicate selectors, duplicate provenance keys, both selectors, unknown keys, mismatched concept IDs, missing keys, empty values, wrong paths, fragments, external origins, or malformed encodings do not become links.
    - Test 3: a readable reference with an unsupported or rejected destination keeps its lawfully supplied display name/code and type as nonnavigable text.
    - Test 4: a restricted reference is accepted only as `{ entity_type, restricted: true }`; its render model/DOM contains the translated type plus `受限` and contains no identifier, name, code, href, title, source content, or metadata.
  </behavior>
  <action>Parse backend-provided internal URLs and, per D-26, allow `/lineage` only when its query contains exactly three entries: exactly one selector (`scenarioTechnicalLineageId` or `nodeId`) with a non-empty backend ID, `from` exactly equal to `semantics`, and `semanticConceptId` exactly equal to the current detail concept ID supplied to the validator. Reject duplicate keys, both selector keys, unknown keys, missing provenance, mismatched concepts, fragments, and non-local origins. Derive the positive fixtures from the production emitters `CatalogQueryService._describe_detail_asset` and `_lineage_node_references`, and preserve each validated readable href byte-for-byte rather than rebuilding or reordering its query. Keep the already authorized internal paths unchanged. When a readable reference has no supported destination, render its lawful display fields with `尚无可导航详情`. Handle restricted references as the strict D-27 union variant containing only `entity_type` and `restricted=true`; render the shared translated type label plus `受限` without ever reading or inferring an identifier, name, code, href, title, source content, or metadata. Do not synthesize a graph or link from a restricted reference.</action>
  <verify><automated>npm --prefix frontend test -- --test-name-pattern="lineage query|restricted reference"</automated></verify>
  <acceptance_criteria>Both three-parameter backend-emitted lineage query shapes navigate with byte-identical hrefs; only one selector is accepted, required provenance/current-concept values match, duplicates/unknowns/mismatches are rejected, readable nonnavigable and restricted variants remain distinct, and restricted output contains no protected field or identifier.</acceptance_criteria>
  <done>Backend-authorized lineage query destinations are reachable through the production binding list.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render conflict source summaries without changing formal truth</name>
  <files>frontend/components/semantic-catalog/SemanticDetailHeader.tsx, frontend/components/semantic-catalog/EvidenceDisclosure.tsx, frontend/lib/semantic-detail-contract.mjs, frontend/lib/semantic-detail-contract.d.mts, frontend/tests/semantic-detail-contract.test.mjs</files>
  <read_first>frontend/components/semantic-catalog/SemanticDetailHeader.tsx, frontend/lib/api.ts, frontend/lib/semantic-catalog-view-model.mjs, frontend/tests/semantic-catalog-dom.test.mjs, frontend/app/globals.css, .planning/phases/11-semantic-catalog-ui/11-CONTEXT.md</read_first>
  <behavior>
    - Test 1: a two-source conflict renders both attributed source type/summary values in stable list order next to the conflict summary.
    - Test 2: absent/empty sources produce no empty landmark or fabricated source.
    - Test 3: formal definition remains sourced only from SemanticConceptVersion and is visually distinct from conflict evidence.
    - Test 4: a long source summary is line-bounded initially, exposes an accessible full-summary control, and returns the exact unmodified full text when expanded.
    - Test 5: when source count exceeds the visible banner bound, the banner reports the remaining count and a keyboard-operable disclosure exposes every source without dismissing the conflict.
  </behavior>
  <action>Extend the production detail header to render the conflict source collection already supplied by the real API. Show the first two source rows in the persistent banner; when more exist, report the remaining count and expose the full stable-order list through a native-button disclosure. Use the reusable disclosure for long per-source summaries with a deterministic three-line collapsed presentation, stable `aria-controls`, and exact full text when expanded. Keep the conflict region persistent and separate from formal definition/status per D-11, D-12, D-15, and D-21. Do not select a winner, mutate governance, or substitute a source summary into formal content.</action>
  <verify><automated>npm --prefix frontend test -- --test-name-pattern="conflict source"</automated></verify>
  <acceptance_criteria>Two sources are directly inspectable, overflow sources and long summaries are fully keyboard/screen-reader accessible, no source is lost or fabricated, and formal truth remains distinct.</acceptance_criteria>
  <done>The production conflict header exposes complete provenance without altering governance semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bound long evidence behind accessible disclosure</name>
  <files>frontend/app/semantics/[id]/page.tsx, frontend/components/semantic-catalog/EvidenceDisclosure.tsx, frontend/lib/semantic-detail-contract.mjs, frontend/lib/semantic-detail-contract.d.mts, frontend/tests/semantic-detail-contract.test.mjs</files>
  <read_first>frontend/app/semantics/[id]/page.tsx, frontend/components/semantic-catalog/AsyncRegion.tsx, frontend/components/semantic-catalog/TrustSourceRegion.tsx, frontend/app/globals.css, frontend/tests/semantic-catalog-dom.test.mjs, .planning/phases/11-semantic-catalog-ui/11-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: long evidence shows a deterministic bounded excerpt by default and an accessible control exposing the complete original text.
    - Test 2: the control reports expanded state, has an associated content region, and works from keyboard activation.
    - Test 3: two evidence items derive stable distinct `aria-controls` IDs from their type/id keys, and expanding either item leaves the other item's state unchanged.
    - Test 4: expanded content equals the exact original text; short evidence is not needlessly truncated and empty evidence does not create a disclosure.
  </behavior>
  <action>Finish the reusable production disclosure for evidence/audit excerpts with deterministic six-line collapsed bounds, a native button, `aria-expanded`, and an associated region whose ID is derived from the stable evidence type/id key rather than list position or a random runtime ID. Give each rendered item local independent state; expanding one item must not expand or collapse another. Wire it into the real lazy detail regions per D-16, D-17, D-22, and D-30, preserve the exact source text for the expanded state, and preserve URL/project/error behavior. Use existing tokens/styles and dependencies.</action>
  <verify><automated>npm --prefix frontend test -- --test-name-pattern="evidence disclosure|bounded evidence"</automated></verify>
  <acceptance_criteria>At least two evidence items have stable unique control/panel IDs and independent state; long evidence is compact initially; exact full text is keyboard/screen-reader accessible; short/empty states remain clean.</acceptance_criteria>
  <done>Evidence disclosure is bounded, complete, and accessible in the production detail route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend internal URL -> client navigation | An internal-looking string is not lawful until its exact path/query shape is validated. |
| Audit/conflict evidence -> formal presentation | Evidence may be shown but cannot become authoritative definition or mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-07-01 | Elevation of Privilege | BindingList link rendering | high | mitigate | Exact allowlist for path, single query key, value, origin, and fragments. |
| T-11-07-02 | Spoofing | conflict source presentation | medium | mitigate | Render sources as attributed evidence separated from formal truth. |
| T-11-07-03 | Information Disclosure | restricted references | high | mitigate | Keep readable nonnavigable references separate; accept restricted DTOs with entity_type and restricted=true only, and assert IDs/names/codes/hrefs/titles/source content/metadata are absent from render models and DOM. |
| T-11-07-SC | Tampering | package installation | low | accept | No package installation is planned. |
</threat_model>

<verification><automated>npm --prefix frontend test -- --test-name-pattern="lineage|conflict|restricted|evidence"</automated></verification>

<success_criteria>Canonical lineage query links work exactly; readable nonnavigable references remain useful; D-27 restricted references expose only translated type plus 受限; conflicts expose all supplied sources through bounded accessible controls; multiple evidence disclosures have stable independent state.</success_criteria>

<output>Create `.planning/phases/11-semantic-catalog-ui/11-07-SUMMARY.md` when done.</output>
