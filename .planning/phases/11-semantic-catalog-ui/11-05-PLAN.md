---
phase: 11-semantic-catalog-ui
plan: 05
type: execute
wave: 5
depends_on: [11-03, 11-04]
files_modified:
  - backend/app/services/semantic/version_service.py
  - backend/app/services/semantic/catalog_query_service.py
  - backend/tests/test_semantic_catalog_api.py
autonomous: true
gap_closure: true
requirements: [SUI-01, SUI-02]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Catalog and detail responses never expose a same-project semantic version, binding, relation, question, or audit row owned by another institution, including explicit null-institution policy cases."
    - "Catalog has_relation and relation counts include only confirmed relations whose source and target concepts are both confirmed and institution-visible; rejected, deprecated, draft, and foreign-institution endpoints never contaminate trusted aggregates."
    - "The server-emitted __uncategorized__ facet retrieves exactly null/blank-domain concepts and remains part of the same filtered population as totals, facets, and pagination."
  artifacts:
    - path: "backend/app/services/semantic/version_service.py"
      provides: "Backward-compatible canonical effective-version resolution with an explicit UNSCOPED_INSTITUTION sentinel that distinguishes omitted scope from explicit null scope"
    - path: "backend/app/services/semantic/catalog_query_service.py"
      provides: "Institution-scoped subordinate projections, confirmed endpoint relation aggregates, and uncategorized filtering"
    - path: "backend/tests/test_semantic_catalog_api.py"
      provides: "Adversarial isolation, contamination, facet, temporal, and query-count regressions"
  key_links:
    - from: "backend/app/services/semantic/catalog_query_service.py"
      to: "backend/app/services/semantic/version_service.py"
      via: "authorized Project institution scope, including explicit null, passed to the canonical batch resolver rather than omitted"
      pattern: "resolve_effective_versions"
    - from: "backend/app/services/semantic/catalog_query_service.py"
      to: "SemanticBinding/SemanticRelation/SemanticConceptVersion/PendingQuestion/AuditLog"
      via: "one explicit project institution equality-or-null predicate before projection"
      pattern: "institution_id"
    - from: "backend/tests/test_semantic_catalog_api.py"
      to: "catalog, shell, bindings, relations, governance, and versions routes"
      via: "same-project foreign-institution subordinate fixtures"
      pattern: "foreign_institution"
---

<objective>
Close the Phase 11 isolation/security blocker and the dependent server-population inconsistencies. Per D-07, D-11, D-22, D-25, D-27, and D-29, every formal version and subordinate semantic projection must be scoped to the authorized Project institution before it can influence a DTO, trusted count, relation flag, facet, or audit view.

Purpose: Eliminate the demonstrated cross-institution disclosure first so later frontend and verification plans consume a safe authoritative API.
Output: Institution-aware temporal resolution, uniformly scoped semantic subordinate queries, confirmed-endpoint relation aggregates, working uncategorized filtering, and adversarial regressions.
</objective>

## Artifacts this phase produces

- Hardened canonical effective-version resolver contract with an explicit institution scope that preserves existing callers when omitted.
- Hardened catalog/detail projection service with one authorized Project institution policy across every institution-bearing row source.
- Backend regression fixtures covering both critical isolation failures and the relation/facet warnings.

<execution_context>
@C:/Users/李儒伟/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/李儒伟/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-semantic-catalog-ui/11-CONTEXT.md
@.planning/phases/11-semantic-catalog-ui/11-RESEARCH.md
@.planning/phases/11-semantic-catalog-ui/11-REVIEW.md
@.planning/phases/11-semantic-catalog-ui/11-VERIFICATION.md
@.planning/phases/11-semantic-catalog-ui/11-04-SUMMARY.md
@backend/app/services/semantic/version_service.py
@backend/app/services/semantic/catalog_query_service.py
@backend/tests/test_semantic_catalog_api.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject one same-project foreign-institution formal path end to end</name>
  <files>backend/app/services/semantic/version_service.py, backend/app/services/semantic/catalog_query_service.py, backend/tests/test_semantic_catalog_api.py</files>
  <read_first>backend/app/services/semantic/version_service.py, backend/app/services/semantic/catalog_query_service.py, backend/tests/test_semantic_catalog_api.py, backend/app/models/semantic.py, backend/app/models/entities.py, .planning/phases/11-semantic-catalog-ui/11-VERIFICATION.md</read_first>
  <behavior>
    - Test 1: a confirmed version with the correct project/concept IDs and a foreign institution_id cannot become the catalog or detail formal definition.
    - Test 2: the same adversarial shell request returns the authorized institution's canonical version, or a successful null formal state when no authorized version exists, without falling back to legacy or AI content.
    - Test 3: an omitted institution-scope argument preserves legacy cross-institution caller behavior, an integer scope accepts equality only, and an explicitly supplied null scope accepts only null-institution concept/version rows.
  </behavior>
  <action>Start with a failing TestClient fixture that reproduces CR-02 through the real catalog and detail shell routes. Add a named `UNSCOPED_INSTITUTION` sentinel (or an equivalently typed overload) to `resolve_effective_versions` so omission is distinguishable from an explicitly supplied `None`: omission preserves existing Phase 8/9 callers, an integer adds equality predicates, and explicit `None` adds `IS NULL` predicates to both the joined SemanticConcept and SemanticConceptVersion. Pass `project.institution_id` explicitly from every Phase 11 resolver call, even when it is null, and reject any mismatched result before DTO construction. Preserve SemanticConceptVersion as the only formal source per D-11 and preserve inclusive effective boundaries and ambiguity behavior.</action>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_semantic_catalog_api.py::test_catalog_and_detail_reject_same_project_foreign_institution_versions -q -x</automated>
  </verify>
  <acceptance_criteria>The adversarial formal definition is absent from catalog JSON and detail shell JSON; omitted, integer, and explicit-null resolver calls have separate regression assertions; legacy callers remain source-compatible; inclusive and ambiguity tests remain green.</acceptance_criteria>
  <done>The canonical formal-version path is institution-safe end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scope every subordinate semantic partition and trusted relation aggregate</name>
  <files>backend/app/services/semantic/catalog_query_service.py, backend/tests/test_semantic_catalog_api.py</files>
  <read_first>backend/app/services/semantic/catalog_query_service.py, backend/tests/test_semantic_catalog_api.py, backend/app/models/semantic.py, backend/app/models/deliverables.py, backend/app/models/governance.py, backend/app/services/semantic/status_policy.py</read_first>
  <behavior>
    - Test 1: foreign-institution confirmed/candidate/audit bindings, relations, questions, versions, and audit rows do not enter catalog counts, detail partitions, conflicts, governance events, or bounded chains.
    - Test 2: has_relation and related relation projections require a confirmed relation plus confirmed, institution-visible source and target concepts.
    - Test 3: draft/rejected/deprecated or foreign-institution endpoints do not satisfy has_relation; a confirmed-to-confirmed same-institution edge does.
    - Test 4: temporal and rejected/audit fixtures cannot contaminate current trusted aggregates.
  </behavior>
  <action>Apply one authorized Project institution equality-or-null predicate to `_partitioned_rows`, candidate-version selection, confirmed bindings, relation rows, pending-question reads, audit-event reads, and every other institution-bearing semantic query before counts or DTOs are built. Rework `_confirmed_relation_ids` with explicit source/target SemanticConcept aliases so both endpoints are project-scoped, institution-visible, and confirmed, matching the detail relation projection. Keep rejected/deprecated audit-only per D-07 and D-22, keep candidate bindings outside trusted counts/chains per D-25, and construct restricted references only after lawful row selection per D-27. Add adversarial fixtures for version, binding, relation, question, and audit rows rather than testing only a foreign root concept.</action>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_semantic_catalog_api.py -k "foreign_institution or confirmed_relation or audit_rows" -q -x</automated>
  </verify>
  <acceptance_criteria>No foreign-institution subordinate value or audit event appears in any response; relation filters/counts and detail relations agree; current trusted outputs exclude draft/rejected/deprecated counterpart concepts; confirmed same-scope behavior remains present.</acceptance_criteria>
  <done>All semantic subordinate rows and trusted relation aggregates share the authorized institution boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make the uncategorized facet a truthful server filter and retain bounded queries</name>
  <files>backend/app/services/semantic/catalog_query_service.py, backend/tests/test_semantic_catalog_api.py</files>
  <read_first>backend/app/services/semantic/catalog_query_service.py, backend/tests/test_semantic_catalog_api.py, backend/app/api/semantic_catalog.py, .planning/phases/11-semantic-catalog-ui/11-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: domain=__uncategorized__ returns both null and whitespace-only effective business domains and excludes named domains.
    - Test 2: the returned total, business-domain facet counts, page items, and stable null-last order derive from the same filtered population.
    - Test 3: the representative 701-concept request retains a positive bounded statement count and no per-row version/relation query growth after the isolation joins.
  </behavior>
  <action>Translate the wire sentinel `__uncategorized__` into a null-or-trimmed-blank domain predicate in the server matcher while retaining the sentinel in facet JSON for URL stability. Keep server totals/facets/pagination authoritative per D-02, D-05, and D-06. Re-run the representative query-count evidence after the new institution and confirmed-endpoint joins; do not add an index or migration unless the measured test crosses its existing evidence threshold.</action>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_semantic_catalog_api.py::test_uncategorized_facet_round_trips_null_and_blank_domains backend/tests/test_semantic_catalog_api.py::test_semantic_catalog_701_concepts_uses_existing_index_with_bounded_queries -q -s</automated>
  </verify>
  <acceptance_criteria>The sentinel round-trips through the real endpoint, filtered totals/facets/items agree, the measured query count remains bounded and positive, and no migration/index is introduced without failing evidence.</acceptance_criteria>
  <done>The uncategorized facet is server-truthful and the hardened projection remains set-based.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authorized Project -> semantic subordinate tables | Same project IDs do not prove the row belongs to the Project institution. |
| Semantic relations -> trusted aggregates | A confirmed edge is not trusted unless both endpoint concepts are confirmed and institution-visible. |
| Audit/history query -> response DTO | Explicit audit permission does not authorize rows from another institution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-05-01 | Information Disclosure | effective version resolver | critical | mitigate | Use an explicit unscoped sentinel so omitted, integer, and explicit-null institution calls cannot collapse into one default; scope joined concept and version rows and run adversarial route tests. |
| T-11-05-02 | Information Disclosure | binding/relation/question/audit partitions | critical | mitigate | Apply the same institution predicate before counts, DTOs, conflicts, audit events, or chains are built. |
| T-11-05-03 | Tampering | confirmed relation aggregates | high | mitigate | Require confirmed, institution-visible source and target concept aliases for every trusted relation flag/count. |
| T-11-05-04 | Denial of Service | added scope/endpoint joins | medium | mitigate | Retain set-based queries and the positive 701-concept statement/latency gate. |
| T-11-05-SC | Tampering | package installation | low | accept | No package installation is planned. |
</threat_model>

<verification>
<automated>$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_semantic_catalog_api.py backend/tests/test_semantic_layer.py backend/tests/test_governance.py -q -x</automated>
Confirm the adversarial fixture contains same-project foreign-institution subordinate rows and that none appears in catalog, detail, lazy-region, trusted-count, conflict, or audit output.
</verification>

<success_criteria>
The demonstrated cross-institution disclosure is closed across formal versions and every subordinate partition; relation semantics and uncategorized filtering are truthful; canonical temporal, audit-only, query-count, and existing semantic/governance regressions pass.
</success_criteria>

<output>
Create `.planning/phases/11-semantic-catalog-ui/11-05-SUMMARY.md` when done.
</output>
