---
phase: 11-semantic-catalog-ui
plan: 09
type: execute
wave: 9
depends_on: [11-08]
files_modified:
  - .planning/phases/11-semantic-catalog-ui/11-GAP-QUALIFICATION.md
autonomous: true
gap_closure: true
requirements: [SUI-01, SUI-02]
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Adversarial institution/project, temporal, rejected/deprecated, relation/facet, query-count, production-browser, build, lint, and high-risk regression gates pass with exact commands, exit codes, counts, and evidence recorded."
    - "CR-01, CR-02, WR-01 through WR-09, SUI-01, SUI-02, and D-01 through D-30 each map to direct implementation/test evidence in one qualification ledger."
    - "The normal executor creates only 11-GAP-QUALIFICATION.md; canonical security and UI review artifacts remain owned by the later `$gsd-secure-phase 11` and `$gsd-ui-review 11` gates."
  artifacts:
    - path: ".planning/phases/11-semantic-catalog-ui/11-GAP-QUALIFICATION.md"
      provides: "reproducible automated gate ledger, finding/decision coverage matrix, and explicit post-execution security/UI gate routing"
  key_links:
    - from: ".planning/phases/11-semantic-catalog-ui/11-GAP-QUALIFICATION.md"
      to: ".planning/phases/11-semantic-catalog-ui/11-REVIEW.md"
      via: "one evidence row per CR-01, CR-02, and WR-01 through WR-09"
      pattern: "CR-01"
    - from: ".planning/phases/11-semantic-catalog-ui/11-GAP-QUALIFICATION.md"
      to: "frontend/tests/semantic-catalog-browser.test.mjs"
      via: "named real-route browser evidence for loading, empty, error, unauthorized, stale-scope, keyboard/focus, restricted DOM, and long content"
      pattern: "production browser"
    - from: ".planning/phases/11-semantic-catalog-ui/11-GAP-QUALIFICATION.md"
      to: "$gsd-secure-phase 11 and $gsd-ui-review 11"
      via: "post-execution next-gate instructions without creating their canonical artifacts"
      pattern: "gsd-secure-phase"
---

<objective>
Qualify the complete Phase 11 gap implementation without claiming ownership of the dedicated security or UI review workflows. Per D-01 through D-30, SUI-01, and SUI-02, record reproducible automated evidence for every critical/warning repair and route the completed implementation to the two specialized post-execution gates.

Purpose: Produce an auditable automated release ledger while preserving workflow ownership boundaries.
Output: `11-GAP-QUALIFICATION.md` only, followed by explicit `$gsd-secure-phase 11` and `$gsd-ui-review 11` next gates.
</objective>

## Artifacts this phase produces

- One automated qualification ledger mapping all 11 findings, both requirements, all 30 decisions, and the four grouped gaps to executable evidence.
- Exact gate commands, exit codes, pass/fail/skip counts, query statement/latency evidence, browser assertions, and repository/package-lock diff evidence.
- Post-execution routing instructions; no executor-authored `11-SECURITY.md`, `11-UI-REVIEW.md`, or manual viewport evidence.

<execution_context>
@C:/Users/李儒伟/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/李儒伟/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-semantic-catalog-ui/11-CONTEXT.md
@.planning/phases/11-semantic-catalog-ui/11-RESEARCH.md
@.planning/phases/11-semantic-catalog-ui/11-PATTERNS.md
@.planning/phases/11-semantic-catalog-ui/11-UI-SPEC.md
@.planning/phases/11-semantic-catalog-ui/11-REVIEW.md
@.planning/phases/11-semantic-catalog-ui/11-VERIFICATION.md
@.planning/phases/11-semantic-catalog-ui/11-05-SUMMARY.md
@.planning/phases/11-semantic-catalog-ui/11-06-SUMMARY.md
@.planning/phases/11-semantic-catalog-ui/11-07-SUMMARY.md
@.planning/phases/11-semantic-catalog-ui/11-08-SUMMARY.md
@frontend/app/semantics/page.tsx
@frontend/app/semantics/[id]/page.tsx
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove critical isolation and production-browser paths first</name>
  <files>.planning/phases/11-semantic-catalog-ui/11-GAP-QUALIFICATION.md</files>
  <read_first>.planning/phases/11-semantic-catalog-ui/11-VERIFICATION.md, .planning/phases/11-semantic-catalog-ui/11-REVIEW.md, backend/tests/test_semantic_catalog_api.py, frontend/tests/semantic-catalog-browser.test.mjs, frontend/app/semantics/page.tsx, .planning/STATE.md</read_first>
  <action>Create `11-GAP-QUALIFICATION.md` and first run the named same-project foreign-institution version/subordinate tests through the real API, including omitted/integer/explicit-null resolver scope, confirmed-endpoint relation semantics, and uncategorized filtering. Then run the complete real-route browser suite from Plan 11-08. Record exact commands, exit codes, pass/fail/skip counts, named subtests, and the assertions that prove CR-01, CR-02, WR-01, WR-02, WR-03, WR-06, WR-08, and WR-09. Browser evidence must name `/semantics` and `/semantics/{id}` and distinguish loading, empty, error/retry, unauthorized, stale-scope, keyboard/focus, restricted DOM/accessibility absence, conflict sources, and long content. Do not record static SSR/fixture assertions as evidence for effects or interaction.</action>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_semantic_catalog_api.py -k "foreign_institution or confirmed_relation or uncategorized" -q -x; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test frontend/tests/semantic-catalog-browser.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }</automated>
  </verify>
  <acceptance_criteria>Both critical paths and every production-browser state/interaction assertion pass; the ledger records failures rather than waiving them and cites the real `/semantics` route.</acceptance_criteria>
  <done>Critical isolation and production interaction evidence is reproducible before broad regression qualification.</done>
</task>

<task type="auto">
  <name>Task 2: Run the complete regression, build, lint, and bounded-query gate</name>
  <files>.planning/phases/11-semantic-catalog-ui/11-GAP-QUALIFICATION.md</files>
  <read_first>.planning/phases/11-semantic-catalog-ui/11-GAP-QUALIFICATION.md, frontend/package.json, backend/tests/test_semantic_catalog_api.py, backend/tests/test_semantic_layer.py, backend/tests/test_governance.py</read_first>
  <action>Run the full focused backend semantic/catalog/governance suite, normal frontend test command (which includes the production browser file), production build, lint, and the established high-risk backend suite with its two explicit Windows-only deselections. After each external command, check its exit code immediately before starting the next command. Append exact results, counts, skips, warnings, the positive 701-concept statement count/latency, and a scoped `git diff -- frontend/package.json frontend/package-lock.json` result proving Plan 11 added no dependency/lockfile change. Keep automated gate evidence separate from the later human UI-review workflow.</action>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_semantic_catalog_api.py backend/tests/test_semantic_layer.py backend/tests/test_governance.py -q -x; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix frontend test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix frontend run build; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix frontend run lint; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; Push-Location backend; try { python -m pytest -q --deselect=tests/test_productization.py::test_windows_secret_acl_commands_remove_explicit_extra_access_and_are_idempotent --deselect=tests/test_productization.py::test_windows_lifecycle_script_without_action_keeps_control_console_open; $backendExit=$LASTEXITCODE } finally { Pop-Location }; if ($backendExit -ne 0) { exit $backendExit }</automated>
  </verify>
  <acceptance_criteria>Focused and high-risk backend suites, full frontend tests, production browser suite, build, lint, positive bounded-query sanity, and package/lockfile no-change evidence are recorded with no hidden earlier failure.</acceptance_criteria>
  <done>The complete automated regression gate is green and auditable.</done>
</task>

<task type="auto">
  <name>Task 3: Close the evidence matrix and route dedicated post-execution gates</name>
  <files>.planning/phases/11-semantic-catalog-ui/11-GAP-QUALIFICATION.md</files>
  <read_first>.planning/phases/11-semantic-catalog-ui/11-GAP-QUALIFICATION.md, .planning/phases/11-semantic-catalog-ui/11-CONTEXT.md, .planning/phases/11-semantic-catalog-ui/11-REVIEW.md, .planning/phases/11-semantic-catalog-ui/11-VERIFICATION.md</read_first>
  <action>Under a dedicated `Evidence matrix` heading, create exactly one structured row for every CR-01, CR-02, WR-01 through WR-09, SUI-01, SUI-02, and D-01 through D-30 identifier between the exact raw HTML comments `<!-- evidence-matrix:start -->` and `<!-- evidence-matrix:end -->`. Use the exact columns `ID | Production path | Named command/test | Result | Evidence location`: `Production path` is one existing repository-relative production file under `backend/app`, `frontend/app`, `frontend/components`, or `frontend/lib`; `Named command/test` contains the exact runnable command and named test/gate that established the row; `Result` is exactly `PASS`; and `Evidence location` is an existing repository-relative evidence artifact plus a heading or line anchor. Do not use blank cells or placeholder values such as dash-only text, TBD, TODO, N/A, pending, placeholder, unknown, not available, not applicable, or to be determined, including those tokens embedded in longer cell text. Include explicit rows for temporal boundaries, rejected/deprecated contamination, readable-nonnavigable versus restricted references, restricted DTO/DOM absence, two-source conflict overflow, two-item independent disclosures, and all production browser state classes. End the ledger with `Next gates` listing `$gsd-secure-phase 11` followed by `$gsd-ui-review 11`; state that those dedicated workflows own canonical `11-SECURITY.md`, `11-UI-REVIEW.md`, screenshots, exact 320x720/768x1024/1280x800/1440x900 viewport checks, and final human keyboard/visual approval. Do not create or pre-populate those artifacts in this plan.</action>
  <verify><automated>$file='.planning/phases/11-semantic-catalog-ui/11-GAP-QUALIFICATION.md'; if (-not (Test-Path -LiteralPath $file)) { exit 1 }; $text=Get-Content -LiteralPath $file -Raw; $start=([char]60)+'!-- evidence-matrix:start --'+[char]62; $end=([char]60)+'!-- evidence-matrix:end --'+[char]62; if (([regex]::Matches($text,[regex]::Escape($start)).Count -ne 1) -or ([regex]::Matches($text,[regex]::Escape($end)).Count -ne 1)) { Write-Error 'Evidence matrix markers must occur exactly once'; exit 1 }; $startIndex=$text.IndexOf($start)+$start.Length; $endIndex=$text.IndexOf($end,$startIndex); if ($endIndex -lt $startIndex) { Write-Error 'Evidence matrix markers are out of order'; exit 1 }; $matrix=$text.Substring($startIndex,$endIndex-$startIndex); $required=@('CR-01','CR-02','WR-01','WR-02','WR-03','WR-04','WR-05','WR-06','WR-07','WR-08','WR-09','SUI-01','SUI-02') + (1..30 | ForEach-Object { 'D-{0:D2}' -f $_ }); $rows=@{}; foreach ($line in ($matrix -split "`r?`n")) { if ($line -match '^\|\s*(D-\d{2}|CR-\d{2}|WR-\d{2}|SUI-\d{2})\s*\|\s*([^|]+?)\s*\|\s*([^|]+?)\s*\|\s*([^|]+?)\s*\|\s*([^|]+?)\s*\|\s*$') { $id=$matches[1]; if ($rows.ContainsKey($id)) { Write-Error "Duplicate evidence row: $id"; exit 1 }; $rows[$id]=@($matches[2].Trim(),$matches[3].Trim(),$matches[4].Trim(),$matches[5].Trim()) } }; $missing=@($required | Where-Object { -not $rows.ContainsKey($_) }); $extra=@($rows.Keys | Where-Object { $_ -notin $required }); if ($missing.Count -or $extra.Count -or $rows.Count -ne $required.Count) { Write-Error "Evidence row set mismatch; missing=$($missing -join ','); extra=$($extra -join ',')"; exit 1 }; $placeholder='(?i)(?:\b(?:TBD|TODO|pending|placeholder|unknown)\b|\bN/?A\b|\bnot (?:available|applicable)\b|\bto be determined\b|^\s*[-—]+\s*$)'; foreach ($id in $required) { $path,$gate,$result,$location=$rows[$id]; if (@($path,$gate,$result,$location) | Where-Object { [string]::IsNullOrWhiteSpace($_) -or $_ -match $placeholder }) { Write-Error "Blank or placeholder evidence cell: $id"; exit 1 }; if ($path -notmatch '^(?:backend/app|frontend/app|frontend/components|frontend/lib)/' -or -not (Test-Path -LiteralPath $path)) { Write-Error "Invalid production path: $id => $path"; exit 1 }; if ($gate -notmatch '(?:pytest|node\s+--test|npm\s+--prefix|test_[A-Za-z0-9_]|production browser)') { Write-Error "Unnamed command/test: $id => $gate"; exit 1 }; if ($result -cne 'PASS') { Write-Error "Non-PASS result: $id => $result"; exit 1 }; if ($location -notmatch '^[^#:\s]+(?:/[^#:\s]+)*\.(?:md|txt|log)(?:#.+|:\d+)$') { Write-Error "Unanchored evidence location: $id => $location"; exit 1 }; $evidencePath=($location -split '[#:]')[0]; if (-not (Test-Path -LiteralPath $evidencePath)) { Write-Error "Missing evidence artifact: $id => $evidencePath"; exit 1 } }; foreach ($route in @('/semantics','production browser','$gsd-secure-phase 11','$gsd-ui-review 11')) { if ($text -notmatch [regex]::Escape($route)) { Write-Error "Missing qualification route/gate: $route"; exit 1 } }</automated></verify>
  <acceptance_criteria>Every finding, requirement, and locked decision has one complete structured evidence row with an existing production path, named runnable command/test, exact PASS result, and anchored existing evidence location; duplicate IDs, missing IDs, blank cells, placeholders, nonproduction paths, unnamed gates, non-PASS results, and unanchored or missing locations fail automation; the ledger does not claim security/UI approval and routes canonical audits to their dedicated skills.</acceptance_criteria>
  <done>The executor-owned qualification artifact is complete and the required post-execution gates are explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Automated evidence -> closure claim | A green subset cannot stand in for omitted adversarial, browser, build, lint, or high-risk gates. |
| Normal executor -> dedicated audit artifacts | The executor may qualify implementation but cannot impersonate security/UI audit workflows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-09-01 | Repudiation | qualification ledger | high | mitigate | Record exact commands, immediate exit-code checks, counts, finding IDs, and evidence paths. |
| T-11-09-02 | Information Disclosure | isolation/browser gates | critical | mitigate | Re-run adversarial institution/project, restricted-output, and stale-request tests before recording closure. |
| T-11-09-03 | Denial of Service | query/build regressions | medium | mitigate | Record positive query-count/latency, full high-risk suite, production build, and lint. |
| T-11-09-04 | Spoofing | security/UI verdict ownership | high | mitigate | Create only the gap ledger and route canonical verdicts/screenshots/human approval to `$gsd-secure-phase` and `$gsd-ui-review`. |
| T-11-09-SC | Tampering | dependency supply chain | low | accept | Record scoped package/lockfile diff evidence; Plans 11-05 through 11-08 install no package. |
</threat_model>

<verification>
<automated>$file='.planning/phases/11-semantic-catalog-ui/11-GAP-QUALIFICATION.md'; if (-not (Test-Path -LiteralPath $file)) { exit 1 }; rg -n "CR-01|CR-02|WR-01|WR-02|WR-03|WR-04|WR-05|WR-06|WR-07|WR-08|WR-09|SUI-01|SUI-02|D-30|gsd-secure-phase|gsd-ui-review" $file; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }</automated>
</verification>

<success_criteria>All four grouped gaps and all 11 findings have green automated production evidence in `11-GAP-QUALIFICATION.md`; canonical security and UI verdicts remain pending until the dedicated post-execution skills run.</success_criteria>

## Multi-Source Coverage Audit

| Source | Items | Coverage |
|--------|-------|----------|
| GOAL | Full semantic catalog and detail in existing Next.js/Design Tokens | 11-05 through 11-09 cover safe API truth, `/semantics` catalog/detail UX repairs, real-route browser interaction, and automated qualification. |
| REQ | SUI-01, SUI-02 | Both appear in gap-plan frontmatter, production browser tests, and the final qualification matrix. |
| RESEARCH | Real API authority, SemanticConceptVersion truth, temporal/status/institution policy, bounded set-based queries, read-only governance | 11-05 server repair; 11-06/07 production UI/controller repairs; 11-08 browser harness; 11-09 automated gate ledger. |
| CONTEXT | D-01 through D-30 | Cited across objectives/tasks and exhaustively mapped in `11-GAP-QUALIFICATION.md`; D-27 is covered by strict DTO plus restricted DOM absence. |

Deferred Phase 12+ Workspace, Dashboard, DataQuality, Impact, SQL, free-form graph, fact store, package, migration, and unmeasured index work is excluded and is not a coverage gap.

<output>Create `.planning/phases/11-semantic-catalog-ui/11-09-SUMMARY.md` when done.</output>
