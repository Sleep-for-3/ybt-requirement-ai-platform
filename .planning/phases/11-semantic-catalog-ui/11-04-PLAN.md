---
phase: 11-semantic-catalog-ui
plan: 04
type: execute
wave: 3
depends_on: [11-01]
files_modified:
  - backend/app/schemas/semantic_catalog.py
  - backend/app/services/semantic/catalog_query_service.py
  - backend/app/api/semantic_catalog.py
  - backend/tests/test_semantic_catalog_api.py
autonomous: true
requirements: [SUI-02]
estimate:
  tokens: 42000
  raw_tokens: 21000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Detail shell and lazy-region responses are additive projections over canonical semantic, binding, relation, evidence, knowledge, lineage, governance, review, and version stores."
    - "The effective SemanticConceptVersion is resolved server-side for inclusive as_of dates; legacy fields and AI candidates never become formal definition fallback."
    - "Optional region permissions return HTTP 403, audit-only rows require audit.read, restricted references are constructed before serialization, and no index/migration is added without measured query-plan evidence."
  artifacts:
    - "backend/app/schemas/semantic_catalog.py detail and lazy-region DTOs"
    - "backend/app/services/semantic/catalog_query_service.py detail projections and set-based aggregates"
    - "backend/app/api/semantic_catalog.py detail and lazy-region routes"
    - "backend/tests/test_semantic_catalog_api.py detail isolation/security tests"
  key_links:
    - "detail route -> PermissionService/resource guards -> project/institution-safe projection"
    - "detail projection -> resolve_effective_versions(as_of) -> nullable formal version"
    - "region endpoint permission failure -> HTTP 403 -> frontend AsyncRegion unauthorized state"
    - "binding target permission -> readable/restricted union before JSON serialization"
---

<objective>
Build the backend detail and lazy-region read projections consumed by the governed `/semantics/{id}` frontend. Per D-09 through D-15, D-18 through D-22, D-25, D-27 through D-30, this plan supplies canonical temporal meaning, permission-safe traceability, lifecycle/workflow separation, audit isolation, and bounded region data without changing existing semantic mutation APIs.

Purpose: Make the detail route’s backend contract explicit and independently testable before the frontend assembles its tabs and visual regions.
Output: Strict detail/lazy DTOs, projection service methods, additive read-only endpoints, permission gates, and API/security regression coverage.
</objective>

<execution_context>
@C:/Users/李儒伟/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/李儒伟/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-semantic-catalog-ui/11-CONTEXT.md
@.planning/phases/11-semantic-catalog-ui/11-UI-SPEC.md
@.planning/phases/11-semantic-catalog-ui/11-RESEARCH.md
@.planning/phases/11-semantic-catalog-ui/11-PATTERNS.md
@backend/app/api/semantic.py
@backend/app/services/semantic/version_service.py
@backend/app/services/semantic/status_policy.py
@backend/app/services/auth/permission_service.py
@backend/app/services/auth/resource_guard.py
@backend/app/api/audit.py
@backend/app/models/semantic.py
@frontend/app/lineage/page.tsx
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define strict detail and lazy-region projection DTOs</name>
  <files>backend/app/schemas/semantic_catalog.py</files>
  <read_first>backend/app/schemas/semantic.py, backend/app/schemas/deliverables.py, backend/app/models/semantic.py, .planning/phases/11-semantic-catalog-ui/11-RESEARCH.md</read_first>
  <behavior>
    - Test 1: detail header represents nullable confirmed effective_version, lifecycle, separate review workflow, unresolved questions, conflicts, and declared per-region temporal capability.
    - Test 2: bindings, relations, evidence/knowledge, lineage, governance, and versions have explicit populated/empty/current-only/audit partitions and bounded list metadata.
    - Test 3: readable references include only lawful existing-route fields; restricted references contain entity_type and restricted=true and omit identifiers, names, codes, hrefs, titles, and metadata.
  </behavior>
  <action>Extend semantic_catalog.py with `extra="forbid"` read-only DTOs for the detail shell and each lazy region. Reuse exact semantic lifecycle, concept-type, binding-type, relation-type, and question-status literals. Model readable versus restricted reference unions, current-only flags, conflict summaries without a winner, review-task destinations, temporal interval fields, bounded chain overflow, and audit metadata. Keep DTOs projection-only and preserve D-11, D-15, D-17, D-19, D-20, D-21, D-22, D-24, D-25, D-27, and D-29. Do not add schema/migration artifacts; the resolved index/migration decision is evidence-gated during execution.</action>
  <verify><automated>python -m pytest backend/tests/test_semantic_catalog_api.py -q -x</automated></verify>
  <acceptance_criteria>DTO validation rejects undeclared fields, formal definition is nullable, candidate/audit/review/current-only dimensions are explicit, restricted unions cannot carry protected metadata, and no new persisted fact model is introduced.</acceptance_criteria>
  <done>All backend detail response shapes are strict, permission-safe, and aligned with canonical source vocabularies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement detail projection service and additive region routes</name>
  <files>backend/app/services/semantic/catalog_query_service.py, backend/app/api/semantic_catalog.py</files>
  <read_first>backend/app/services/semantic/catalog_query_service.py, backend/app/services/semantic/version_service.py, backend/app/services/semantic/status_policy.py, backend/app/api/semantic.py, backend/app/services/auth/resource_guard.py, backend/app/api/audit.py, frontend/app/lineage/page.tsx, .planning/phases/11-semantic-catalog-ui/11-RESEARCH.md</read_first>
  <action>Implement project/institution-scoped detail shell and standalone lazy routes for bindings, relations, evidence/knowledge, lineage, governance, and versions. Reuse resolve_effective_versions with inclusive `effective_from <= as_of <= effective_to`; never select a winner from browser ordering or legacy Concept fields. Build set-based region projections with confirmed/candidate/audit partitions, unresolved-question filtering, bounded Concept -> Target -> Mart -> Source data, conflict summaries without recommendations, and per-entity permission checks before DTO construction. Optional region permission failures must return HTTP 403 so the frontend can render a region-level unauthorized state; explicit rejected/deprecated filters or audit endpoints must require `audit.read`. Destination references must use the existing canonical route allow-list, with no route invented for unsupported entity types. Begin with existing indexes and no migration; only measured query-plan evidence during execution may justify an additive index/migration and a revised plan, per the resolved research decisions and D-09 through D-15, D-18 through D-30.</action>
  <verify><automated>python -m pytest backend/tests/test_semantic_catalog_api.py backend/tests/test_semantic_layer.py -q -x</automated></verify>
  <acceptance_criteria>Detail and lazy endpoints preserve safe 404/403 distinctions, return HTTP 403 for optional forbidden regions, resolve canonical effective versions, exclude audit facts from trusted paths/counts, require audit.read for audit-only visibility, cap chain output, and return no protected metadata in restricted references.</acceptance_criteria>
  <done>The additive backend detail contract is wired and permission-safe for all seven frontend tabs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add detail API security, temporal, audit, and query-plan regression tests</name>
  <files>backend/tests/test_semantic_catalog_api.py</files>
  <read_first>backend/tests/test_semantic_catalog_api.py, backend/tests/test_semantic_layer.py, backend/app/services/semantic/catalog_query_service.py, backend/app/api/semantic_catalog.py, .planning/phases/11-semantic-catalog-ui/11-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: project/institution isolation, visible-project 403, cross-project safe 404, and optional lazy-region 403 are distinct.
    - Test 2: inclusive effective_from/effective_to boundaries, ambiguity, no-confirmed-version, AI-only candidate, conflict, pending-review, and resolved-question exclusion are deterministic.
    - Test 3: rejected/deprecated rows require audit.read and never contaminate trusted counts/paths; successful audit reads remain visibly marked.
    - Test 4: restricted references contain no hidden identifiers or metadata; chain caps and overflow counts are stable; query count has no per-row effective-version or aggregate N+1.
    - Test 5: representative 700+ concept query-plan/latency evidence is captured; no index or migration is proposed unless measured evidence crosses the execution threshold.
  </behavior>
  <action>Extend the existing semantic test harness with focused endpoint assertions for every backend contract feeding SUI-02 and the 28-state UI matrix. Include permission, audit.read, redaction JSON inspection, temporal boundary, ambiguity, conflict, candidate, review, question lifecycle, chain cap, stable pagination, and query-count cases. Record representative query-plan evidence in the test output or summary; preserve existing test_semantic_layer.py as regression coverage. Do not add an index or migration merely because a query is broad, per the resolved evidence gate.</action>
  <verify><automated>python -m pytest backend/tests/test_semantic_catalog_api.py backend/tests/test_semantic_layer.py -q</automated></verify>
  <acceptance_criteria>Backend detail API tests pass for isolation, HTTP 403 lazy-region behavior, audit authorization, canonical temporal resolution, conflicts/candidates/reviews/questions, redaction, bounded chain, and N+1/query-plan safeguards; no schema change is made without measured evidence.</acceptance_criteria>
  <done>The backend contract is independently regression-tested and ready for the frontend detail plan 11-03.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Detail request -> projection API | Project/concept/tab/as_of/version inputs are untrusted and must pass scope and permission checks. |
| Projection service -> related stores | Binding, knowledge, lineage, review, audit, and question data have distinct authorization boundaries. |
| ORM rows -> response DTO | Protected related-asset data must be minimized before serialization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-11-15 | Elevation of Privilege | lazy region routes | high | mitigate | Enforce project.view plus resource-specific permissions; return HTTP 403 for optional forbidden regions. |
| T-11-16 | Information Disclosure | restricted reference construction | critical | mitigate | Build readable/restricted union before DTO serialization and assert JSON absence of hidden fields. |
| T-11-17 | Tampering | audit-only filters | high | mitigate | Require audit.read for rejected/deprecated exposure and exclude audit rows from trusted projections. |
| T-11-18 | Tampering | temporal version selection | high | mitigate | Delegate all effective-date resolution to resolve_effective_versions with project scope and inclusive boundaries. |
| T-11-19 | Denial of Service | detail aggregates and chain | medium | mitigate | Use set-based aggregates, bounded region/page sizes, capped chain output, and query-count regression tests. |
| T-11-SC | Tampering | npm/pip installs | low | accept | No package installation is planned. |
</threat_model>

<verification>
<automated>python -m pytest backend/tests/test_semantic_catalog_api.py backend/tests/test_semantic_layer.py -q</automated>
Review query-plan evidence before considering any schema/index change.
</verification>

<success_criteria>
The detail backend exposes strict additive DTOs and independent region endpoints with canonical effective-date semantics, HTTP 403 lazy-region authorization, audit.read isolation, server-side redaction, bounded projections, and complete security/temporal regressions.
</success_criteria>

<output>
Create `.planning/phases/11-semantic-catalog-ui/11-04-SUMMARY.md` when done.
</output>
